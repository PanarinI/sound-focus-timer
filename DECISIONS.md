# Decisions — почему так (журнал решений)

## 2026-07-05 — Материал звука: шум, не дрон (пересборка ядра)
**Решение:** уйти от синусового дрона (прототип mir) к **тёплому brown/pink-шуму + пространство
(реверб) + микро-дрейф**. Ядро — характер «Brown-ядро» из `lab/taste2.html`.
**Почему (не вкусовщина):** психоакустика — дрон сидит в зоне шероховатости 20–200 Гц + суб-бас 55 Гц
= измеримое напряжение; чистый устойчивый тон ухо метит как неестественный. Шум/природа мозг читает
как «безопасный фон» (парасимпатика). Рынок это подтверждает: всё любимое (myNoise/Noisli/brown noise)
— шум и полевые записи, НЕ синтез-дрон. Записей у нас быть не может → генерируем шум. Автор переслушал
прототип: «тот же звук с трубой, теперь напрягает» — загадка «раньше нравилось» разобрана (ночь+новизна
+фоновое внимание тогда; аналитическое вслушивание утром сейчас). Тест звука честен только ПОД работой.

## 2026-07-05 — Не-монотонность через ДРЕЙФ, не события (закон под ADHD)
**Решение:** живость звука — только медленный непрерывный дрейф (слои с несоизмеримыми LFO, сумма не
повторяется). Никаких макро-событий: порывов, мотивов, щелчков, скачков. Пульс/ритм — отвергнут
(автор: «рябит в голове»).
**Почему:** микро-дрейф = генеративный ров (лупы так не умеют без файлов) + мозг отпускает «живой» фон;
но у ADHD слабый сенсорный фильтр хватается за любую новизну → событие = отвлечение. Значит неровность
нужна, но гладкая. Это закрывает и вопрос «ритм как инструмент возбуждения»: нет.

## 2026-07-05 — Таймер НЕ обязателен: «шум-база + опциональная сессия» (разрешение узла)
**Решение:** база продукта = «нажал → пошёл шум» (без таймера; обслуживает «просто хочу шум» и
бессонницу); сессия/таймер = опциональный слой сверху. Нишу НЕ меняем.
**Почему:** автор спросил «а нужен ли таймер, может уйти в генераторы шума?» — но это УЖЕ измерено:
1-я итерация (чистый генеративный шум в CWS) = park (коммодити, бесплатно на YouTube/myNoise, форма
«extension» мертва 20/мес, денег в канале нет). Таймер — то, что делает нас продуктом, а не лупом:
даёт дверь (aesthetic pomodoro timer), деньги (IAP-сосед) и отстройку (мягкие границы, звук-как-время).
Разрешение не «или», а «и»: движок уже умеет оба режима (ручей + сессия). Бессонница — приятная тень
базы, не цель пивота (сон = контекст телефона, ещё коммодитизированнее, размывает ADHD-фокус).

## 2026-07-05 — Аудитория: ядро ADHD-взрослые; двери = симптомы, не «adhd app»
**Решение:** плацдарм — ADHD-взрослые. SEO/имя строить НЕ вокруг слова «adhd», а вокруг симптом-дверей
(visual timer 14.8k · time blindness 9.9k · brown noise · adhd timer). Полный разбор — орган `AUDIENCE.md`.
**Почему:** ключи «adhd + продукт» ≈ ноль (adhd app 10/мес) — люди с ADHD ищут свою боль, не ярлык, и
узнают себя сами. Студенты — больший круг, но забитый и неплатящий → хуже плацдарм (закон «мерь деньги,
не объём»). Звук для ADHD функционален (возбуждение+маскировка) — наш шумовой поворот механизм-верен
именно для этого ядра (бел/роз шум ✅ наука; brown 🔶 любим, но не доказан — без мед-обещаний).
Функциональные настройки (адаптация, не декор): возбуждение · маскировка · чувство времени · трение
старта · мягкие переходы. Красота (темы/палитра суток) — для эстет-кольца, не ADHD-функция.

## 2026-07-05 — Динамический arousal (идея автора)
**Решение:** уровень возбуждения звука — не только ручка, но и ПОВЕДЕНИЕ сессии: вход присутственный
(помогает войти) → по мере оседания в поток звук отступает (тише/спокойнее).
**Почему:** сходится с законом «в потоке звук меняется меньше» и с наблюдением автора «громкость
хочется всё тише со временем». В потоке поддержки нужно меньше — звук это уважает.

## 2026-07-04 — Генезис: рождён из разведки app-studio (решение автора, Фаза C)
**Решение:** гипотеза «фокус-таймер из звука» отпочкована в самостоятельный организм-проект;
студия остаётся мета-уровнем (закон отпочкования). Слова автора: «мы уже не только выбираем идею,
но развиваем гипотезу — а для этого нужно начинать строить».
**Почему выбран (не влюблённостью):** гейты измерены — ниша живая (Focus To-Do 500k+IAP · Otto
50k+IAP · StayFocusd 700k куплен Sensor Tower), трон бесплатного короля пуст (Marinara† 01.2026,
MV2), гейт Софт ✅×3, дверь «aesthetic pomodoro timer» Σ~1.6k/KD44-50 занята лишь неоптимизированным
(40 юзеров), боль «будильник рвёт поток» реальна (Reddit-вердикт: «designed for people who can't
focus, not for people who need to protect it»), веб-пара как SEO-канал доказана соседом
(pomofocus.io 112k органики/мес). Пред-оценка 🧮 ~37-40/50 — лучшая в реестре студии. Остаточный
риск назван: щель «звук-как-интерфейс» = 🔶 гипотеза — проверяется самим запуском (ставка низкая:
~$200-300, 1-2 недели). Полное досье: `app-studio/research/sound_focus_timer_niche_report.{md,html}`.

## 2026-07-04 — Две вехи и метаморфоза (постановка цели, автор)
**Решение:** Веха 1 — дойти до публикации в CWS (форма «стройка»). После публикации организм
НЕ закрывается — линяет в продукт-ДНК (как ExportGPT): мониторим и растим. Веха 2 — 2000 юзеров
(порог монетизации по методике). Возможные будущие вехи (переосмысление позиционирования,
оптимизация) — отдельные, не сейчас.
**Почему:** автор сперва усомнился «нужно ли разделять проект/продукт», сам же и разрешил: «раз
посадили организм — значит решили, что ему быть». Закон сада о метаморфозе снимает дилемму:
не два проекта, а две жизни одного организма. Мотив зафиксирован однозначно: **честный коммерческий
запуск по методике** (не арт-проект; тяга автора = актив, не аргумент).

## 2026-07-04 — Концепция: день-среда, сессия-событие (автор нащупал ядро)
**Решение:** продукт живёт весь день как тихая **среда** («ручей»), а фокус-блок — это её
**сгущение** в сессию. Не два продукта, а два состояния одного звука: разлитое ↔ собранное. «Мы не
про периоды — мы про день, внутри которого периоды приходят волнами». Помодоро (25/5) = один из
ритмов-пресетов, не суть. Страховка от съезда обратно в парк: продукт всё равно **продаётся как
таймер** (дверь спроса), но живёт как спутник дня — если убрать сессии, останется эмбиент-библиотека
(вчерашний парк).
**Почему:** сняло путаницу «при чём тут помодоро» и «зачем время суток, если мы про психологию» —
время суток = освещение среды (забота о теле, чтобы не выключать весь день); сессия = ядро (психология
фокуса). Слова автора: «волна подхватывает и приводит в фокус, и ты плывёшь — мне это нравится».

## 2026-07-04 — Закон «нет резких границ» (из наблюдения автора про «трип»)
**Решение:** ни одного резкого перехода во всём продукте — ручей вливается, собирание нарастает,
рассвет раскрывается, ниточка истончается, выключение гаснет. Всё — градиенты (технически: каждый
параметр движется через `setTargetAtTime`, скачков нет by design). Это и есть «обнимающее» качество.
**Почему:** автор про сценарий-2 (шумная среда): «в этом есть что-то психоактивное… трипы… плавный
вход и выход — обнимет и даст фокус». Отсутствие краёв — не фича, а физика продукта и отстройка.
Флагман позиционирования = сценарий-2 «фокус, который тебя держит в шуме».

## 2026-07-04 — Архитектура MVP + рабочее имя «Tide»
**Решение:** MV3-расширение. Звук — в **offscreen-документе** (играет непрерывно, переживает
переключение вкладок = условие «весь день»). Три поверхности: **иконка** (периферийное состояние) ·
**попап** (пульт: дышащая точка + одна кнопка по состоянию + крутилки характера) · **сам звук**
(главный дисплей — время в тембре). Движок + дирижёр сессии = один модуль `engine.js` без
chrome-зависимостей (переиспользуется в offscreen и в `dev.html`-стенде). Разрешения — только
`offscreen`+`storage` (никаких вкладок/хостов → чистейший листинг, аргумент приватности).
Рабочее кодовое имя продукта — **«Tide»** (волна подхватывает); финальное имя — на этапе брендинга
(⚠ без «Pomodoro»). Словарь состояний = 5 «слов» (ручей·собирание·ткань·углубление·рассвет + ниточка);
зов — v2 (но шина событий-фаз в движке заложена сразу).
**Почему:** offscreen — единственная форма для «спутника дня» (new-tab был бы клеткой). Общий модуль
даёт `dev.html` — автор слышит драматургию сразу, не загружая расширение.

## 2026-07-04 — Рамка MVP: драматургия без слежки; «зов» = v2 с шиной событий сразу
**Решение:** MVP = вход «минута до» · плато · выход-рассвет · палитра времени суток. Никакого
отслеживания вкладок/поведения в MVP. НО архитектура движка сразу строится на шине событий
(таймер-фазы = события), чтобы «зов» из дрейфа (v2) встал без перестройки.
**Почему:** слежка = широкие права → трение модерации CWS и деликатность приватности; драматургия
сессии самодостаточна как ценность (боль потока бьётся уже ею). Зов — сердце будущей уникальности,
его нельзя потерять архитектурно, но нельзя и тащить в первый релиз (усложнит и стройку, и листинг).

## 2026-07-10 — Стена (маскировка) = толщина, не яркость (фикс «воды по железу»)
**Решение:** развязать две оси. `energy` = яркость/стимуляция (brown→светлее) с ТЁПЛЫМ потолком среза (≤~1 кГц);
`masking` = ТОЛЩИНА стены (плотность+низ+уровень), растёт вниз, и на толстой стене убирает верхний «воздух».
**Почему:** прежняя маскировка поднимала СРЕЗ фильтра → на высоком значении звук резал как «плотная вода по
железному листу» (автор, ночью, без наушников). Стена портила НАШ звук вместо изоляции от внешнего. Развязка:
стена = укрытие (толщина), яркость — отдельная ось. «Кокон» — верная метафора для оси стены (слабый сенсорный
фильтр ADHD → изоляция), но кокон обязан быть КОМФОРТНЫМ, иначе сам стрессор.

## 2026-07-10 — Два слоя basic/premium; премиум продаёт опыт, не настройки
**Решение:** `tier` = basic (проверенное Brown-ядро + energy/masking/volume — «не отвергнет») · premium
(эксперименты: слой гармонизации; дальше — характеры-примеси). Дефолт basic.
**Почему:** жанр free-heavy → платный слой ручек умирает. Премиум обязан продавать РЕЗУЛЬТАТ/ОПЫТ (глубина,
характеры, темы — как Endel: Colored Noise/Nature Elements/Rainy), не «больше настроек». Базовый минимум = то,
что научно/спросово держит фокус и что автор подтвердил ухом; эксперименты — сверху, отдельным тумблером.

## 2026-07-10 — Слой гармонизации (премиум-эксперимент)
**Решение:** редкие (2.5–7.5 с) мягкие консонансные тоны (C-пентатоника), генеративно, без ритма, в реверб —
«воздушные трубки», не музыка (принцип саундскейпа Voices of the Void — обволакивающее присутствие).
**Почему:** автор искал «генеративную гармонизацию среды». Тон — мягкое СОБЫТИЕ, а закон «без событий» под ADHD →
слой живёт в ПРЕМИУМЕ-эксперименте (уши автора решают keep/cut), не в basic. Первый прогон был неслышимо тих
(баг: пик 0.05 + весь в реверб) → сделан слышимым, регулируется крутилкой.

## 2026-07-10 — Форма = side-panel, не попап (как реализован орб)
**Решение:** главная поверхность = **боковая панель Chrome** (side-panel), не попап. Клик по иконке открывает
панель (`side_panel` в манифесте + `openPanelOnActionClick` в background). Права `sidePanel`+`storage`.
**Почему:** попап ЗАКРЫВАЕТСЯ при клике мимо → орб не глянуть краем глаза. А смысл орба/убывающего кольца
(Time Timer) — периферийный взгляд ПОКА работаешь → нужна персистентная поверхность. Side-panel санкционирован
методом студии (PATTERNS: «new-tab/side-panel/виджет — почти апп-опыт», права чистые). **Отменяет «попап» из
решения 07-04** (offscreen для звука остаётся — вернём для персистентности; сейчас движок крутится в панели).

## 2026-07-10 — Таймер: кольцо = время, орб = звук-душа (Time Timer + Forest)
**Решение:** единый объект-компас. Кольцо-безель = ВРЕМЯ (крути=завёл, убывает в сессии — диск Time Timer;
безцифренно, цифра лишь пока крутишь). Орб-центр = ЗВУК-ДУША (дышит, теплеет с фокусом — дерево Forest, но БЕЗ
вины/смерти; клик = вкл/пауза). ⏻ = стоп (всегда виден).
**Почему:** «два языка про время» избыточно (автор поймал). Развязка: кольцо = «сколько осталось» (считываешь),
орб = «звук живой, растёт со мной» (ощущаешь) — разные вопросы, разные регистры. Безцифренность — не вкус:
Time Timer безцифрен by design (взгляд=статус, без чтения/подсчёта = ниже когнитивная нагрузка у слепоты ко
времени). Смерть дерева Forest = кнут/вина → против «мягкого фокуса»; берём рост (пряник), выкидываем смерть.

## 2026-07-10 — CWS решено; крупный игрок нишу не исключает (метод)
**Решение:** это **CWS-продукт** (не App Store; App Store-характер = отдельная будущая тропа). Endel/Brain.fm
рядом — НЕ стоп-сигнал.
**Почему:** метод прямо (`app-studio/RESEARCH.md:36`): «не пропускать идею из-за крупного игрока»; гейт занятости
закрывает ОПТИМИЗИРОВАННЫЙ листинг по нашему ключу, не присутствие бренда. Конкурируем словами (занятость) +
качеством + переводом доказанного спроса. Перепроверка гипотезы по методу (07-09, DFS свежий) держится ~37-40/50.

## 2026-07-10 — MV3: скрипт страницы расширения — только внешним файлом
**Решение:** вся логика `sidepanel.html` вынесена в `sidepanel.js` (внешний `<script src>`).
**Почему:** MV3 CSP `script-src 'self'` НЕ исполняет встроенный `<script>` в страницах расширения → обработчики
не навешивались, панель не кликалась. В песочнице-превью CSP нет → баг всплыл только в реальном расширении.
Урок: страницы расширения проверять В расширении (load unpacked), не только в standalone-превью.

## 2026-07-11 — ФОКУС восстановлен: таймер-центр, звук-тело (развёрнута ошибка «шум первичен»)
**Решение:** фокус инструмента = **фокус-ТАЙМЕР, чьё время сделано из звука**. Таймер = существительное
(дверь спроса, деньги, идентичность); звук = тело/душа/клин. Тап = запуск сессии-таймера (дефолт **25**);
«просто шум без времени» = тень, не главный вход. Якорь-строка живёт в STATE («Фокус инструмента»).
**Почему:** в сессии Клод выдвинул тезис «пользователь пришёл за шумом, таймер вторичен, тап=открытый шум».
Автор потребовал сверить с исследованиями (не поверил на слово) — и тезис **противоречит досье**:
двери спроса ТАЙМЕРНЫЕ (`pomodoro` 135k · `25 minute timer` 90.5k), `pomodoro timer with music` пустой (110);
1-я итерация (чистый эмбиент/шум) уже = **park**; гейт «1 функция» пройден как «таймер; звук = его тело».
Тезис откатывал в park. Урок метода: непроверенную интуицию Клода помечать 🔶, не выдавать за вывод;
фокус держать на виду (якорь в STATE), иначе расплывается.

## 2026-07-11 — Отстройка уточнена: два полюса ADHD, «сопровождать, не наказывать»
**Решение:** формула отстройки = **«сопровождать, а не наказывать»** — мягко ВВЕСТИ (лечит трение старта)
и бережно НЕ РАЗОРВАТЬ (защищает гиперфокус). Заменяет прежнее «защищать, а не создавать» как неполное.
**Почему:** автор кольнулся противоречием — Reddit-боль «designed for people who can't focus, not to protect it»,
а наше ядро = ADHD, которые вроде и есть «can't focus». Разрешение: ADHD — **дисрегуляция** внимания, не
отсутствие; у них ОБА полюса — тяжело войти (трение старта) И особенно больно, когда рвут (гиперфокус →
потеря контекста). Помодоро-наказатели дают только принуждение на входе и рвут звонком — худшее для ADHD.
Мы бьём в оба: мягкий вход (звук втягивает) + не-будильник-рассвет. Значит антитеза работает ДЛЯ ADHD, а не
против. (Автор узнал в этом себя.) Уточнение вписать в `AUDIENCE.md`. Вербатим r/ADHD ещё не добрали 🔶.

## 2026-07-11 — Форма: side-panel → floating overlay
**Решение:** главная поверхность — **floating overlay** (орб плавает в углу любой страницы, перетаскиваемый),
не side-panel. Звук — в offscreen · орб-визуал — content-script на странице · background — мост. Настройки
разворачиваются по клику. **Отменяет side-panel из решения 07-10.**
**Почему:** side-panel для ПОСТОЯННОГО интерфейса плох — занимает место, надо открывать/закрывать, орб не
глянуть краем глаза пока панель закрыта (слова автора). Метод студии допускает floating (PATTERNS: «new-tab/
side-panel/**виджет** — почти апп-опыт»); рынок подтвердил (Real Pomodoro — Floating Focus Timer, перетаскиваемый).
Только floating даёт «орб всегда в углу, краем глаза, пока работаешь на сайте». Цена = шире права (content-script)
→ **открытый узел: проверить модерацию** перед переездом (Фаза 2). Звук в offscreen нужен в любом случае —
чтобы сессия не рвалась при переключении вкладок (переформулировка «спутника дня»: не вечный фон, а звук сессии).

## 2026-07-11 — Душа орба = незримое оберегающее присутствие (костёр)
**Решение:** орб — **не персонаж**, а **душа сути**: незримое оберегающее ПРИСУТСТВИЕ, чьё тело = тёплый
дышащий орб, а голос = звук. Образ-ключ: «костёр, у которого сидишь спиной» (видишь отблеск/тепло, не морду).
Три «нельзя»: не являть существо · не давать взгляд (слежка) · не заставлять заботиться о нём. Три «да»:
дышит · теплеет с тобой · говорит звуком.
**Почему:** «наделить душой суть», а не «приклеить маскот» — душа эманирует из того, чем продукт уже является
(тёплый шум-время-защита), убери её — просто выключенный продукт, а не «продукт без талисмана». Апофатика
(«не знаю, что это») = не слабость, а **рыночная сила**: незримое = сосуд для проекции, каждый вкладывает свою
поддержку (глубже привязывает — нерв Finch/Calm/cozy). «Не глаз» совпало с нашим законом (никакой слежки,
приватность) — смысл+техника+рынок (body doubling = со-присутствие, не надзор) сошлись в одной точке.
Забота направлена НА пользователя (стихия греет/хранитель бережёт), не он ухаживает — иначе лишний долг = вина.

## 2026-07-11 — Орб НАРАСТАЕТ к полноте (не убывает); рост по площади; рассвет = опадание
**Решение:** визуальный таймер = орб **НАРАСТАЕТ** внутри кольца-завода (кольцо = цель/финиш, орб = плод,
зазор = остаток), полон ровно к концу заведённого; в рассвете **ОПАДАЕТ** в уголёк. Рост по ПЛОЩАДИ
(`r = √(min² + grown·(max²−min²))`), не по радиусу. Дыхание — фиксированной длительности. Звук в рассвете
гаснет синхронно (`0.5·(1−easeInOut(p))` = пропорционально размеру орба). **Инвертирует «убывающее кольцо» 07-10.**
**Почему:** психология автора — «рост приятен, исчезновение тревожит; время исчезает = тревога». Нарастание
консистентнее нашей позиции (мягкий фокус) и прошлого решения «рост, не смерть» (Forest без гибели): убывание-
в-ничто тайком тащит ту самую утрату, что мы выкинули. Принцип Time Timer (площадь = время без цифр) сохранён,
инвертировано лишь направление; остаток читается как тёмный зазор до кольца. Рост по площади — потому что глаз
читает площадь (∝r²): при линейном r орб «ускорялся» к концу. Фазы движка (вход/поток/рассвет) наложили на
ровный рост свою неравномерность — автор прочёл это как «живой рост организма фазами» (эмерджентно, не план).
Черенок-метка на 12ч = плодоножка; орб = вызревший плод. Убыв. движение орба по экрану — отвергнуто (дубль сигнала).

## 2026-07-11 — Взаимодействие: орб = единственная кнопка; завод и запуск разделены
**Решение:** орб — единственный орган управления. **Тап** = контекстный toggle (покой→старт · активное→выкл
плавным выдохом). **Кручение кольца** = выставить время, дуга «застывает» и ждёт; **запуск — отдельным тапом**
(2 действия: «выставил намерение → подтвердил стартом»). Метка-ноль = ориентир (откуда/куда крутить).
Убрано: кнопка ⏻ · словесные ярлыки фаз (ручей/собирание/… — язык движка, с экрана ИСЧЕЗАЮТ; состояние читается
только звуком+орбом) · бегунок-маячок (дублировал орб-рост) · пауза (в идеи — модуль).
**Почему:** ADHD — усталость от решений и слабый сенсорный фильтр → минимум органов, минимум чтения. Надписи-
ярлыки делали опыт непонятным (слова автора) — присутствие говорит без слов. Разделение завод/запуск — интуиция
автора: настройка времени и старт звука — разные акты. Дефолт (тап без завода) снимает порог входа для того, кто
пришёл просто начать. Пауза плодила залипание (звук на паузе оставался) → убрана до отдельного модуля.

## 2026-07-11 — Блокер — открытый узел (mindful-кандидат в v2, не MVP)
**Решение:** блокер сайтов в MVP — НЕ добавляем, но статус меняется с «не-цель» на **открытый узел для
переанализа**. Кандидат: **mindful/мягкий блок** (лёгкое трение-напоминание, не жёсткая тюрьма) как v2-модуль
нашим способом, отключаемый. Требует полноценной разведки по методу студии до решения.
**Почему:** топ поляны CWS — блокеры (StayFocusd 700k · Otto 50k · Website Blocker 20k) → спрос огромен; у ADHD
слабый импульс-контроль → внешний блок функционален. Против: наша антитеза «зовём, не запираем», широкие права
(→ трение модерации, «sus permissions» — реальный упрёк Tide), размывание MVP/клина (пустой трон = таймер+звук
БЕЗ блокера). Разрешение (слово автора «mindful»): мягкий блок ≠ наказание и ложится в душу-**хранителя** (орб
оберегает фокус не только звуком-стеной, но и мягким трением на отвлечении). Не решать на бегу — отдельная
разведка (донор блок-ниши · цена прав · отстройка mindful-блок vs StayFocusd · история Otto).

## 2026-07-12 — Форма: new-tab-ПОВЕРХНОСТЬ как MVP (без инжекта); мир-таверна и floating — метаморфозы
**Решение:** MVP-хост = **своя new-tab-страница** (`chrome_url_overrides`) — уголёк+звук+таймер, ноль host-прав, без инжекта на чужие страницы. Богатый «мир-таверна» (обои/уют/накопление) и floating-вынос (инжект) — метаморфозы ПОСЛЕ запуска. Отменяет «немедленный переезд во floating» из 07-11 (floating остаётся горизонтом, не первым шагом).
**Почему:** прогон через обновлённый метод (добор канала @its_capitan). ① «Метрика = активация ГЛАВНОЙ функции; кнопочки убивают друг друга» → богатый new-tab-мир рискнул бы сменить позиционирование с искомого «фокус-таймер» на **мёртвую дверь** `aesthetic new tab` (**20 запросов/мес** DFS 07-12; пересечение new-tab+pomodoro младенческое: Ashdeck 383·Provent 548). ② new-tab-ПОВЕРХНОСТЬ (в отличие от МИРА) = ноль прав, проще floating — упрощение, не усложнение. Развёл автору два «new-tab»: поверхность (хост MVP, ✅) vs мир (v2 мечта, риск). Снимает головную боль «не усложняю ли»: усложнение — не new-tab сам, а преждевременный мир.

## 2026-07-12 — Права floating разрешены: находка optional_host_permissions
**Решение:** узел прав floating (был открытым с 07-11) закрыт тремя путями. Для «орба поверх работы» — путь **C** `optional_host_permissions`: заявлен в манифесте с 1-го дня (задел, как велит метод про раннюю закладку прав), но спит → установка чистая (ноль предупреждений); «везде» юзер будит кнопкой в рантайме; апдейт с optional НЕ выключает расширение (в отличие от required → окно Remove → потеря базы).
**Почему:** проверено по докам Chrome. `<all_urls>` (путь A) = предупреждение «читать все данные» + углублённое ревью 1-2 нед + риск reject → бьёт клин приватности. C достигает цели метода (не потерять базу на росте прав) БЕЗ платы за широкую установку — «выполняет наказ метода лучше самого метода». Приём чист (Google рекомендует), финальную планку ставит ревью CWS. Иллюстрация-находка → личная папка `2026-07-12-optional-permissions-clean-start.svg`.
**⚠ Поправка 07-17 (факт-чек канона по требованию автора):** «углублённое ревью 1-2 нед + **риск reject**» для пути A — **наслоение Клода, каноном НЕ подтверждено**. Курс (`knowledge-base/module-3/3.6-publish-cws.md:152-154`) host-права считает нормой: даёт готовый пример обоснования («show extension action button on any website»), запрет один — не просить прав СВЕРХ функционала. Значит optional (путь C) — не обязательная защита от ред-флага, а опция (чистая установка ценой пробуждения); **required host для присутствия-с-1-секунды допустим по методу** (норма класса: Sider/Grammarly). См. решение формата 07-17 ниже.

## 2026-07-12 — Монетизация: коррекция «широкие права ≠ все продукты»; подписка-компаундинг
**Решение:** наша монетизация = IAP/подписка (5.3 способ №1, продвигаемый устно) — широких прав НЕ требует. Floating просит права ради ПРИСУТСТВИЯ, не денег. Модель: **подписка** (регулярный продукт → компаундинг, не lifetime), премиум продаёт ОПЫТ (глубина/характеры/мир), не настройки; НЕ freemium-ловушка (щедрая проба → пейволл на весь опыт после 4-5k WAU).
**Почему:** Клод ошибочно протянул «монетизация→широкие права» (представил рекламу-на-чужих-страницах как модель буткемпа). Сверка 5.3 (сохранена в `app-studio/knowledge-base/module-5`): меню 6 способов, IAP первый, рекламные способы живут на СВОИХ поверхностях (Welcome Page/new-tab). Горизонт студии 3-5 лет = идеальное условие компаундинга подписок (метод). Авторизация −95% конверсии → MVP ноль аккаунтов (совпало с приватностью), вход только после WOW в 1 клик.

## 2026-07-12 — Суть обогащена: уголёк+его мир; глубина в связке экст→апп, не в раздувании
**Решение:** орб = **уголёк-очаг** одного присутствия в двух проекциях (new-tab=дом-пристанище · floating=вынос очага в работу; один звук/состояние). Накопление = whiteboard-ritual автора (тепло мира, без вины/цифр). «Интереснее просто помодоро» = **связанный апп-продукт** (Readlax-модель: таймер-дверь воронит в глубокий продукт), НЕ раздутый таймер.
**Почему:** автор искал глубину/эстетику и честно назвал драйв «сделать мир побогаче, сразу фундамент». Разрешение методом: глубина живёт в СЛЕДУЮЩЕМ продукте связки (горизонт экст→апп, iOS ×7.5 денег), а MVP-таймер прост, но не скучен (звук-душа). Планирование (телескоп студии — сила) ≠ раздувание MVP: планирование ведёт к УЗКОМУ MVP под искомую дверь. Образ таверны/очага — из образов автора (таверна/кафе, непогода за окном, уголёк как часть мира). Схема-эскиз показана; в `schemas/` занести когда форма устоится.

## 2026-07-12 — Прогон метода: щель «звук как тело» легализована как сигнал
**Решение:** щель «звук-как-интерфейс времени» перестаёт быть только 🔶-влюблённостью — метод даёт ей опору.
**Почему:** обновлённый метод «плохой UX/кривой лидер = сигнал ниши» → Real Pomodoro (ровно наш угол floating+звук, но «ужасный записанный звук», 29 юзеров) + гиганты со звуком-придатком (Focus To-Do 500k — лупы) = валидная щель «сделать звук телом», не наша фантазия. demand-first как ГЕЙТ: дверь-таймер искомая (✅ pomodoro 165k, aesthetic pomodoro KD7🟢), мир неискомый (park до запуска).

## 2026-07-16 — Рамка MVP: очаг-страница + PiP-вынос; не блокер; онтология жар/угли/очаг (сменяет new-tab-рамку 07-12)
**Решение:** MVP-хост = **очаг-СТРАНИЦА** расширения (открывается кликом/хоткеем, БЕЗ `chrome_url_overrides`) + **PiP-вынос** жара поверх всех окон (Document Picture-in-Picture, пермишенов не требует). На очаге — угли дня (дискретные следы сессий). Тап-25 + кручение. БЕЗ блокера · БЕЗ цифр-статистики · БЕЗ палитры суток (v2, реш. автора). «Очаг новой вкладкой» = спутник-расширение v1.x (override опциональным не бывает — это декларация манифеста, не право; в манифесте = предупреждение при установке). Онтология починена: **ЖАР** (орб, одно присутствие) · **УГЛИ** (следы сессий) · **ОЧАГ** (дом) — раздвоение «уголёк=орб и уголёк=след» снято. Подтверждено автором («да/да»).
**Почему:** доразведка 07-16 (датасеты `app-studio/research/focus_timer_{doors,newtab_census}_2026-07-16.csv` + `format_probe.md`): (1) **формат ⊥ дверь** — поверхность не ищут (floating 140 · PiP 30 · always-on-top 10) → формат выбирается сценарием, и ядровой сценарий ADHD («externalize time»: таймер виден, пока работаешь) держит только PiP; (2) перепись: **ни один таймер ≥10k не живёт в new-tab** (потолок Kaizen 1000), new-tab-миллионники = красота-дашборды с каналом вирусности-самопоказа, не поиска (Momentum 2M; Flocus-ext = 6k и без таймера — их таймер в веб-аппе); (3) PiP-угол младенческий (Focus Timer Pro 19 · Real Pomodoro 29 · Floating Timer 1k generic) — щель «присутствие+тёплый звук в PiP» пуста, прецеденты формата в CWS есть; (4) блокер = парадигма наказателей + host-права, а чистые таймеры живут огромными без него (pomofocus 112k органики/мес) — отстройка «зовём, не запираем» дороже table-stakes-блока; mindful-блок остаётся узлом v2; (5) retention-архитектура: место (PiP=«таймер на столе») · дозированная новизна (генеративный звук) · накопление (угли) · подстройка ритма (кручение) — все четыре паттерна жанра закрыты. Двери-тренды: растёт мягко-ADHD (visual timer adhd 1600 **+164%/г** · gentle +40% · flow +23% · adhd timer +14%), падает generic (aesthetic pomodoro 880/KD7 −19%/г); ADHD ищет видимое время, НЕ метод (pomodoro adhd = 140). SERP `visual timer adhd`: физтовары + один софт-инди (visualtimer.com, поз. 3) → пробиваемо; AI Overview рекомендует инструменты, не заменяет их = ниша ИИ-устойчива.

## 2026-07-16 — Образ доопределён: НЕ персонаж; дверь-помодоро совместима с душой-сопровождением
**Решение:** образ = **безымянная вещь-присутствие** (жар в очаге), НЕ маскот с именем. Закон трёх нельзя подтверждён (не являть существо · не давать взгляд · не заставлять опекать). «Огонёк/теплячок» — нежность в чате, не имя в продукте; юзер сам даст имя, если захочет (пустое место для проекции сильнее навязанного персонажа). Ember — в копилку имени продукта (веха 1, с бронью). Дверь остаётся таймерной (включая pomodoro-слова) при душе-«сопровождении».
**Почему:** (а) тамагочи-поле занято (Otto 60k · Forest · Study Bunny), поле «вещь-с-душой в браузере» пусто — отстройка там; (б) вещи-без-персонажа живут крупнее тамагочи (Time Timer — компания, флип-часы 68k, Not Boring Timer); (в) инсайт автора 07-16: продукт сопровождает ПОТОК любимого дела, а не заставляет («защищает фокус, а не подгоняет») — совпал с вербатимом ниши 07-04 («designed for people who can't focus, not for people who need to protect it») = сцепка вкуса и данных; (г) «pomodoro» в спросе = слово-категория, не верность методу 25/5 (aesthetic-искатели = вайб-искатели; ADHD-искатели = видимое время); тап-25 обслуживает привычку-дверь, кручение (+ кандидат «∞» на сборке) обслуживает сопровождение потока.

## 2026-07-16 (вечер) — Механика закрыта вкусом автора: спека MECHANICS.md v1.1
**Решение:** все микро-развилки механики закрыты (свод — MECHANICS §10). Ключевые упрощения родились из замечаний автора: (1) **угли-слепки** (диаметр=длительность) вместо «порога чести» — порог упразднён: искорка за 3 минуты честна и не укоряет, инфляция полки невозможна физикой образа (интуиция автора «кружочки разного диаметра»); (2) **фаза перерыва упразднена** — «перерыв» = поведение приглашений в покое (первое через 5 мин — эхо помодоро; из уснувшей паузы — тоже; «кто сказал, что человек» — автор против диктовки ритма); (3) **жесты по-браузерному** — «долгий тап» изгнан (CWS!), гашение = докрутка завода в ноль → медленный рассвет; кручение на ходу разрешено (боль автора на стенде); (4) **клик в рассвете = «ещё 5 минуток»** (+5 в плато — гиперфокус не рвём); (5) **∞ спрятан в докрутку до упора** — снимает противоречие «таймер vs сам решаешь»: завод = обещание себе, ∞ = крайняя точка той же шкалы, находится любопытством (лестница открытий); (6) **жар без топлива** — не костёр (ни поленьев, ни пламени), чтобы образ не обещал звук crackling fireplace, которого нет (у нас brown); редкое синтез-потрескивание = опция покоя, еле-еле; (7) **лестница открытий** = принцип автора «продукт рассказывает себя» (уголь → приглашения → кручение → ∞ → PiP → полка). Автомат сжался до 4 состояний (покой→вход→плато→рассвет) + пауза-подсостояние.
**Почему:** каждое замечание автора УПРОЩАЛО механику, не усложняло — признак того, что образ несёт механику сам (физика тепла вместо правил). Критерий приёмки образа зафиксирован словами автора: «честная и по-настоящему, а не декоративно тёплая». Полка «на будущее»: умный слой (ИИ/паттерны — «легко переборщить», родня v2-«зова», шина уже заложена) · рилсы-промо сценариев · циклы 4×25 (возможно never). Сценарий регулярного дня (🔶 синтез) — MECHANICS §8; проверит запуск + ГОЛОСА.md.

## 2026-07-16 (ночь) — Юзер-тест №4: жесты выводятся из СЦЕНАРИЕВ; «кручение на ходу» удалено целиком
**Решение:** канон §3 MECHANICS переписан в форму «сценарии → жесты» (S1 старт · S2 время-в-покое · S3 пауза · S4 «+5» в рассвете · S5 пауза→«☀ завершить» · S6 конец ∞ = S5). **Жест вне сценария не существует.** Кручение во время сессии удалено (в сессии скролл не перехватывается вовсе); раннее завершение = единственная кнопка «☀ завершить», живущая только в паузе (едет в PiP вместе со сценой).
**Почему:** три итерации фиксов кручения-на-ходу (сглаживание → аккумулятор → разделение каналов) чинили симптомы, а хаос остался: свайп доматывал сотни секунд, солнце-вспышки, «скролл зумит орб» (слова автора: «непонятный хаос… сделай сначала СЦЕНАРИИ и убери всё лишнее»). Разбор по сценариям показал: фича избыточна — продление покрыто S4 («+5» кликом в рассвете, сколько угодно раз), раннее завершение — S5 (пауза = естественная прихожая конца); а тащила она полы/потолки/вспышки/аккумуляторы = весь источник хаоса. Урок метода (третий за день, самый дорогой): **сценарии → жесты, не наоборот; фича без сценария = генератор багов.** Прогон после чистки: 40 скролл-событий в сессии → дуга 0px сдвига, вспышек нет; пауза → ☀ → рассвет → уголь ✅.

## 2026-07-16 (ночь) — Юзер-тест №5: «+5 в рассвете» — на полку; пауза-НЕ-обнуление подтверждено; ☀ принят
**Решение:** клик в рассвете = ничего; «+5 минуток» уходит на полку (вернуть ОПЦИЕЙ с облачком-подсказкой «ещё 5 минут?»). Кнопка «☀ завершить» в паузе — принята автором («хороший ход, главное чтобы читалось» — читаемость проверяется глазами). Пауза без обнуления — подтверждена как ценность («хороший момент»).
**Почему:** неявный контекстный жест (клик в рассвете делает не то, что клик в плато) = сюрприз-поведение без подсказки — против заслона «без непродуманных сценариев». Механика extend() остаётся в движке — полка, не могила.

## 2026-07-17 — Юзер-тест №6: ПРИНЦИП ДВУХ КОНЦОВ (рассвет ≠ выдох); стоп — первичный жест
**Решение:** разведены два завершения. **Таймер решил** (время вышло) → рассвет ~30–40с (церемония un-alarm). **Я решил** («завершить») → быстрый ВЫДОХ 3–5с → тишина+уголь (новая фаза движка `угасание`/`extinguish(sec)`). Клик во время ЛЮБОГО угасания = оборвать в тишину сейчас (баг «после завершить орб некликабелен» убит). «Завершить» видна ВСЮ сессию (тихая, всегда под рукой), не спрятана за паузой. Прод-рассвет укорочен 150с→40с.
**Почему:** автор — «жать кнопку и ждать ещё 2 минуты — сложно представить такой сценарий… у меня вообще нет возможности всё выключить сразу». Корень: я слил ручной стоп с автоматическим рассветом. Психологический ключ: **церемония хороша, только когда решение НЕ моё** — когда таймер закончил, плавность = подарок; когда я закончил, я уже мысленно вышел, каждая секунда = трение. Стоп — первичный жест (не вторичный за паузой). Урок метода (продолжение №4): сначала СЦЕНАРИЙ ЧЕЛОВЕКА, потом жест.
**Тех-заметка (важно для автора):** `<script src>` без версии → браузер кэшировал старый `engine.js`, и часть правок ДО автора не доходила (он тестировал местами старый код — источник части «хаоса»). Лечение: `?b=N` на скриптах стенда + хард-релоад (Cmd+Shift+R). Прогон после фикса: «завершить»→выдох 1.8с (fast)→уголь+покой ✅; клик в выдохе→обрыв 0.16с ✅.

## 2026-07-17 — Формат ЗАНОВО ОТКРЫТ: разворот к side-panel (сомнение автора в PiP); видимая ручка завода
**Решение (гипотеза, ждёт вкуса автора):** PiP собран и работает, но автор усомнился — «уродливая системная рамка + отдельное окно = громоздко, не уверен что нужно». Разбор ВСЕХ поверхностей расширения глазами 5 портретов (AUDIENCE) → **разворот:** дом присутствия = **Side Panel** (докнутая тёплая полоса, орб рядом с работой, ноль host-прав, без рамки) + **уголёк-иконка в тулбаре** (пилот-лайт) · **PiP → опция** «поверх ОС» (Тео в IDE) · **new-tab → опция-дом** v1.x. Merlin-паттерн (инжектнутый язычок у края) — ещё вариант лаунчера (цена: host-права). Не закрыто: крест «в браузере vs поверх ОС».
**Почему:** PiP давал присутствие-над-всем, но ценой громоздкости/уродливой анти-спуфинг-рамки. Портреты показали: Ре/Ая/Ми/Лу работают В браузере → им нужен орб рядом, красиво, без рамки = ровно Side Panel (докнута, живёт через вкладки, ноль прав). Только Тео (вне браузера, IDE) реально нужен PiP-над-ОС → он опция, не дефолт. Side-panel мы отвергали 07-10/11 ради floating-над-страницей — но тогда искали присутствие-поверх; теперь, когда PiP показал цену, докнутая полоса = не компромисс, а чистый ответ. Прогон узкого формата (380px) скриншотом: орб ложится естественно.
**Видимая ручка (юзер-тест: «куда делась настройка времени?»):** установка времени была невидимым жестом (кручение) → не находима, на телефоне спорила с прокруткой. Фикс: **огонёк-ручка на кольце** (видно → тянут) на позиции завода + `touch-action:none` (надёжный тач). В сессии прячется (кольцо = дуга-остаток). Это правка №1-находимость из портретного разбора, всплывшая живьём.
**Публикация стенда:** обновлён `panarini.github.io/orb-stand` (index.html=hearth, HTTPS→PiP работает). Пуш — токеном из keychain «IntelliJ Platform GitHub» (`git push https://$TOKEN@github.com/...`; osxkeychain в non-interactive shell не читается). Pages иногда виснет в «building» — пустой коммит пинает. Дал автору 3 стенда: localhost · LAN-IP (та же WiFi, без PiP) · Pages (HTTPS, с PiP). PiP на iOS не работает (Document PiP = только десктоп; наш продукт и есть десктоп-CWS).

## 2026-07-17 (продолжение) — Формат РЕШЁН с автором: лестница присутствия; host-права не ред-флаг
**Решение:** крест «в браузере vs поверх ОС» закрыт — ядро В браузере → **дом = Side Panel** (весь жар + богатое: кручение/настройки/угли), «поверх ОС» = PiP-опция. Добавлен слой присутствия по мысли автора: **язычок-закладка у края страниц** (Merlin-паттерн) как **живой отблеск** (уголёк дышит/зреет/статус сессии, клик → открывает дом) — НЕ полный орб (полный орб на краю = floating, дом растворяется; богатое остаётся в доме). Лестница: тулбар-иконка (база) · Side Panel (дом) · язычок-отблеск · PiP-опция.
**Почему host-права перестали быть барьером:** факт-чек по требованию автора («проверь меня»). Канон `knowledge-base/module-3/3.6-publish-cws.md:152-154`: host permissions = штатный пример обоснования («show extension action button on any website»), единственный запрет — не просить прав СВЕРХ функционала. Наш язычок host требует функционалом → честно. Ред-флага по методу НЕТ; запись 07-12 («риск reject») — наслоение, снято (поправка там). Клин отстройки переформулирован: не «ноль прав» (это было НАШЕ позиционное решение против Tide, не метод), а **«права прозрачны, только ради присутствия — не следим»** (content-script рисует орб, не читает страницу). Уголёк-на-всех-страницах = по сути floating (07-11), но два прежних возражения растворились: (а) права — не ред-флаг; (б) «занимает место» — язычок у края свёрнут. Форма не поехала назад, а дозрела.
**Открытый под-узел (на сборке):** обёртка дома — нативная Chrome Side Panel (чисто, вне страницы, надёжно, но вид в рамке Chrome) vs наш инжектнутый сайдбар (Sider — свобода образа/перетекания отблеск→очаг, но поверх страницы, конфликты, не на chrome://). Раз host берём для язычка — инжект доступен даром. Механизм «клик язычка → open дом» 🔶: `sidePanel.open()` требует user-gesture — обвязку content-script→background проверить на сборке.

## 2026-07-22 — Сборка экста: offscreen-архитектура · язычок-клик упёрся в user-gesture · баг спасённого рассвета
**offscreen — «дальний движок» с тем же контрактом.** Звук вынесен в offscreen-документ (`offscreen.js`), чтобы переживал закрытие side-panel; панель говорит с ним через `remote.js` — прокси с ТЕМ ЖЕ публичным контрактом, что `AudioEngine` (`turnOn/startSession/pause/...`). Почему так: логику панели (`hearth.js`) не пришлось трогать вообще — она не знает, движок локальный или дальний. Выбор движка: `chrome.runtime.id` есть → `RemoteEngine`, нет (localhost-стенд) → `AudioEngine` напрямую, верстак цел. **Ловушка, снятая явно:** `performance.now()` у каждого документа СВОЙ отсчёт → сырые метки времени из offscreen в панели = чушь; offscreen шлёт «сколько прошло» (elapsed/phaseElapsed), прокси пересчитывает в локальные метки. Разрешение `offscreen`, host-прав НЕ добавили.
**Язычок-клик → дом: развилка разрешилась НЕ в пользу.** `chrome.sidePanel.open()` требует жеста пользователя. Клик по язычку — жест, НО он в content-script, а до SW идёт через `runtime.sendMessage` → активация жеста НЕ переживает этот переход, Chrome тихо отклоняет. Это документированное ограничение MV3 (под-узел 07-17 «проверить на сборке» — проверен). **План Б:** дверь домой = иконка в тулбаре (там жест живой, `openPanelOnActionClick` уже стоит), язычок = чистое присутствие + клик = пауза-тишина. Урок: под-узлы с `sidePanel.open`/user-gesture в MV3 планировать сразу с планом Б, не как «доделаем на сборке».
**Баг спасённого рассвета.** `lastGrown` объявлялась и читалась (`grownAtDawn=lastGrown` в рассвете, `grownAtExt=lastGrown` в выдохе), но ей НИКОГДА не присваивали → вечный 0 → тело схлопывалось мгновенно и в рассвете, и в выдохе. Церемония рассвета — смысловое ядро («un-alarm», принцип двух концов) — визуально не работала С РОЖДЕНИЯ; CSS-переход .5с маскировал прыжок, потому не бросалось в глаза на стенде. Плюс: движок в фазе угасания не тикает → панель не перерисовывалась; добавлена своя дорисовка кадров (`startFadePaint`). Урок: «работает на глаз» ≠ работает — короткий CSS-transition прячет отсутствие логики; ключевые анимации проверять замером промежуточных кадров, не только «до/после».
**Мелочи-решения автора:** дефолт завода 25→**15** (ADHD-канон 15/5 + шанс дожить до рассвета в первом сеансе) · ручка завода возвращается на 0.6 угасания (раньше, чем звук истает) · вспышка «звук пошёл» над орбом (системный мьют браузеру недоступен — показываем лишь свой ползунок) · Welcome page = ОДНА задача pin (автор: «раздвоив задачу, рискуем не выполнить ни одну» — снял мою попытку навесить туда ещё звук+наушники).
**Урок про STATE (мой промах):** отблеск сперва сделал разливом света по низу экрана — автор искал его 5 минут, почувствовал «подпекает снизу». Облик БЫЛ решён 07-17 (язычок-закладка), но я не сверился со STATE и взял метафору «свет из-под двери» буквально. Сверяться со STATE ДО реализации, а не после отклика автора.

## 2026-07-22 (вечер, студийная сессия) — Язычок: ТОЛЬКО дверь · зеркальность · жест ВЫЖИЛ (поправка утра)
**Решения автора:** (1) язычок = **только дверь** («присутствие, с которым нельзя взаимодействовать — пусто»);
план Б «язычок=пауза» снят. (2) Облик v1 отвергнут («растёт с нуля — не сразу замечаешь»); v2 = закладка-ПРЕДМЕТ:
выезжает из-за кромки (движение ловит периферия, непрозрачность не гуляет), жила света внутри (жар кормит свет,
не габарит), ховер-выезд + title, отказ = качание «нет». Стенд: `lab/glow-stand.html`.
**Факт-переворот: жест ВЫЖИВАЕТ.** Утренняя запись «user-gesture не переживает messaging» была правдой
ИЗМЕРЕНИЯ, но не приговором: отказ никто не читал (ответ глотался), форма была `{windowId}`. Синхронный
`sidePanel.open({tabId})` прямо в onMessage — работает (проверено автором живьём: «теперь кликабельно»).
**Урок: «Chrome тихо отклоняет» ≠ «нельзя» — сначала прочитай ТОЧНУЮ ошибку, потом выноси вердикт.**
**Зеркальность (ощущение автора №1 «дрессируем на ярлычок, а закрывать крестиком»):** клик по язычку при
открытой панели = закрыть её. API закрытия нет — панель закрывает СЕБЯ (`window.close()`) по сигналу через
уже живой порт `panel`. Следствие-пересмотр: язычок горит, пока идёт СЕССИЯ, независимо от панели (прежнее
«панель открыта → отблеск не нужен» снято — иначе закрывать нечем). Обе двери теперь toggle (иконка — нативно).
**Мёртвый язычок на других вкладках (ощущение №2):** Chrome замораживает фоновые вкладки (Memory Saver) —
они просыпают рассылки состояния/off. Лечение: досылка правды в момент взгляда (`tabs.onActivated` +
`windows.onFocusChanged` → `ensureGlowInTab`: инжект идемпотентен + свежий state/off); в скрытой вкладке
появление БЕЗ анимации (rAF заморожен; выезд — событие старта, на чужой вкладке язычок просто СТОИТ).
**Приёмка за автором (живой Chrome):** toggle обеими дверями · вкладки оживают без перезагрузки · после
конца сессии язычок исчезает и на фоновых вкладках (при переключении).

## 2026-07-22 (ночь, та же сессия) — Приёмка автора: сироты поколений · опциональность язычка · ретрай дома звука
**Три находки приёмки и их корни:**
1. **«Старые вкладки неактивны до перезагрузки» = СИРОТЫ ПОКОЛЕНИЙ.** После перезагрузки экста content-script
прошлого поколения остаётся в DOM с мёртвым chrome.runtime, а мой guard «уже висит — выходим» не пускал новый
скрипт. Лечение: **усыновление** (новый glow.js сносит старый host и ставит свой) + **ping-протокол** (живой
язычок текущего поколения отвечает фону — скрипт не переливается, дублей-слушателей нет; молчание = влить).
Уборка сирот и после конца сессии: на activation шлём «off», некому принять → скрипт-уборщик сносит host.
Это же закрыло «пропадает при переходе с открытым сайдбаром» (тот же корень).
2. **«Кружок в панели молчит после 1+ использований» (редкий, репро нет) — гипотеза + страховка.**
`remote.js` кэшировал «дом звука поднят» навсегда, а Chrome ЗАКРЫВАЕТ offscreen-документ после тишины →
первая команда уходила в пустоту и молча падала. Лечение: сброс кэша + **ретрай команды один раз**
(ensureOffscreen пересоздаст дом). Тир 🔶 — корень не доказан репро; после ретрая остаётся warn
`[ember] дом звука не поднялся` — если баг повторится, эта строка = улика.
3. **Опциональность язычка — дизайн автора, реализован:** крестик ✕ на ховере язычка (над ним; переживает
перелёт курсора задержкой скрытия) = выключить совсем; включить обратно — **тумблер «язычок» в настройках
очага** (первая переключалка; тёплый мини-свитч в ряду энергия·язычок·кокон). **ТРУБА-ИСТИНА =
`storage.glowEnabled`**: крестик и тумблер пишут туда, фон слушает `storage.onChanged` — ни одного прямого
сообщения между тремя органами. Права НЕ снимаются при выключении (обратно — без системного окна);
запрашиваются при включении (жест жив). Версии скриптов подняты `?b=10` (кэш-закон).

## 2026-07-22 (поздний вечер) — Сцена образа: А «как есть»; якорь «одна функция»; uninstall+оценка до графики
**Решение автора — сцена A:** визуальную поэтику НЕ переносим (язычок-мини-жар и PiP-жар остаются
обратимыми опциями на живых пользователей). Основание разбора: идея «время-из-звука» не затушена —
её носитель УХО (offscreen-звук живёт в любой вкладке при закрытой панели); затушена была только
визуальная поэтика, а это вопрос сцены, не концепта. Панель = пульт и полка углей; образ живёт в звуке
и в моментах завода/рассвета.
**Метод-проверка «одна функция» (заказ автора «рассыпается видение»):** редукция $-конкурентов даёт
два ряда — таймеры продают РАМКУ сессии, звук-игроки СРЕДУ; никто не продаёт их как одно. Наша функция
одна: «сессия фокуса, где рамка и среда — одно» (таймер-существительное, звук-тело). Тест вычитанием:
−звук = таймер без отличия · −таймер = парк (измерено 07-04). Специализация у́же соседей-комбайнов.
Схема-якорь: `schemas/2026-07-22-odna-funkciya-yakor.svg`. Деньги ряда платятся ЗА ЗВУК (Momentum Plus
$4.95/мес soundscapes · Flocus Plus $5–9 · BrainWave $6.99 · Endel/Brain.fm $60–120/год) → премиум-план
(глубина звука, tier в движке) стоит на измеренном; меряется после 2k (веха 2), не чувством.
**Порядок (реш. автора): дознание звука → uninstall page + оценка → графика→имя→листинг.**
Сделано в тот же вечер: uninstall page live (канон 3.2:51 «причины через Google-форму»; паттерн
ExportGPT: карточка+iframe-форма+reinstall; слот формы ждёт ссылку автора) · оценка по ExportGPT
(со 2-й сессии, в покое, ≥4★→стор / 1–3★→форма; молчит до заполнения ссылок — честно к методике:
просят у живого стора). Дознание звука — фон-агент (слуховой разбор Endel Scenarios / Session), ждём.

## 2026-07-22 (шаббат) — Поправка автора: «премиум» ≠ слой платных функций; это полка экспериментов
**Решение (терминология, важно для чтения старых записей):** по методу у нас НЕТ логики «free-функции vs
платные функции» — двух слоёв не существует. На старте бесплатно ВСЁ; при росте расширения платным становится
ДОСТУП целиком (веха 2 = 2000 юзеров — порог разговора). `tier basic/premium` в движке и «премиум-подвал» —
техническое место складирования идей/экспериментов (гармонизация · характеры · «прибой»), не монетизационная
архитектура. Записи 07-10 («два слоя basic/premium; премиум продаёт опыт») читать через эту поправку:
«опыт продаёт» будущий ПЛАТНЫЙ ДОСТУП ко всему продукту, а не слой поверх бесплатного.
**Почему записано:** Клод в звуковой ревизии сказал «кандидат в премиум-характер» — автор поймал смещение
термина до того, как оно вросло в планирование.

## 2026-07-23 — УГОЛЬКИ: слой накопления = ТЕПЛО ОЧАГА (не полка, не календарь, не существо)
**Вопрос автора:** «где складируются угольки, когда их много? нужен ли этот слой сущностно?» (+ память
о чём-то похожем у Otto). **Разбор данными:** (1) код — экран жил ДВУМЯ днями (сегодня-кружки + зола-вчера),
глубже история копилась в localStorage невидимо; при >9 углей/день ряд тихо слипался у краёв (а 12–15
коротких сессий для ADHD-ядра = хороший день). (2) Разведка 16 конкурентов: 4 ответа индустрии на «куда
девается много» — перелистывание (Forest: лес по периодам, мёртвое дерево = руина-укор) · агрегаты
(Pomofocus/Session/Focus To-Do: графики, CSV) · один счётчик (Pomodoro Kitty) · полка-коллекция без
хронологии (аквариумы, Focumon). **Метафору огня не делает никто** — «остывающий след» свободен.
Otto оказался НЕ складом: там копится СВЯЗЬ с существом (Essence Particles, Stranger→Friend→Guardian) —
нам путь закрыт онтологией «жар НЕ персонаж» (07-16), и это спокойно: мы не «недо-Otto», мы другая ветка.
**Решение автора — B «Тепло очага»** (из A эфемерность / B тепло / C полка дней): прожитое не показывается
дискретно — ЧУВСТВУЕТСЯ, продолжая физику огня: уголь (сегодня) → прогорает в золу (вчера) → оседает
теплом (вся жизнь очага). Три тихих проявления: осадок золы ниже и шире вчерашней полоски · тон комнаты
теплеет · камень-орб в покое чуть ярче. **Инвариант «без вины» конструктивно:** тепло = монотонная шкала
1−exp(−мин/3000) от СУММЫ прожитого (50 ч → 0.63) — только растёт, пропуск дней не отнимает ничего;
остывания нет (камни держат тепло). Ни цифр, ни клеток — цифры остались тихой честностью в ховер-тултипе
(«прожито очагом N ч»). C отвергнута: пустые полоски дней = тень вины, шаг к статистике. A отвергнута:
«жар зреет от прожитого» (CLAUDE.md) оставалась бы словами. Заодно чинен ряд: шаг сжимается, угли теснятся
грядой (192px всегда хватает). Схема: `schemas/2026-07-23-teplo-ochaga.svg`. Проверено стендом (сид 43 ч,
13 сессий/день): осадок ±63.5 · комната 26→34 · тултип честен · свежий очаг неотличим от прежнего.

## 2026-07-24 — Два бага (язычок-дверь · гонка звука) + ПИВОТ: полное переосмысление сайдбара
**Приёмка автора живьём:** язычок принят («в целом ок»), но два дефекта походя + большой дизайн-вопрос.

**B1 — язычок не виден до старта таймера (реш.: ПОСТОЯННАЯ дверь).** Корень: `syncGlow` зажигал язычок
только в рабочих фазах (`WORKING`). Как дверь-для-поиска это провал: на свежей вкладке двери нет, а чтобы
запустить — надо открыть панель иконкой. Автор: «нелогично, пусть появляется при первом открытии сайдбара
и дальше чётко». Сделал персистентным: `shouldShow = glowEnabled` (+ права) — виден всегда, в покое ТЛЕЕТ
(glow.js `--lit` 0.3+heat·0.6), в сессии разгорается. Обоснование не только UX — портрет Ре: у ADHD НЕТ
объектного постоянства (скрылось = не существует) → дверь должна стоять всегда. Guard `glowReady` от
мигания на холодном старте SW. `WORKING` удалён (был только в shouldBurn).

**B2 — «клик не запускает звук», рецидив (корень: ГОНКА).** Ретрай 07-22 лечил одну команду, но старт шлёт
ДВЕ разом (`turnOn`+`startSession`): каждая независимо обнуляла кэш `_ready` и пересоздавала offscreen —
гонка, порядок терялся, вторая попытка иногда глохла молча. Лечение в `remote.js`: (1) убрал кэш «дом
поднят» — всегда спрашиваю фон (он решает по `hasDocument`); (2) команды через ОЧЕРЕДЬ `_chain` (строгий
порядок, дом звука не пересоздаётся дважды). Ретрай оставлен как страховка. Термин для автора: race
condition / краевой случай. Стенд remote-путь НЕ гоняет (там AudioEngine прямой) → проверка на перезагрузке.

**ПИВОТ (реш. автора): ПОЛНОЕ переосмысление сайдбара — присутствие-СПУТНИК ведёт имя.** Развилка была
A(атмосфера-легко)/B(полный передел)/C(держим минимал-запуск) — автор выбрал B, сознательно приняв риск
сдвига запуска. Назвал закон вслух (минимализм-для-ADHD, «кнопки убивают активацию», мы у ворот) — автор
всё равно за передел. КЛЮЧ развилки, снят автором: углубляем присутствие как **АТМОСФЕРУ** (комната у огня,
глубина, живой свет — НОЛЬ новых кнопок), НЕ как функции/маскот (это рвёт онтологию «жар не персонаж» +
закон «мало ручек»). Сердце — body doubling (18.1k, самая большая дверь ядра): очаг как тихий спутник,
пока делаешь страшную задачу (портрет Ми). Канат: спутник = огонь и комната, felt-присутствие, НЕ существо
(ни глаз, ни лица, ни опеки). **Дизайн ведёт имя** (не наоборот): семантика (очаг·спутник·тепло·приют)
устаканится дизайном, потом имя; графика стора тоже ждёт финального вида — порядок дизайн→имя→листинг верен.
**Путь из текущего состояния (5 шагов, инкрементально, можно встать на любом):** 1 живой свет очага на всю
комнату · 2 циферблат отступает, огонь вперёд (тикающее время = тревога ADHD) · 3 угли садятся на пол очага ·
4 вся комната дышит с огнём (спутник, felt) · 5 ручки к тихой кромке. **Шаг 1 СДЕЛАН** (`#room` в hearth,
opacity кормит жар: покой 0.08 → сессия растёт; медленное дыхание; reduced-motion). Скрипты b=14.
Концепт-борд показан автору (show_widget). Шаги 2–5 — очередь. Нейминг отложен до формы комнаты.

## 2026-07-24 (продолжение) — Эссенция центрального элемента: ФОРМА решена проработкой (Workflow)
**Многоракурсная проработка** (7 замыслов × 2 судьи + синтез, run wf_8ea16804-dfa): вопрос «орб не читается
как очаг, хочется живее». **Вердикт единодушный: форма = ЖАР СКВОЗЬ КОРКУ ПЕПЛА** (неровный уголь, серо-тёплая
корка, сквозь трещины дышит жар). Почему победил над «дешёвыми» правками: гладкий СИММЕТРИЧНЫЙ круг обречён
читаться бусиной/пузырём/планетой (и случайно ловит зрачок→лицо, парейдолия структурна); светящиеся трещины
сквозь корку — единственный в природе образ «то, что осталось от огня», ярлык не нужен. Оба судьи: дуальность
орб≠очаг снята «по существу, а не подписью». Онтология даже СИЛЬНЕЕ орба: несимметрия активно сопротивляется
чтению лица.
**Разрублен узел роста «5 мин мало / не закипать»:** рост в РАЗМЕР — неверная ось (размер=закипание). Рост =
ВНУТРЬ (цвет тёмно-красный→янтарь→золото · загораются прожилки · раскрываются окна пепла), силуэт ~фиксирован.
Живость РАЗВЯЗАНА с ростом: always-on дыхание яркости (±10%, НЕ размер) + фазово-смещённое мерцание трещин +
дрейф жара (конвекция) → живо с 1-й секунды даже в 5 мин. Кривая на доле сессии p: m=1−e^(−p/τ), τ≈0.35,
фронт-загружена (~45% к p=0.2). Опадание при p>0.85 = ЗАГНЕТАНИЕ (уголь берегут), не тушение/не тревога;
финал — тёплый дышащий уголь → слепок в грядку УГЛИ. Накопление ОЩУЩЕНИЕМ (совпало с досками-квадратами автора).
**Отброшено по закону:** одинокое пламя (#3 «Язык очага» — S-дрейф = танцующая фигурка/Кальцифер, риск
«существа», единственный, бьющий в священное НЕЛЬЗЯ) · «оживить круг как есть» (#5 — доминируется, парейдолия
на симметрии) · «Со-дыхание» (#6 — уклоняется от вопроса ФОРМЫ; ритм забран в графты).
**Нейминг всплыл (держим до формы, сильные кандидаты):** **ЗАГНЁТКА/ЗАГНЁТОК** (закут печи, где угли берегут
тёплыми на ночь = «очаг, что держит компанию» → body doubling) · живой элемент звать **ЖАР** (НЕ «уголёк» —
занято слепками УГЛИ), цикл ЖАР(светится)→УГОЛЬ(остыл, в грядку) чист. EN: Ember/Bank/Emberkeep.
**РАЗВИЛКА ВКУСА автору (это смысл, не механика):** насколько тёплым/близким присутствие. Полюс А — тёплая
светящаяся МАССА (дышит, обволакивает, светит в комнату; сильнее «ты не один»; ближе к краю «существо»).
Полюс Б — тёмное тело + живая грань (движение=физика жара; железно «явление, не существо»; холоднее).
Один выбор задаёт форму + регистр живости + температуру финала; сидит на нерве (реврайт хочет тепла ↔ закон
хочет дистанции). **Реш. автора 07-24: собрать ОБА живьём — выбрать телом, не по картинке.** Стенды строятся.

## 2026-07-25 — КОГЕРЕНТНОСТЬ ЗВУК↔ОБРАЗ: звук а-концептуален, уголёк ДЕРЖИМ (Workflow)
Вопрос автора: с чем ассоциируется наш brown-звук, связан ли с огнём-визуалом или а-концептуален, и что наука
шумов реально говорит. Workflow (5 линз веб-грунт + 2 скептика + синтез, run wf_39868347).
- **Ассоциации:** звук тяготеет к ВОДЕ/ВОЗДУХУ, НЕ к огню — гул салона самолёта (топ для фокуса) · далёкий
  водопад/река · океан/прибой (наш медленный дрейф = накат волны) · ливень-масса · дальний гром · ветер в кроне;
  реверб → раковина-у-уха/утроба/подводность/пещера. Огонь в спонтанных описаниях brown НЕ всплывает вообще (⬜).
  Абстрактно ✅: тёплое·низкое·большое·мягкое·округлое·обволакивающее (сторона bouba).
- **Ответ автору (двойной, непротиворечивый):** звук работает А-КОНЦЕПТУАЛЬНО/функционально (люди берут ради
  СОСТОЯНИЯ, ассоциации пост-фактум, эффект = что звук ДЕЛАЕТ, не что «означает»). С огнём связан НЕ акустически
  (огонь=потрескивание=запрещённые транзиенты; тлеющий уголь без пламени почти БЕЗЗВУЧЕН — крэк даёт влага, не жар;
  лучистое тепло = ИК, без акустики), а только МЕТАФОРИЧЕСКИ через ТЕПЛО. Огонь = визуальная метафора, не претензия уха.
- **Когерентность — гипотеза автора ПОДТВЕРЖДЕНА: тихий уголёк ≫ потрескивающий костёр.** Диссонанс «слышу воду —
  вижу огонь» становится АКТИВНЫМ только у трескучего костра (визуал обещает звук, которого нет → внимание ищет
  «что не так» = прямой антипаттерн фокуса, инконгруэнтность снижает presence ✅). Уголь = ТИХАЯ фаза огня
  («тлеющий», «догорающие угли») → праймит тишину, ложного акустического ожидания НЕТ. Мост = ТЕПЛО (термико-
  аффективное 🔶) + гладкость + обволакивание, НЕ цвет и НЕ имитация (цветовой мост «низ→оранж» эмпирически near-null).
  Два независимо тёплых канала на общей семантике «свечение без пламени», а не изображающие друг друга.
- **РЕШЕНИЕ: УГОЛЁК ДЕРЖИМ, не смещаем.** Закон «без crackling» — ровно то, что удерживает метафору честно
  визуальной. **Запретные слова (включают диссонанс):** треск·искра·пламя·костёр·crackle·spark·kindle·campfire —
  вон из графики И из слов. **Безопасное поле:** тепло·жар·свечение·очаг·уголь-под-пеплом·загнётка·banked·низкое-
  глубокое. **НЕ называть по ВОДЕ** (rain/ocean/river) при огне-визуале (раскол имя↔картинка + клише категории).
  «Рассвет/светление спектра» удачно двусмыслен (уголь разгорается ИЛИ свет зари) — держим.
- **Развилка нейминга (автору, ПОТОМ):** насколько громко уголёк звучит в ИМЕНИ (имя-очаг vs нейтрально-тёплое) —
  чей голос ведёт бренд. Механика Клода (не вкус): РАЗВЕСТИ СЛОИ — визуал/эмоция = уголёк-свечение · звук-дескриптор
  для ASO = «тёплый низкий гул» · общий мост = слово ТЕПЛО; тогда ни один канал не врёт.
- Наука перекалибрована в AUDIENCE.md (07-25): эффект мал+на детях · SR убрать · brown=0 исследований · «тёплый
  шум без клеймов» = самая точная позиция · закон «без потрескиваний» согласуется с доказанной базой.

## 2026-07-25 — РАЗВОРОТ МЕТАФОРЫ на столе: рассвет-небо vs уголёк (сравнение, в прод — один)
Из вопроса о звуке вырос вопрос метафоры. Данные DFS (calm/sky/cloud/flight): **метафора — выбор ВКУСА/ОТСТРОЙКИ,
не спроса.** «calm timer» 590/KD2 informational + гнездо «N-min timer with calm music» = контентный YouTube-мир
(софт-гейт ❌, не стор-дверь). `sky/cloud/flight timer` = ЛОЖНЫЕ ДРУЗЬЯ (астрономия/авиация/облачные вычисления)
— лексика неба к фокус-спросу НЕ ведёт → именем по небу НЕ идём. Реальные двери те же: `aesthetic timer` 2900/KD25 ·
`cute timer` 2400 · `aesthetic pomodoro` 880 · `focus timer` 4400. И огонь, и небо заходят в ОДНИ двери → SEO не решает.
- **Честная развилка:** небо/воздух — когерентнее звуку (brown=воздух/вода) + тяга автора, НО голубо-пастельно-облачный
  = самый ходовой эстетик-лук → отстройка слабее + тепло теряется. Уголёк — отстройка сильна, звук закрыт «тихим теплом»,
  НО автор остыл.
- **СИНТЕЗ (концепт Клода, рекомендован): «РАССВЕТ — небо тепла».** Небо, под которым работаешь (aloft, над облаками —
  иллюминатор абстрактно, без салона); центр = мягкий тёплый СВЕТ в небе (не огонь), по-прежнему ОДНО присутствие
  (дышит, не персонаж). Арка = ход неба: пред-рассвет (индиго теплеет) → плато (дрейф облаков) → **РАССВЕТ (наш финал —
  теперь буквальный!)**. Палитра — рассветный градиент индиго→янтарь/золото (НЕ плоский голубой) → сохраняет ТЕПЛО +
  отстройку. Звук НЕ трогаем (уже когерентен). Механика (вход/плато/рассвет, жесты, накопление) — вся выживает, меняется кожа.
  Флаг: держать отдельно от `nebosvod`/`sky-timelapse` (другой домен — данные-космос).
- **РЕШЕНИЕ АВТОРА 07-25: строить ОБА (рассвет-небо + доведённый уголёк), СРАВНИТЬ живьём, в прод выпустить ОДИН.**
  Уголёк-финал = вкус автора «между А и Б, но А слепит» → тёплый, но ПРИГЛУШЁННЫЙ (тепло А в теле Б). Стенд сравнения:
  `lab/compare-stand.html`. Нейминг/поле — после выбора кожи (огонь-поле vs рассвет-поле: dawn/first-light/aloft/hush/drift).

## 2026-07-25 (продолжение) — Рассвет ОТКЛОНЁН (время суток); янтарь/уголёк ведёт; воздух пере-разбираем НЕ через сутки
Автор пощупал `compare-stand`. **Рассвет-небо ОТКЛОНЁН:** не нравится привязка к ВРЕМЕНИ СУТОК (ночь→рассвет) —
«не хочу днём видеть палитру рассвета». Подтверждает старый флаг AUDIENCE: Endel владеет «время суток+ADHD» — не наш клин.
**Уголёк нравится как есть (уютно, тепло); ЯНТАРНАЯ (amber) тема сильно откликается автору — пока янтарь выигрывает.**
НО автор просит ещё раз разобрать ВОЗДУХ/НЕБО не через рассвет, а иначе — ВНЕ-ВРЕМЕННЫ́Е трактовки. Требование к любой
воздух-трактовке: БЕЗ времени суток · тёплая/янтарная (не холодный голубой) · живое присутствие · когерентно звуку (воздух) ·
отстройка · победить «уютный янтарный уголёк» (бэйзлайн). Идёт Workflow. Гипотеза Клода на проверке: гибрид «уголёк в тёплой
янтарной атмосфере/дымке» (уголь-присутствие + воздух-обволакивание, вне времени).

## 2026-07-25 (III) — Воздух-НЕ-через-рассвет: вердикт = ГИБРИД «уголёк в янтарной атмосфере» (Workflow)
6 вне-временны́х воздух-трактовок (Workflow wf_5a490f0b, 13 агентов), ВСЕ прошли гейт «без времени суток».
Вывод №1: **воздух без суток ДОСТИЖИМ** структурно (радиальный изотропный свет · один янтарный тон · фазы меняют
ЯРКОСТЬ/ПЛОТНОСТЬ, НЕ оттенок · дрейф на месте, ничего не всходит/заходит · смысл-замок: янтарь = застывшая смола
= остановленное время = анти-часы, идеально ADHD-таймеру). **ЧЕСТНЫЙ вердикт: чистый воздух НЕ бьёт уютный янтарный
уголёк** — 3 ничьи, 2 проигрыша, 1 победа, и та («уголёк во взвеси») = уголь СОХРАНЁН. Все 6 сошлись на merge.
**Реальный подарок воздуха = ENVELOPE** (тёплая дышащая янтарная толща вокруг ядра): чинит единственную слабость
уголька под ФИКС-звук — голый уголь = точка в пустоте, а brown ОБВОЛАКИВАЕТ → envelope сажает юзера ВНУТРЬ звука.
**РЕШЕНИЕ-НАПРАВЛЕНИЕ: ГИБРИД «уголёк в янтарной атмосфере».** Слои (тёмная панель, от центра): (1) ЯДРО =
существующий уголёк (НЕ трогаем) · (2) ENVELOPE = большой радиальный янтарный градиент от ядра (мёд→дымка→коньячная
кромка; изотропно, без горизонта) · (3) опц. МАРЕВО (слабый feTurbulence / 2–3 дрейф-блоба — тело света) · (4) опц.
ВЗВЕСЬ (редкие медленные тусклые пылинки, конвекция вверх-в-стороны). Янтарь = ТЕМПЕРАТУРА всей среды (жар в
трещинах → медовая дымка → коньячная кромка, ноль холодного). Воздух причинно: уголь тёплый → конвекция → дымка
дышит со сдвигом фазы. Вне времени СТРУКТУРНО. Звук+механика не трогаем — только кожа. **Правило дозы: воздух
ВСЕГДА подчинён углю; пылинки НЕ выстрел искр (искры вверх = костёр = огонь, запрещён — звук воздух/вода).**
**РАЗВИЛКА АВТОРУ = ДОЗА воздуха** (только felt-тест): A минимум (дышащий envelope, пылинки в ноль — ближе к
любимому, риск «фон чуть теплее») ↔ B видимая взвесь/ток (макс обволакивание/подвешенность — главный выигрыш под
звук, риск боке/потери острого угля). Стенд `lab/dose-stand.html`: 3 дозы (соло · +envelope · +envelope+взвесь).
Опц. 2-я развилка (ценностная, под доски-квадраты автора): финал = смягчение угля ИЛИ ХРАНЕНИЕ (пылинка «ловится»
вкраплением-следом = накопление без пепла/вины).

## 2026-07-25 (IV) — Уголёк-в-панели ОТВЕРГНУТ («картошка с ободком»); новая кожа-кандидат: ПОЛЁТ К ЗВЕЗДЕ
Автор увидел вариант Б в реальной панели (b=16) и ОТВЕРГ: неровный уголь + кольцо-таймер вокруг = «несуразная
картошка с ободком-таймером. Нет.» Новая идея автора: МИНИМАЛЬНЫЙ ПОЛЁТ — «летим к ЗВЕЗДЕ»: одна тёплая звезда
по курсу ПРИБЛИЖАЕТСЯ по ходу сессии (рост = ПРИБЛИЖЕНИЕ, не раздувание → «закипать» невозможно by design);
летим сквозь тёплое воздух-космос-пространство (искры сносит от центра наружу = полёт вперёд). Вне времени суток
(космос, не сутки — снимает проблему рассвета). Тёплая ЯНТАРНАЯ звезда (не холодный сай-фай). **Кольца-таймера НЕТ**
— время = БЛИЗОСТЬ звезды. Сшивает нитки: воздух/полёт = дом звука (гул салона/высоты, топ brown-ассоциация) ·
вне-временность · рост-приближением · тепло сохранено. Звук = ВОЗДУХ, сквозь который летишь (НЕ «звук звезды»;
вакуум тих — поэзия, не физика). Держать отдельно от `nebosvod` (там МНОГО звёзд-данных; тут ОДНА личная).
Флаги: движение ОЧЕНЬ медленное (старфилд укачивает/отвлекает — фокусу нельзя); завод (длина сессии) без кольца —
добить отдельно. Стенд `lab/star-stand.html` (порт 8783): ползунок старт(звезда далеко)→прибытие + «прожить сессию».
Проверено на стенде (старт=далёкая тусклая точка · прибытие=близкое тёплое солнце · искры-полёт · тёплое поле).
**⏳ ждёт felt-реакции автора: звезда-полёт vs уголёк-envelope.** Звук+механика по-прежнему не тронуты (только кожа).
Побочно (аside автора «генератор тишины»): копии звука кулера СКЛАДЫВАЮТСЯ (синфазны), не вычитаются; вычитание =
противофаза = ANC, работает лишь в точке у уха (наушники) + живой микрофон. Наш продукт наоборот — маскировка (поднять пол).

## 2026-07-25 (V) — Звезда-полёт ПРИНЯТА; кольцо долой, время=ползунок; + ГЛУБОКИЙ вопрос ЦЕННОСТИ (feel-before-ship)
Автор: «прекрасно, мне прям нравится» → кожа = **ПОЛЁТ К ЗВЕЗДЕ**. **Кольцо-таймер убрать совсем.** Время = ПОЛЗУНОК
под сценой (градации 5мин→∞, шкалу согласовать), «в одно касание ухватил-дотащил». **Двойное чтение** (приближение
к звезде ↔ шар, из которого исходят светлячки) автору дорого — ХРАНИМ. Нажатие на звезду = старт шума. Перенести в
реальный сайдбар.
**ГЛАВНОЕ — вопрос ЦЕННОСТИ (автор вслух):** «а что вообще даёт этот таймер? я сам таймерами не пользуюсь и не пойму
зачем он нужен. не могу выпустить, пока не почувствую кожей ценность — иначе выстрел в молоко». Признаёт возможные
критические допущения. **Реформа-ключ (идея автора):** время = «сесть в поезд/самолёт и ехать заданное время»
(под ассоциацию салона). → **ПЕРЕФРЕЙМ:** это не countdown-таймер (терпеть), а РЕЙС, на который садишься; ценность =
обязательство + тебя НЕСЁТ + прибытие, а не счёт времени. Это и есть «тело звуку / протез времени» ощутимо: не
смотришь время — ЕДЕШЬ в нём. Честно: автор может НЕ быть целевым (боль = ADHD-слепота-ко-времени; он гиперфокусер,
таймерами не пользовался) → его не-чувствование ≠ бесполезность, НО правило feel-before-ship в силе. «Таймер» = как
НАХОДЯТ (SEO-двери), «рейс» = как ЧУВСТВУЮТ (одно не отменяет другое). Путь почувствовать: собрать в панель + автор
летит РЕАЛЬНУЮ сессию + 1 живой ADHD-пользователь (validate niche not idea). ⏳ frame на согласовании перед сборкой.

## 2026-07-25 (VI) — Рейс: релексикон + ЭНЕРГИЯ=СКОРОСТЬ + движение только в полёте (реш. автора)
Автор пощупал рейс-панель (b=17). Правки (b=18, проверены на стенде):
- **«настройки очага» → «настройки»** (без рудимента). **Оценка — УБРАН текстовый ярлык** («очаг греет?» снят,
  остались только звёзды; механизм оценки/фидбека цел). **«кокон» ОСТАВЛЕН** (короткое слово сути=герметичность;
  «кабину» автор отверг) — БЕЗ визуализации (реш. автора: «герметичность» визуально менять нечем → только звук).
- **ЭНЕРГИЯ = СКОРОСТЬ ПОЛЁТА** (да/нет автора → ДА): энергия правит скоростью сноса искр (`--speed`=0.7…1.6 от
  значения; CSS `animation-duration: calc(--fd / --speed)`), «слабо, но заметно». Больше энергии → быстрее летишь →
  сильнее стимуляция (одна ось со звуком). Кокон визуал не трогает.
- **Движение ТОЛЬКО когда летим** (наблюдение автора «на паузе/в покое всё равно летим» → чинено): `--flying`
  (render по фазе) — покой=0 (искр нет, не взлетел) · пауза(ниточка)=0.35 (гаснут) · полёт=1 · посадка=0.55.
  Искры = opacity var(--flying). Концептуально: летишь только сев в рейс (тап).
- **ЗВУК В РАСШИРЕНИИ МОЛЧИТ (автор):** стенд (AudioEngine) чист — рефактор звук НЕ сломал, старт+движок+0 ошибок.
  Молчит offscreen-путь (RemoteEngine→фон→offscreen; стендом не воспроизводится, расширение в этот браузер не грузится).
  Гипотеза: застрявший SW/offscreen после hot-reload → чистая переустановка (Remove→Load unpacked) + переоткрыть панель.
  Щуп: панель→Inspect→Console→тап; `[ember] команда звука не прошла` = offscreen не встаёт (тогда доусилить прицельно).
  ⏳ ждёт результата чистой перезагрузки у автора. Язычок (когерентность рейсу, glow.js) — с тем же extension-проходом.

## 2026-07-26 — Кожа: небо на CANVAS во весь сайдбар (не CSS-искры в квадрате)
**Решение:** сцена «рейс к звезде» = звёздное поле на `<canvas id=sky>` во ВЕСЬ сайдбар. Покой — звёзды
левитируют на месте + мерцают (небо стоит с 1-го кадра); полёт — тихо плывём СКВОЗЬ них от звезды-цели
наружу; пауза — поток ЗАМИРАЕТ; энергия = скорость потока. Прежний тёмный `.field`-квадрат + CSS-`.sparks` — убраны.
**Почему (не косметика):** (1) CSS-искры при смене `--speed` **прыгали** — браузер пересчитывает фазу
`animation-duration` уже играющей анимации; canvas делает скорость множителем кадра → плавно by design.
(2) `<canvas>` — заменяемый элемент: `inset:0` его НЕ растягивает, а в side-panel высота приходит позже 1-го
кадра → небо садилось в `y≈0` = **горизонтальная полоса** у кромки (баг, который автор видел как «одна звезда
+ полоса»). Лечится `ResizeObserver` (сеем звёзды только с реальным размером) — не разовым замером. (3) Покой =
левитация, НЕ дрейф-наружу: иначе «фонтан из центра», а автор хотел «небо просто висит, запуск = плывём сквозь».
Тюнинг (медленнее ×3, 60 звёзд, мягче мерцание) — против «ряби». Язычок → тонкая нить света с тёмной кромкой
(видна и на белой странице). ⚠ присутствие язычка на всех вкладках = право `<all_urls>`, слетает при Remove→reinstall.

## 2026-07-26 — SEO-дверь = `minimalist timer`; звук — ОТСТРОЙКА, не дверь
**Решение:** главный ключ имени — **`minimalist timer`** (480/KD1/+125%год/CWS-free). Отклонены выдачей: cute
(kawaii-студенты), visual timer adhd (физ-диск, несофт), flow timer (шум pokemon/water + физбренд), calm timer
(YouTube-класс), cozy timer (google-выдача раздвоена sleep⚔focus), aesthetic/focus/study timer (заняты опт),
ВСЕ pomodoro-ключи, ВСЕ звук-ключи (brown noise·focus sounds·brown noise machine).
**Почему (уроки метода, дорого добытые за сессию):**
- **fit решает, а не только «свободна+крупна».** Дверь, не совпадающая с ожиданием пришедшего, даёт низкую
  конверсию + плохие поведенческие → Google пессимизирует. cute timer free и крупна, но ждут kawaii → не наш.
- **Звук — отстройка, НЕ дверь.** «Время сделано из звука» словами НЕ ищут (новая потребность). Звук-ключи =
  плеер/аудио/физустройства (brown noise = Spotify/YouTube/Yogasleep), выдача НЕСОФТОВАЯ → расширение не пустят
  (`1.1:138`). Вывод: **free census ≠ берётся** — если выдача несофтовая, дверь «мёртвая для расширения».
- **Pomodoro отпал по СУТИ, не юридике** (право есть, канон `1.4:39` без него). Продукт АНТИ-pomodoro
  (`MECHANICS:54` перерыв-фаз нет · `AUDIENCE:140` ядро «бросило помодоро — звонок разбил поток»). «pomodoro» в
  имени обяжет быть pomodoro → поведенческие + стреляем в собственную отстройку. Имя = ОДИН ключ (канон `2.1.2`:
  микс — только нац.языки; бренд-слов не добавляем) — «Cozy Aesthetic Pomodoro Timer» было грубой ошибкой.
- **census-дискавери СЛЕП** (важно для инструмента): стор-топ ~10 пропускает глубоких/новых опт-держателей
  (по `focus timer` census дал weak, а AppDB автора нашёл опт `Focus Timer` 52яз/4350симв). Занятость по канону =
  оптимизирован ли конкурент (имя-из-тех-же-слов · desc>3К · >30 языков), НЕ census-ярлык «занята слабо»/юзеры.
  → занятость финалиста сверять по AppDB. minimalist timer выжил: суть (минимализм/чистота/no-tracking/ad-free)
  совпадает с продуктом (одна звезда, пустое поле, приватность on-device) — редкий случай fit ключа И сути.

## 2026-07-26 (листинг) — Имя=чистый ключ · Тургенев 1-флаг · баннер честен реальными кирпичами · палитра синяя
Сессия закрыла листинг-блоки (имя, мета, графика, палитра). Неочевидные «почему»:
- **Имя = сам ключ дословно (`Minimalist Timer`), НЕ креативно «собранное».** Клод предложил «собрать конкретное имя из ключа» — автор поймал ошибку. Канон `2.1.2:23`: «берём тот ключ, что нашли, в `name`» → работа над именем = ВЫБОР ключа (уже сделан на 1-й неделе), отдельного сочинения НЕТ. Бренд-слово (звезда/тепло) в имя не добавляем (микс — только нацязыки `2.1.2:28`, размывает SEO-вес); душа-звезда живёт в ГРАФИКЕ/тоне, не в `name`. Урок: не протаскивать креатив-нейминг туда, где метод требует чистый ключ.
- **Тургенев: 1 красный флаг ДОПУСТИМ** (личное общение автора с методом, весит выше расчётов Клода): держать на «1»; дошло до «2» → убрать ОДИН ключ (одно вхождение) → снова «1»; в ноль частоту НЕ гнать — продукт-«…timer» структурно держит ~3-4%, окрашенность (ключ названия #1 в словосочетаниях Тургенева) важнее борьбы за <3%. Клод перестраховался (`timer` 4.1%→3.4%), зря выкинул ценные ключи (`work timer` 1900, `concentration timer`) — вернули. Правило зафиксировано в `app-studio/METHOD.md §2.1 п.5`.
- **Баннер: честность = реальные кирпичи, не выдумка.** Клод нарисовал waveform (v1) — красиво, но waveform в продукте НЕТ → это обман: после установки человек видит звёздное небо, не волны → отток → пессимизация (метод 3.1: баннер = скрин продукта; разрыв баннер↔продукт бьёт поведенческие, автор почувствовал «обманывает»). Разрешение: суть = ЗВУК невидима, честно нарисовать нельзя → её несёт ТЕКСТ (`brown noise`), а визуал = сцена из НАСТОЯЩИХ элементов (небо + звезда + warp-полёт). Метод `3.1` разрешает «отдельные элементы / упростить» → генерировать можно (не буквальный скрин с ползунками/пустотой), но из реальных кирпичей. Граница автора: «баннер не обязан быть честным до мелочей кадром, но не должен обманывать после установки».
- **Палитра продукта → глубокий синий почти чёрный** (реш. автора): автору синий+звезда нравятся больше чёрного фона. Ключ-инсайт: не баннер подгонять под красоту (был бы обман), а ПРОДУКТ дорастить до красивой палитры — тогда баннер из реального кадра честно её берёт. Аура звезды `#room`: проба .18 (теплее) → автор вернул .06 (холоднее, «прошлый нравился больше»); синий углублён до `#0e1638→#090d22→#05060f`. Панель+2 баннера+иконка = единая гамма.
- **Иконка = тёмно-синяя плашка + искра** (форма 2, отступ от метода «фон прозрачный»): автор выбрал фирменную синюю плашку — консистентно с продуктом, искра контрастнее. Метод советует прозрачный фон, но плашка — частая практика; выбор автора. Ромб-искра на иконке vs круглая звезда в сцене/большом баннере — не конфликт (иконка=знак, сцена=присутствие).

## 2026-07-26 (права + переводы + канон) — host-наслоение развенчано · дверь локалей = pomodoro · finish быстрее
Сессия: права-обоснование 3.6, полная досыпка канона (CWS IV+V, App I–V — база знаний ЗАКРЫТА), выверка переводов, 53 локали + 8 FULL. Неочевидные «почему»:
- **Дверь ЛОКАЛЕЙ = POMODORO, не focus/minimalist (выверка перевернула интуицию).** На EN дверь = `minimalist timer` (KD1, суть-fit). Но на локалях DFS/Wordstat показал: «фокус/концентрация/foco/enfoque/集中» пусто ВЕЗДЕ (Fokus DE 90 · concentración ES 0 · de foco PT 0 · 集中 JA 320 · фокус RU 115), а **pomodoro доминирует** (DE 14.8k · IT 18.1k · JA 33.1k · PT 27.1k · RU помодоро 1815). Английское чутьё Клода («focus timer 4.4k живой в US») НЕ переносится — интуиция 🔶 системно ошибалась, выверка окупилась ДО размножения на 50. Ловушки: IT `pomodoro` соло = помидор (омоним) → только составное; FR pomodoro ищут через англ. «timer» не «minuteur». Pomodoro в имени локалей — реш. автора (пробуем; поведенческие по странам покажут; ANTI-pomodoro-суть 07-24 держится в EN, где имя pomodoro НЕ несёт).
- **host-наслоение «= ред-флаг модерации» развенчано КАНОНОМ 5.5 (в квадрате).** Клод 2-й раз воскресил снятое 07-17 «broad host = риск reject» (как основание развилки «убрать язычок»). Автор поймал. Канон `module-5/5.5` рекомендует МАКСИМУМ host (`<all_urls>`) — ПРЯМО ОБРАТНО докам Chrome; артефакт-на-всех-страницах для оправдания = буквально наш язычок (примеры YouTube Dark Mode / Merlin = `glow.js`). Корень повторной ошибки: метод-сканер (`app-studio §2.5`) требовал сверки для «ниши/двери/конкурента/оценки», но НЕ для «прав/модерации/публикации» → Клод легально проскочил греп (мета-фикс §2.5 — в app-studio 07-26). Развилка язычка ПЕРЕОСМЫСЛЕНА: не «проходимость» (host = норма), а required-рано (метод: не терять базу на апдейте + почва под аффилиат-монетизацию) vs наш optional (чистая установка, host спит) — открыта, решать с автором при росте.
- **8 сложных скриптов (bn ta te gu kn ml mr am): имя-транслит + short EN-fallback** (инструмент=явь). Переводить SHORT на языке, которым не владею = мираж (носитель увидит кривизну → хуже поведенческие, обратное цели). Решение: appName = локальный транслит pomodoro (SEO-дверь, засчитывает язык в счётчике CWS → 53) + shortDesc = честный EN. Имя-транслит помечено на вычитку носителем.
- **Клод заменил Localizer** (софт буткемпа 3.3): генерация `_locales` напрямую = метод `3.3:64` «идеальный вариант — ChatGPT-перевод», но качество выше (знание продукта + SEO-адаптация имён под локальную дверь). Пайплайн воспроизводим: `lab/locales-data.json` → `lab/build-locales.py` → `_locales/`.
- **finish быстрее в тишину** (уточнение принципа двух концов 07-17, из feel-before-ship): ручной `finish` = почти мгновенный fade (не 0 — щелчок), НЕ 3-5с. Почему: когда юзер сам нажал «завершить», он уже мысленно вышел — задержка = трение → ухудшает поведенческие (в отличие от авто-рассвета, где плавность = подарок, т.к. решение не его). Правка отложена ради переводов, НЕ сделана.

## 2026-07-27 (публикация) — засабмичен в CWS · FULL все языки · дубль-title убран · single purpose = суть-словосочетание
Сессия закрыла ВЕСЬ путь к публикации и **отправила продукт на модерацию** (v1.0.0, 53 языка). Неочевидные «почему»:
- **FULL догенерён НАТИВНО на весь хвост, не EN-fallback.** Клод сперва предложил English-fallback для 34 недостающих языков (перестраховка «инструмент=явь»). Автор оспорил — и метод на его стороне: канон `3.3:9,30` = «переводить ВСЕ языки; проблемные — Google Translate», НЕ английский (перевод даёт ×несколько шансов в выдаче). Агенты = ChatGPT-класс → догенерили 25 языков. EN-fallback остаётся ТОЛЬКО для 8 транслит-скриптов (принцип 07-26: те, кем реально не владеем). Урок: перестраховка ≠ метод; проверять грепом даже свою осторожность.
- **Строка-заголовок `⭐ Имя — tagline` убрана из всех FULL** (дубль Title). Клод оправдал её каноном «ключ 15-20 раз» — но это НАТЯЖКА: `2.1.5` про частоту КЛЮЧА (`pomodoro`/`番茄鐘`), а не про повтор ПОЛНОГО title-с-брендом в первой строке. Автор поймал сверкой с живым ExportGPT: там описание открывается ХУКОМ («Need a simple chatgpt downloader?»), названия-дубля нет. Ключ в теле и так 22-26× (больше ExportGPT) → убрать первую строку безопасно. Урок: проверенная практика автора > моя интерпретация канона; не выдавать натяжку за уверенность.
- **Single purpose + justifications = МАКСИМАЛЬНО КРАТКО** (суть-словосочетание). Клод навалил абзацы; канон `3.6` = «не вдаваясь в подробности», эталон автора ExportGPT: single purpose `chatgpt to pdf converter` (24 симв!), justification `store user settings`. Наш → `minimalist focus timer` + короткие фразы. Почему кратко: меньше сказал модерации = меньше поводов зацепиться, быстрее ревью.
- **optional_host избегает in-depth review.** Форма Privacy у ExportGPT (обязательный host) показывает «Host permission → may require in-depth review → delay». Наш `optional_host` → форма host justification НЕ просит, задержки нет. Реальный плюс optional-выбора (развилка required-vs-optional 07-26 — на старте optional оправдан и этим тоже).
- **Служебные страницы были в старом «Ember/очаг» стиле** — рассинхрон с продуктом (имя+палитра) пойман при подготовке; welcome/uninstall/privacy переписаны в Minimalist Timer + синий/звезда. Welcome показывается при установке — расхождение било бы доверие.
- **Privacy policy на github-pages, не Google-Doc** (канон предлагает Doc). Оба дают публичный URL (требование CWS); github-pages чище (свой домен, рядом с welcome). Содержание — честный zero-collection + оговорка: веб-страницы могут считать посещения (privacy-friendly). Таймер private = отстройка; сбор В расширении = v2 по DNA, потребует обновления policy + CWS Data-disclosure (правило: декларация = реальность, иначе нарушение). **Открытый хвост:** проверить, не нарушает ли это `save-chatgpt-as-pdf` (счётчики без декларации) — чип-задача.
- **Аккаунт: регионного риска у автора нет** (Грузия ≠ РФ-гео-блок CWS). Антидетект+прокси (метод `1.3`) = страховка от связывания сетки по fingerprint (бан-домино), не от региона. Для 2 честных продуктов один аккаунт тоже приемлем (🔶 моё суждение, не буква канона). РФ-номер для 2FA — избегать (гео-след + SMS-риск).
- **Категория = Workflow & Planning** (не «Productivity» — это общая группа, не выбирается). Таймеры/тайм-менеджмент/pomodoro живут там; discovery сильнее, чем Well-being (спокойный угол несут описание+графика, не категория). Канон категорию не диктует — 🔶 по логике находимости.

## 2026-07-29 (сессия продвижения — реклама · аналитика · Reddit · тест-юзеры)

- **Reddit → веб-рейс, НЕ листинг CWS.** Инстинкт — пиарить листинг (у нас же экст). Метод (видео Капитана, 5.4): на Reddit **веб > расширение** — человек нас не искал, install = новый контекст → режет конверсию («нет ни одного случая, чтобы пре-ленд повышал конверсию»). Ведём на `focus-pages/flight/` (уже собран → почти бесплатно). Архитектура 2.6: вебсервис = ядро (Reddit), CWS-экст = лаунчер, открывающий его (стор-траст + Яндекс). Два контура Герольда: живой лог (Bluesky, резонанс) ≠ продуктовый (Reddit, дистрибуция) — не мешать.
- **Игры (zhivite-kak-hotite) на Reddit можно СЕЙЧАС, без 20-комментного прогрева.** Showcase «смотрите, что строю» БЕЗ промо-ссылки — не self-promo, а нативный Reddit-контент; фильтры бьют по промо/ссылкам, не по этому. Плюс это авторский **выход** (искренний шеринг = его нерв) И органично греет аккаунт под продуктовый пуш. «Прогрев-риск» и «последовательно не параллельно» — про монетизируемые ЗАПУСКИ, не про хобби-шеринг.
- **Тест-юзеры: платный путь — не компромисс, а ЛУЧШИЙ для автора.** Найти людей автору тяжелее, чем строить. Платные usability-платформы (Userbrain: think-aloud видео) превращают «просить одолжение» в «купить запись» — убирают саму просьбу. Потому первичен платный канал, не Reddit/друзья.
- **Clarity-баг (процесс):** устно подтвердил «раскомментировал Clarity», но правку НЕ применил → задеплоился закомментированный код. Урок: не рапортовать «сделано» без факта — проверять живой страницей (сделал через браузер). Clarity-деплой остаётся открытым.

## 2026-08-01 (волна прод-фиксов на живом тесте — критбаг звука, класс «эфемерный offscreen»)
Автор гонял 1.0.0 вживую (реклама уже идёт), принёс пачку багов; 6 закрыто, собран 1.0.1. Неочевидные «почему»:
- **Корень критбага доказан СИМПТОМОМ, не репро.** 07-22 пометили 🔶 «Chrome закрывает offscreen после тишины — не доказано репро». Симптом автора («увёл громкость в 0 → подождал → вернул — звука НЕТ») замкнул цепь: vol=0 = реальная тишина на выходе → Chrome гасит offscreen (reason `AUDIO_PLAYBACK` держит дом, лишь пока идёт ЗВУК, НЕ пока жив контекст) → умирает движок с фазой сессии → возврат громкости поднимает ПУСТОЙ дом (phase='off') → тишина. Урок: симптом-разрез точнее, чем гадание Chrome-таймингов вслепую — правильно было СПРОСИТЬ точный сценарий (`AskUserQuestion`), а не чинить наугад (память «intuition vs verified»).
- **Лечение — floor keep-alive, не восстановление.** Держать неслышимый пол master-гейна (~0.0015 ≈ −80 дБ, ниже 16-бит кванта) пока phase≠off → выход не молкнет → Chrome считает дом «играющим». Почему floor, а не «пересоздавать сессию при потере»: floor бьёт в ПЕРВОПРИЧИНУ (дом не умирает), восстановление лечило бы следствие и рисковало перезапуском рейса с середины. 🔶 зависит от эвристики Chrome — если выгрузит всё равно, второй эшелон (восстановление из панели) заложен на потом.
- **Эфемерность offscreen = КЛАСС багов.** Состояние сессии живёт только в памяти дома звука, Chrome вправе убить его в любой миг → веером: (а) конец таймера при свёрнутой панели не глушил звук — `turnOff` звалась ТОЛЬКО панелью через justEnded (промах: дом, что «переживает закрытие панели», обязан и ЗАВЕРШАТЬ сам → natural-end теперь зовёт свой turnOff); (б) rate-счётчик занижен (dropEmber в панели). Вывод: критичную мутацию состояния держать в доме/`chrome.storage`, не в панели.
- **Side panel НЕДЕТЕРМИНИРОВАН: то хранит DOM, то перезагружает.** Два симптома казались взаимоисключающими — призрак кнопки Finish (DOM сохранён, состояние устарело) vs прыжок ползунка громкости в дефолт (DOM перезагружен, взял HTML-значение). Не противоречие, а две ветки Chrome → лечение с ДВУХ сторон: `visibilitychange`→`sync` (догнать фазу) + персист громкости/energy/cocoon в localStorage (пережить перезагрузку; раньше персистился только завод времени).
- **Версия: инкремент от ОПУБЛИКОВАННОЙ, не от рабочей.** Бампнул 1.0.1→1.0.2, автор поймал: в сторе 1.0.0, рабочий 1.0.1 никогда не выходил (в dist только 1.0.0.zip) → правильный апдейт = 1.0.1. Перескок скрыл бы «потерянную» версию.
- **«Ускорение дыхания» на КРУПНОМ объекте меняет смысл.** Автор просил дыхание быстрее к концу — сделал (6.5→2.3с), автор отверг: «глубоко работающее сердце, не нужно». Мелкое медленное дыхание = спокойное присутствие; частое на большой звезде = орган/тревога. Вернули спокойное 7с. Тот же приём (пульс) на разном масштабе несёт разное чувство.
- **Живой тест = добыча давних багов.** 5 из 6 жили в проде 1.0.0 (не регрессии от правок). Ценность платного холодного старта не только в установках — баги всплывают на 100 юзерах, где дёшево, а не на 10k.

## 2026-08-04 — Аналитика: Clarity снят, порядок Umami → GA4 → Директ
**Решение (автора):** тепловую карту Clarity с welcome-страницы **не ведём**. Подключаем по очереди:
Umami (сейчас) → GA4 → Яндекс.Директ. Метку из HTML не выдираем — просто не читаем и не строим на ней пульс.
**Почему:** welcome — обычный лендинг-приветствие, на нём **нечего кликать**; тепловая карта отвечает на
вопрос «куда жмут», которого здесь нет. Логика карты пришла из ExportGPT, где интерфейс был на самой
странице, — здесь это перенос чужой формы, вторичный и неканоничный. Плюс у Clarity API жёсткий потолок
(10 запросов/сут, только 1–3 последних дня) — дорогой в обслуживании счётчик ради вопроса, который мы не задаём.
**Цена ошибки ≈ 0:** метка стоит и молча копит; понадобится — начнём читать.

## 2026-08-04 — Второй воздух: стенд, а не релиз-функция; и НЕ библиотека звуков
**Решение:** развилку «переключатель звука» отрабатываем **в деве, на стенде** (`lab/air-stand.html`) —
три воздуха одного рейса (тёплый · светлее · с тоном), а не список звуков и не загрузка файлов.
Стенд гоняет НАСТОЯЩИЙ `extension/engine.js` штатными ручками (`setChar`/`setTier`).
**Почему форма именно такая:** (1) ДНК паркует «библиотеку звуков/лупов» — данные это подтверждают
с другой стороны: поле шума (429k/мес) — рынок сна и железок, входить туда функцией = стать
пятидесятым noise-генератором без отстройки (`app-studio/research/focus_sound_demand_en_2026-08-04.md`);
(2) но индивидуальный разброс реален и измерен (интроверты страдают от фона сильнее; N-of-1-протоколы
подбирают музыку персонально) — один тембр не может подойти всем; (3) **строить почти нечего**: слой
гармонизации уже написан в движке и молчит за `tier:'premium'`, «светлее» — это existing `energy`/`masking`.
**Почему стенд раньше интерфейса:** закон ДНК feel-before-ship — автор решает ушами на реальной сессии;
и метод: «кнопочки убивают активацию», значит право на кнопку в панели ещё надо заслужить.

## 2026-08-06 — Иконка-таймер вместо язычка как носителя присутствия
**Решение:** постоянное присутствие времени даёт **иконка расширения** (`extension/icon.js`, дуга остатка),
а не язычок. Язычок остаётся опцией в настройках, не снимается.
**Почему иконка:** язычок — впрыск в ЧУЖИЕ страницы, за него платим правом `scripting`, необязательным host
и вниманием модерации, а даёт он одну функцию — дверь обратно. Иконка видна всегда, на любой вкладке, вообще
без вмешательства в чужие страницы, и клик по ней уже открывает панель. Итог: минус впрыск, минус сущность
в голове пользователя, плюс визуальный таймер, которого не было.
**Почему язычок ВСЁ РАВНО оставлен:** `optional_host` — заранее выданная почва под аффилиатную монетизацию
в поисковиках (канон 5.3) при 4–5К WAU. Если снять сейчас, потом придётся пугать набранную базу запросом прав
(`lab/permissions.md`, развилка 07-26). Право дороже, чем экономия одной настройки.
**Закон отрисовки:** показываем ОСТАВШИЙСЯ ПУТЬ, а не утекающее время — без цифр, без красного, без ускорения
к финалу. Постоянно видимый обратный отсчёт давит, а ДНК говорит «сопровождает, не наказывает».

## 2026-08-06 — Тон в продукте: ОДНА ручка, дефолт по центру только новым
**Решение:** тоновый слой идёт в продукт как опция с **одной** видимой ручкой «шум ⟷ тон». Остальное
(характер, плотность, мелодия, путник) замораживается на значениях, выбранных автором на стенде.
**Почему одна:** каждая вторая ручка требует решения в момент, когда человек пришёл работать, а не настраивать
(«кнопочки убивают активацию» + садовый закон «рост вглубь, не вширь»). Вторая ручка — только если попросит
живой голос.
**Почему ручка тянет НЕСКОЛЬКО параметров:** это не баланс двух громкостей. Кривая равномощная (при линейном
кроссфейде проседает середина); шум не просто тише, а УТОНЧАЕТСЯ (`masking` вниз), иначе стена забивает тон;
шум никогда не в ноль — дорога остаётся, тон едет по ней попутчиком.
**Почему центр только новым:** дефолт по центру меняет звук у тех, кто уже привык к чистому шуму. Отличить
новичка от старожила по отсутствию записи НЕЛЬЗЯ (у старого её тоже нет — настройка новая); отличать надо по
СЛЕДУ, который у старожила уже есть: `hearth.vol` или `hearth.sessions`.
**↑ ПЕРЕСМОТРЕНО 08-07 (см. запись ниже): дефолт — ноль для всех, деление на новых и старых снято.**
**Хранить в `chrome.storage.local`, не в `sync`:** у sync квота 120 записей в минуту, а крутилка при
перетаскивании пишет постоянно.

## 2026-08-06 — Ресурс листинга CWS читается OAuth'ом автора, не сервис-аккаунтом
**Решение:** GA4-ресурс листинга (`547365719`) читаем токеном автора (`analytics/ga4_oauth.py`), а не
сервис-аккаунтом.
**Почему (проверено, час потерян на это):** ресурс живёт в служебном аккаунте Google «Chrome Web Store
developer properties». Разработчик там гость без роли: `Property access management` не показывается,
администратора нет **ни у кого** — выдать доступ сервис-аккаунту физически некому. Но чтобы ЧИТАТЬ данные,
админка не нужна: хватает просмотра, который у автора есть. Отсюда второй путь авторизации.
**Следствие для сада:** у каждого продукта своя почта (изоляция по методу 1.3) → токен именованный
(`ga4_oauth.py exportgpt`), иначе второй вход затирает первый.

## 2026-08-06 — Маршрут в сцене: вехи по перспективе, мера = доля пути
**Решение:** второй ход направления «визуальный таймер» (после иконки) — **маршрут в панели**:
десять вех на весь рейс, оставшиеся впереди у звезды, пройденные проносятся мимо и уходят за кромку.
Код — `extension/route.js`, общий с `lab/route-stand.html` (тот же приём, что у `icon.js`: один
источник на две поверхности, чтобы вкус выбирался ровно на том, что уедет в продукт).
**Почему вехи, а не полоса:** наблюдатель летит В точку схода, и веха на самой оси схлопнулась бы в
точку. У вехи есть боковой вынос, экранный радиус живёт по перспективе `r = K / глубина` — это ровно та
геометрия, по которой уже несётся звёздное поле. Маршрут не нарисован ПОВЕРХ сцены, он в ней;
полоса-индикатор была бы интерфейсом на стекле.
**Почему десять на весь рейс, а не по минутам:** одна веха = десятая доля пути и в пятиминутном рейсе,
и в девяностоминутном. Тот же закон, что у дуги иконки: показываем ДОЛЮ пути, а не минуты.
**Почему у вехи ореол и ровный свет:** первая сборка провалилась на стенде — веха была неотличима от
звезды поля, и «остаток как расстояние» не читался. Различаем двумя тихими признаками: ореола у звёзд
нет (они точки), и звёзды дышат, а веха стоит. Размером и цветом не кричим — это снова стало бы интерфейсом.
**Почему в рассвете доля ЗАМИРАЕТ, а гаснет прозрачность:** прогресс в угасании убывает (звезда сдувается),
и если вести вехи по нему, путь поедет назад к старту — «рейс отменился». Рейс окончен, а не отменён.
**ОТКЛОНЕНО автором в тот же вечер:** «в сайдбаре всё ок, не надо пока больше ничего городить» —
сцена читается и без вех, звезда растёт. Панель возвращена к сторовой версии, файл и стенд запаркованы
в `lab/` (`route.js`, `route-stand.html`). Урок для меня: развилка «иконка + маршрут» была прочитана
автором не как я её задумывал — он выбирал состав РЕЛИЗА (иконка + тон + ручки), а не следующую стройку.

## 2026-08-06 — Сборка билда — скриптом с белым списком, не руками
**Решение:** `bash lab/build-zip.sh` собирает `dist/minimalist-timer-<версия из манифеста>.zip`
по списку файлов внутри скрипта и падает, если файла нет на диске или его нет в готовом архиве.
**Почему:** `icon.js` и `route.js` подключены мягко (`importScripts` в try, `typeof drawRoute`) — забытый
файл НЕ роняет расширение, оно молча едет без иконки-таймера и без маршрута. Такой билд прошёл бы
модерацию и приехал юзерам. Ручная сборка по памяти — единственное место, где это могло случиться.
**Защита от перезаписи:** существующий архив скрипт не трогает без `--force` — версия в `dist/` может
быть той, что уже лежит в сторе.
**Версию скрипт не считает:** единственный источник — `extension/manifest.json`, поднимает человек.

## 2026-08-06 (вечер) — Тон в продукте: слой едет по рельсам рейса, а не рядом
**Решение:** тоновый слой перенесён из стенда в `extension/tone.js` и подключён к движку так, что его
выход идёт в `engine.master`, а фазы рейса он получает от движка.
**Почему в master, а не в динамики:** у стенда пласт шёл прямо в `AC.destination` — там это правильно,
слой сравнивали отдельно. В продукте это дало бы разъезд: на паузе шум замолкает, а тон продолжает
играть; в рассвете шум истончается, а тон стоит стеной; keep-alive держит шум, а тон живёт сам по себе.
Один выход — одни рельсы: громкость, пауза, угасание и пол keep-alive применяются ко всему разом.
**Почему слой обязан знать фазы (закрытый узел 08-06 «бесконечность без прибытия»):** музыка, которая
одинаково звучит в начале и в конце, отменяет смысл рейса. Поэтому вход — в собирании (проступает за всё
собирание), стояние — в ткани, и РАЗРЕШЕНИЕ на рассвете: мотивы, ход и путник прекращаются, аккорд
возвращается на первое созвучие палитры, верхние голоса уходят. Уровень при этом гасит master — слой
своей громкостью в рассвете не распоряжается, иначе два затухания спорили бы друг с другом.
**Почему ручка не трогает громкость и кокон:** это ручки ПОЛЬЗОВАТЕЛЯ. Ручка тона подрезает шумовой
тракт (`noiseTrim`, пол 0.34) и утончает стену (`wallThin`) — относительный баланс, а не захват чужих
настроек. На стенде ручка писала прямо в громкость движка, в продукте так нельзя.
**Найдено на приёмке:** увёл ручку в ноль и вернул посреди рейса — пласт пересобирался НЕМЫМ: голоса
гудят, а аккорд не переселяется и мотивов нет. Причина: жизнь слоя запускалась только на смене фазы.
Теперь `_ensure` сообщает, что пласт родился заново, и жизнь возвращается вместе с ним.
**Гигиена состояния:** `clearTimeout` не обнуляет id, поэтому проверка «таймер снят» по `!!timer` врала
бы всегда. Поля таймеров обнуляются явно — иначе состояние слоя нельзя честно прочитать.

## 2026-08-06 (вечер) — Крутилки; дефолт по центру только новым, метка старожила
**Решение:** ручки звука — крутилки (вертикальное перетаскивание + колесо + стрелки): «Air» в доке,
«Energy» и «Cocoon» в допнастройках. Ползунок остался у громкости и у длины рейса.
**Почему крутилка:** в узком сайдбаре у ползунка ход короткий, точность плохая; крутилку можно вести
сколько угодно, не упираясь в ширину панели.
**Почему ползунки не тронуты:** громкость и длина рейса — то, что человек трогает чаще всего и уже умеет.
Менять освоенный контрол ради единообразия — риск на ровном месте.
**Метка старожила — только следы РЕАЛЬНОГО пользования** (`hearth.vol` · `hearth.sessions` ·
`hearth.embers`) и читается ПЕРВОЙ строкой панели. Поймано на проверке: в списке был `hearth.dial`,
который панель пишет сама при инициализации ползунка, — и «старожилом» оказывался каждый, кто впервые
открыл панель, то есть дефолт по центру не достался бы никому.

## 2026-08-07 — 1.1.0, а не 1.0.2; один идиом контролов; язычок починен
**Версия — 1.1.0** (слово автора). 1.0.x читается как заплатка, а тут новый слой звука и переделанное
управление: это функциональный релиз, номер должен об этом говорить.

**Все ручки звука — крутилки; полоса осталась ТОЛЬКО у времени.** Наблюдение автора: полоса времени
хороша и логична — длина рейса читается как РАССТОЯНИЕ (ассоциация со световыми годами). А полоса
громкости под ней — та же форма про совершенно другое, и получалась эклектика. Теперь форма несёт смысл:
полоса = путь, крутилка = количество.
**Симметрия дока:** над «Settings» две главные ручки (Volume · Tone), под ним — две служебные
(энергия · кокон). Верхние подписаны словом, нижние — только значком: смысл нижних несут значок и ховер,
а четыре подписи подряд превратили бы док в список.
**Имя ручки — Tone, не Air.** «Air» держался за внутреннюю метафору воздуха, но снаружи не читается;
рассматривался ещё «Space» (космос, таинственнее и в идиоме) — выбран Tone как однозначный.
**Подписи-лесенка под ручкой убраны** («noise + tone beneath» и т. д.): продукт нигде не комментирует
сам себя, звук слышно и так.

**ЯЗЫЧОК — ЛОЖНАЯ ТРЕВОГА, правки откачены.** Я принял «edge tab не работает» за поломку и нашёл в коде
два теоретически возможных обрыва (потерянный жест у `permissions.request`, немая запись в storage) —
после чего переписал включение и добавил фону прямой зов. Автор поправил: язычок работал всё это время;
он не появляется на вкладке `chrome://extensions`, и это НОРМА — в служебные страницы браузера расширение
впрыснуть не может по устройству Chrome. На обычных сайтах дверь была на месте. Всё, что я тронул,
возвращено к сторовому состоянию (блок сверен построчно с архивом 1.0.1).
**Урок — мой, не проекта:** «не работает» с одной вкладки — это симптом, а не диагноз. Прежде чем чинить
живой механизм по чтению кода, надо спросить, ГДЕ именно не работает: правдоподобная гипотеза о поломке
дороже симптома, и я поменял рабочий код по своей догадке. Сперва воспроизвести, потом лечить.

**Открыто (за автором):** язычок стал третьим носителем присутствия — сайдбар · иконка в тулбаре ·
впрыск в страницу. Автор сам назвал это избыточностью. Моя ставка: иконка закрыла присутствие, у язычка
осталась ровно одна работа — дверь обратно; держать его выключенным по умолчанию (так и есть: без прав
он не появляется) и не выносить в глаза. Право `optional_host` при этом сохраняется как почва под
аффилиатную монетизацию (канон 5.3) — снимать его сейчас дороже, чем оставить (DECISIONS 08-06).

## 2026-08-07 — Дефолт тона: ноль для всех, а не центр новым
**Решение (автор):** при установке ручка «Tone» стоит на нуле. Развилка «новым по центру, старым ноль»
(08-06) отменена вместе с определением старожила — делить пользователей стало не на что, код снят.
**Почему это правильнее:** весь листинг продаёт тёплый шум, и самый сильный из добранных болевых ключей —
`brown noise no ads` (2900). Человек ставит расширение, чтобы получить шум без рекламы; если первое, что
он слышит, — музыкальный пласт, он получил не то, за чем шёл, ещё до того, как узнал про ручку.
Тон должен быть тем, что НАХОДЯТ, а не тем, что выдают вместо обещанного.
**Побочная выгода:** снят целый слой хрупкости. Определение старожила читало localStorage в строго
определённый момент и уже один раз ошиблось (ловилось на `hearth.dial`, который панель пишет сама).
Механизма нет — ошибаться нечему.

## 2026-08-07 — Канон меряет плотность, редактура меряет смысл
**Решение:** снять FAQ-пункт «Why not just play brown noise in a streaming tab?» из полного описания,
хотя канон его пропускал.
**Почему это не спор с методом:** канон 2.1 задаёт потолок ПЛОТНОСТИ (частота ключа ≤3 % по Тургеневу,
небольшой выход в красное на 1–2 вхождения допустим) и для полного описания прямо ТРЕБУЕТ повторов —
Main Keywords 15–20 раз. У нас `ads` стоял на 0.67 % при потолке 3 %, то есть вчетверо ниже. Резали не
за спам, а за то, что одна мысль была сказана трижды подряд: буллет, абзац, вопрос-ответ. Тот же раздел
канона отдельно велит вычищать лишние слова руками — это и было сделано.
**Как выбирали, что резать:** по ключам, а не по вкусу. FAQ-пункт своих ключей не нёс (`no ads` и
`black screen` есть в абзаце), а `deep brown noise` 1600 живёт ТОЛЬКО в абзаце — значит абзац
неприкосновенен, режется FAQ.
**Побочный эффект, за которым следить:** текст стал короче на 300 симв, и доля `timer` выросла
2.79 → **2.97 %** при потолке 3 %. Флагнет Тургенев — менять одно вхождение на синоним, не вычёркивать.
**Правило на будущее (важнее самой правки):** правило «каждый ключ по одному разу» — это правило
КРАТКОГО описания (не-главные слова ≤2, безопаснее 1–2). В полном описании оно не действует, там
повторы обязательны. Прежде чем чинить текст по памяти — открыть `knowledge-base/module-2/2.1`.

## 2026-08-09 — Просьба об оценке: карточка поверх звезды (паттерн ExportGPT), версия 1.1.1
**Решение (автор):** тихий ряд звёзд в доке больше не единственная просьба. С **третьего зачтённого
рейса** по прибытии над звездой встаёт **карточка** «How was the flight?» с пятью звёздами. Пока она
висит, взлететь нельзя: клик по небу её СБРАСЫВАЕТ, но рейс не поднимает. Сброс — не отказ навсегда:
после следующего рейса карточка вернётся. **Первый ответ звездой закрывает просьбу НАВСЕГДА** (≥4★ →
отзывы CWS, 1–3★ → форма автора). Референс взят по коду, не по памяти: `save-chatgpt-as-pdf/common.js`
395–469 — `gptpdfShowRateUs` подменяет лицо кнопки Export, клик вне → `gptpdfRevertToExport`, счёт в
`gptpdf_export_count`, терминальный флаг `gptpdf_rated`.
**Почему не тихий ряд:** ряд в доке видел только тот, кто уже решил оценивать, — то есть почти никто.
Просьбу видят там, куда смотрят, и тогда, когда ценность только что получена (прибытие). Цена нахальства
оплачена возвратом: человек не заперт, он лишь не может сделать вид, что просьбы нет.
**Почему с 3-го, а не со 2-го, как в ExportGPT:** экспорт PDF — одно нажатие, знакомство с ним кончается
на первом. Рейс — интерфейс, в котором надо разобраться (ползунок, две ручки, звук); первые два уходят
на «что это вообще делает». Просить в них — просить оценить то, чего человек ещё не понял.
**Зачтённый рейс ≠ любой конец.** ExportGPT считает УДАЧНЫЕ экспорты; здешний эквивалент удачи — рейс, в
котором тебя реально несли. Порог `ASK_MIN_MIN = 1` «минута» несомого времени: тычок в звезду
«послушать, что это» счётчик не двигает. А вот ТИП конца не различаем (рассвет или свой выдох): у
∞-рейса рассвета нет вообще, и гнать таких мимо просьбы — терять как раз самых верных.
**Одна просьба на экране:** пока карточка висит, ряд в доке скрыт; после сброса ряд возвращается — он
дверь для того, кто захотел ответить сам, не дожидаясь прибытия.
**Что осталось честным:** просьба не лезет в рейс (только фаза `off`) и не забирает док — громкость,
время и настройки под вуалью работают. Мы просим, а не запираем.
**Мягче референса на одну строку:** в карточке есть видимое «Not now». У ExportGPT выход невидим —
догадайся кликнуть вне кнопки. При 49 установках невидимый выход дороже пары недополученных отзывов:
«оно требует оценку» приходит одной звездой в тот же стор, который мы кормим. Строка снимается одним
словом автора, если решит, что мягкость лишняя.
**Граница, которую мы не перешагнём знанием:** дошёл ли человек до отзыва в сторе, мы не видим —
`hearth.rated` ставится по клику по звезде, как и `gptpdf_rated`. Спрашивать повторно того, кто уже
ответил, значит наказывать за отклик; цена — часть кликов не станет отзывами, и видно это только в
дашборде стора.

## 2026-08-10 — Две громкости одной просьбы: плашка-дверь в углу и карточка-просьба поверх звезды
**Решение (автор):** тихий ряд звёзд остаётся постоянной плашкой, но переезжает **под закрытый Settings,
в правый нижний угол, неброско**, и видна **СРАЗУ, с первого открытия панели** — «она должна появиться
сразу же» (слово автора). Карточка поверх звезды остаётся как есть — с третьего рейса. Пока карточка
висит, плашка скрыта.
**Почему две, а не одна:** это разные жесты. Плашка — ДВЕРЬ: человек сам решил сказать спасибо, и ему
нужно место, куда пойти. Карточка — ПРОСЬБА: мы окликаем в момент прибытия. Раньше дверь и просьба
стояли на одном затворе (с третьего рейса), и тот, кому зашло с первого рейса, до третьего не имел
никакого способа ответить — терялся самый тёплый отзыв, какой бывает.
**Почему сразу, а не после первого рейса (развилка закрыта автором).** Я предлагал открывать плашку
после первого зачтённого рейса: просить у человека, который ещё ни разу не летал, нечестно, и он может
поставить звезду до того, как поймёт, за что. Автор решил иначе: **дверь не открывается по расписанию** —
захотел сказать, говори сейчас. Это последовательно с самой природой плашки: она не просит, она просто
есть; расписание — свойство ПРОСЬБЫ, и оно осталось у карточки (третий рейс).
**Цена решения снята подписью — и подпись же сняла лишнюю сущность.** «Rate this flight» человеку,
который не летал, читается небрежно, поэтому я развёл две подписи по счётчику рейсов. Автор оборвал это
одним словом: **«Rate us»**. Одна строка вместо двух и вместо ветки в коде — и метафора не пострадала,
её несёт ховер-подсказка «a rating helps others find the flight». Урок общий: когда правка добавляет
сущность (здесь — состояние подписи), сперва проверить, не решается ли задача сокращением.
Порог зачёта рейса не изменился: 1 «минута» несомого времени — он касается только карточки.
**Почему НЕ правый верхний угол** (моё возражение, автор согласился): верх — это поле рейса, а там по
онтологии капсулы одно присутствие, звезда. Бейдж со звёздами в этом поле — четвёртый носитель
присутствия рядом с тремя, которые автор уже назвал избыточностью. Во время рейса его всё равно
пришлось бы гасить, то есть «постоянной» плашка там не была бы; а в покое ровно эту работу делает док.
Плюс продукт продаёт «мягкий фокус, который не наказывает» — постоянная просьба в поле зрения
работающего человека противоречит обещанию.
**Поправка вводных, которая изменила решение:** «сделаем как в ExportGPT» опиралось на память, будто там
плашка на поверхности. По коду (`save-chatgpt-as-pdf/templates.js:1196`) тихий «Rate us» живёт ВНУТРИ
меню «More», за кликом — постоянный, но на виду его нет никогда. Так что структурный аналог у нас — не
бейдж в поле неба, а тихая строка в доке; выбранный угол ближе к референсу, чем казалось.
**Размер = громкость:** звёзды в плашке 15px и приглушены (.62, оживают под курсором), в карточке 25px
в полную силу. Одна и та же воронка, разная настойчивость — и это видно глазом, без подписей.

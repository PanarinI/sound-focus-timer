# Имя продукта: дверь ADHD — перезамер 2026-09-24

Повод: решение автора 24.09 (см. `DECISIONS.md` 2026-09-24) — самодельное ограничение «ADHD только
в SEO-теле» снято, `adhd timer` стал кандидатом в имя. `visual timer adhd` исключён из семьи словом
автора: продукт аудиальный. Цена замера: **$0,1354** (см. `spend.log`), перепись стора — $0.

## Главное (Google US, Google Ads; среднее за 12 мес / август 2026)

| дверь | среднее | август | сложность | год |
|---|---|---|---|---|
| **`adhd timer`** (= `timer for adhd` = `timer adhd`, один ряд DFS) | **1 300** | 1 300 | 0–1 | −8 % |
| `minimalist timer` — нынешнее имя | 590 | 880 | — | +112 % |
| `brown noise for adhd` | 720 | **1 300** | 0 | +204 % |
| `visual timer adhd` (= `adhd visual timer` = `visual timer for adhd`) | 1 900 | **4 400** | 0 | +174 % |
| `adhd music` | 5 400 | 4 400 | — | −23 % |
| хвосты: `adhd timer for adults` 90 · `adhd timer app` 70 · `adhd focus timer` 40 · `best adhd timer` 40 | | | | |

- Порог канона для двери CWS — 750 в месяц. `adhd timer` его проходит, нынешнее имя — нет.
- `adhd timer extension` и `adhd timer chrome extension` в базе отсутствуют ⬜ — расширение этими
  словами не ищут, значит дверь работает внутри стора, а не в Google.

## Занятость стора (перепись 24.09, `census-adhd-2026-09-24.log`)

`adhd timer` → **WEAK**: имя держит «ADHD Timer — Focus Timer & To-Do Side Panel», 10 пользователей,
5 языков, карточка не оптимизирована. Крупнейшие в этой выдаче — Tomato Timeout (ADHD Focus Timer,
520 юз), Meowdoro (3 000, без ADHD в имени), New Tab Pomodoro (423). **Minimalist Timer стоит №10
в выдаче по `adhd timer` уже сейчас, 123 пользователя, 53 языка.**
`minimalist timer` → держим мы сами (№2); выше нас «Minimalist Focus Timer», 1 000 юз, не оптимизирован.

## Выдача Google (верхние 10, `serp.md`)

`adhd timer` — Amazon и Time Timer (физические диски), visualtimer.com, Reddit, add.org, ADDitude,
медицинский блог; плюс товарный блок. `brown noise for adhd` — add.org, ADDitude, Healthline,
Understood, Spotify. То есть в Google по обеим дверям стоят товары и статьи, расширений нет:
канал для нас — поиск внутри стора.

## Что НЕ сделано
Мировые числа (без страны) не снимались · слой App Store не мерен · позиция в сторе после
переименования не предсказана (только 🧮 по канону «имя = ключ») · отзывы конкурентов не читались.

## Файлы
`run.py` (suggest · volume · serp) · `keys.csv` (72 ключа, помесячно 2025-09 → 2026-08) · `serp.md` ·
`census-adhd-2026-09-24.log` · `spend.log` · `raw/`.

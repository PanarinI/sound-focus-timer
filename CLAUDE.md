# Sound Focus Timer — живой контекст (Project DNA)

> Зерно проекта: само себя выращивает. Читать в начале сессии, обновлять в конце
> (шаббат). Держать маленьким и честным.

## 1. Суть (Charter) — живая гипотеза, не контракт
(«пока не знаю» — ок; пересматривается на шаббате)
- **Что это:** CWS-расширение (**десктоп** — расширения не ставятся в мобильный Chrome) — **фокус-ТАЙМЕР,
  чьё время сделано из звука**. Якорь сути: *мягко вводит в фокус и бережно выводит — сопровождает, не наказывает.*
  Таймер = существительное (дверь спроса · деньги · идентичность); звук = его тело/душа/клин (**НЕ** шум-генератор
  с таймером сбоку — это park). Материал — **тёплый генеративный brown-ШУМ** (+pink-воздух +пространство +микро-дрейф;
  на устройстве; НЕ синусовый дрон). Сессия: вход «минута до» → плато → выход-«рассвет» (не будильник).
  Позиция: «мягкий фокус» против блокеров-наказателей. **Ядро — ADHD-взрослые** (звук функционален:
  возбуждение+маскировка); отстройка бьёт в ОБА их полюса — мягко ВВЕСТИ (трение старта) и не РАЗОРВАТЬ (гиперфокус);
  шире — все, кто ищет спокойный фокус.
  **Фрейм (реш. автора 07-25): не «отсчёт, который терпишь», а РЕЙС, на который садишься** — закоммитил маршрут →
  тебя НЕСЁТ → прибытие. Ценность = обязательство + быть-несомым + прибытие, а не счёт минут (это и делает «тело
  звуку / протез времени» ОЩУТИМЫМ). «таймер» = как тебя НАХОДЯТ (SEO-двери), «рейс» = как ЧУВСТВУЮТ.
  **Поверхность (кожа развёрнута 07-25: очаг/уголёк → РЕЙС К ЗВЕЗДЕ; история в DECISIONS 07-23…07-25):**
  дом = **нативная Chrome Side Panel** = поле-космос, где ты летишь к тёплой ЗВЕЗДЕ по курсу (одно присутствие,
  дышит, НЕ персонаж; приближается по прогрессу сессии — рост=приближение, не раздувание). **Тап по звезде = взлёт** ·
  время — **ПОЛЗУНОК** под сценой (`5·15·25·45·90·∞`) · **энергия = скорость полёта**. Вне времени суток (космос ≠ сутки).
  Звук = воздух, сквозь который летишь (а-концептуален, тяготеет к воде/воздуху, с теплом связан кросс-модально; НЕ огонь).
  Язычок-отблеск у кромки = дверь обратно в рейс (когерентность сцене — открытый узел). PiP · new-tab · «мир» — метаморфозы
  ПОТОМ (метод: «кнопочки убивают активацию»). **feel-before-ship (реш. автора): не выпускать, пока автор не почувствует
  ценность кожей** (летит реальную сессию) — автор может быть НЕ целевым (гиперфокусер), потому + 1 живой ADHD-тестер после релиза.
  **Монетизация по методу (поправка автора 07-22): слоёв «free vs платные функции» НЕТ** — на старте бесплатно
  всё, при росте платным становится ДОСТУП целиком (после вехи 2); `tier basic/premium` в движке = техническая
  **полка экспериментов** (гармонизация · характеры), не монетизационная архитектура. Полный фокус+душа+открытые узлы — в `STATE.md`.
- **Зачем / цель:** честный коммерческий запуск по методике студии (продукт CWS-дорожки app-studio;
  доход + серийная практика). Автор — первый пользователь (личная тяга = актив, не аргумент выбора).
- **Готово, когда:** **Веха 1 — опубликован в CWS** (закрывает форму «стройка»; дальше метаморфоза:
  организм линяет в продукт-ДНК). **Веха 2 (уже продукт) — 2000 юзеров** (порог разговора о
  монетизации по методике). Рабочее имя продукта для стора — не выбрано (задача вехи 1, с бронью).
- **Ограничения:** соло + Клод · бюджет ~$200–300, ориентир 1–2 недели до публикации · всё на
  устройстве (приватность by design, $0 инфры) · имя МОЖЕТ нести «Pomodoro» (07-26: канон не запрещает,
  бренд-лист `1.4:39` без него; Cirillo владеет «Pomodoro Technique®», не словом в таймерах) ·
  MVP без слежки за вкладками (права/модерация) · Игорь — вайб-кодер (объяснения по-человечески).
- **Не-цели:** не ЖЁСТКИЙ блокер-наказатель (мы зовём, не запираем; **mindful-мягкий блок — открытый узел v2**,
  см. STATE) · не библиотека звуков/лупов (звук генерируется, не проигрывается) · не «нейро-наука фокуса» с
  громкими клеймами · слежка за поведением («зов» из дрейфа) — НЕ в MVP, это v2 (шину событий закладываем сразу) ·
  App Store-версия — не сейчас (на рост после вехи 2).

## 2. Столпы (живая карта)
Позвоночник: этот файл (суть+правила) · STATE.md (где мы) · DECISIONS.md (почему).
Органы:
- `AUDIENCE.md` — кто пользователь (ядро · двери · конкуренты · функц. настройки vs красота). Рожд. 07-05.
- `MECHANICS.md` — спека механики MVP (автомат состояний · жесты · пороги чести · звук по фазам · заслоны · микро-развилки ⚖).
  ⚠ ОБРАЗ-слой (жар-кино/угли) РАЗВЁРНУТ 07-25 в «рейс к звезде»: механика (фазы, два конца) валидна, визуал-онтология — в STATE/DECISIONS. Рожд. 07-16.
- `schemas/` — схемы сущности для автора (product-essence · audience-portrait · якорь «одна функция»). Правило «визуализируй сущности».
- `lab/` — верстак: стенд звука (`taste.html`) + дизайн-стенды (`star-stand`·`compare-stand`·`dose-stand`·`ember-stand`·`glow-stand`) + **ЛИСТИНГ CWS (07-26)**: `meta-keywords.md` (ключи) · `listing-draft.md` (EN имя+краткое+полное — ФИНАЛ) · `turgenev-paste.txt` · `permissions.md` (обоснование прав 3.6) · **`translations.md`** (досье переводов: дверь локалей=pomodoro, выверка DFS/Wordstat) · **`locales-data.json`+`build-locales.py`** (воспроизводимый пайплайн 53 локалей NAME+SHORT → `_locales/`) · **`full/`** (FULL per-язык: 44 нативных + `en.md`; транслит-8 на EN-fallback) · **`cws-upload-full.js`** (скрипт массовой загрузки FULL в CWS, канон 3.6) · `graphics/` (иконка-искра + баннеры). Рожд. 07-05.
- `extension/` — сам продукт (MV3, **СОБРАН и работает вживую 07-22**). `engine.js` — движок (**Brown-ядро** +
  дирижёр фаз + arousal + tier basic/premium + гармонизация + `extinguish`). **`hearth.html`/`hearth.js` — ПАНЕЛЬ
  = РЕЙС К ЗВЕЗДЕ** (звезда·полёт·ползунок; развернула образ жар·угли·очаг 07-25; `?b=18`). **Звук переживает закрытие панели:** `offscreen.js` (дом
  звука) + `remote.js` (дальний движок — тот же контракт, что `AudioEngine`). `glow.js` — язычок-отблеск у правой
  кромки. `background.js` — service worker (иконка→панель · offscreen · welcome-по-install · отблеск). `orb.html`/
  `sidepanel.*`/`popup.*`/`dev.html` — **ОТЖИВАЮТ**. Служебные страницы (welcome) — отдельный ПУБЛИЧНЫЙ репо
  `PanarinI/focus-pages` (код продукта остаётся приватным). **Локализация (07-26):** манифест `name/description`=`__MSG__`+`default_locale:en`; `_locales/<code>/messages.json` × 53 языка (генерятся из `lab/locales-data.json`).
- `SOUND.md` — **язык звукового дизайна**: слуховой разбор фазных прецедентов (Endel/Session/Brain.fm/Noisli)
  + 8 калибровочных уроков для входа/плато/рассвета (тетрадь узла «дрейф»). Рожд. 07-22 из дознания.
- `LAUNCH.md` — **путь публикации CWS** (чеклист выкладки, готовые тексты, скрипт загрузки). **Рождён 07-27** на вехе 1. Живёт до аппрува; после публикации → архив (метаморфоза в продукт-ДНК).
Досье ниши живёт в студии: `app-studio/research/sound_focus_timer_niche_report.{md,html}` (+ IDEAS.md).

## 3. Протокол роста — дыхание организма
- Вдох (старт сессии): читать этот файл → STATE → DECISIONS → нужные органы.
- Обмен веществ (всегда): любую входящую мысль направляй — устоявшееся/структурное → в столп;
  накапливающееся знание → в орган; сырое → держи в живом контексте. Всегда говори, куда положил.
- Рост: инфа повторяется и не влезает в столпы → предложи орган (имя+зачем), рождай с «да».
- Выдох (шаббат): обнови STATE; допиши «почему» в DECISIONS; поправь этот файл при смене сути/карты;
  пересмотри столпы (родить/слить/убрать); выдай «первое сообщение для новой сессии».

## 4. Законы курирования
Весь закон висит на двух: **возлюби суть** (сигнал, не лог) · **возлюби того, кого тут не было**
(пиши для следующей сессии). Производные: один факт — один дом · стабильное ≠ волатильное ·
структура по весу (YAGNI) · рост вглубь, не вширь (новый принцип вытесняет, не добавляется) ·
столпы малы — читаются на одном дыхании · имена папок/файлов/команд — латиницей · доки в git.

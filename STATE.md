# State — где мы сейчас

Последнее обновление: **2026-07-26** (шаббат, вечер). Этап: **К ПУБЛИКАЦИИ** — за сессию закрыты все ЛИСТИНГ-БЛОКИ (имя, мета, графика, палитра).
**Имя `Minimalist Timer` закреплено + ЗАБРОНИРОВАНО** (бот буткемпа `the_bootcamp_bot`; имя = сам ключ-дверь, канон 2.1.2:23 — отдельного «сочинения» нет). Дверь `minimalist timer` (480/KD1/+125%год/transactional/CWS-free) сверена автором в AppDB (свободна, 8 неоптим держателей). Звук=ОТСТРОЙКА словами в описании (не дверь — «время из звука» не ищут).
**Мета 2.1 — ГОТОВА** (`lab/listing-draft.md` + `turgenev-paste.txt`: имя+краткое 115симв+полное ~4470симв; все критерии 2.1 ✅ + окрашенность `minimalist timer` #1 в словосочетаниях; Тургенев ПРОЙДЕН автором — 1 красный флаг `timer` ДОПУСТИМ по правилу автора, зафиксировано в `app-studio/METHOD.md §2.1 п.5`). Ключи-верстак `lab/meta-keywords.md`. pomodoro в тексте — умеренно (реш. автора). ADHD — только в глубине SEO-тела, не в публичном лице.
**Графика 3.1 — ГОТОВА** (`lab/graphics/`): иконка-ИСКРА (форма 2, тёмно-синяя плашка + янтарная искра, набор 16-128 → `extension/icons/`) · малый баннер 440×280 · большой баннер 1280×800. Большой = сцена из РЕАЛЬНЫХ элементов (глубокий синий + звезда + warp-полёт) + текст «Made of warm sound / brown noise · a gentle focus timer». **Урок баннера (реш. автора):** буквальный скрin интерфейса НЕ передаёт суть (суть=ЗВУК, а его SEO не приводит) → генерируем сцену из НАСТОЯЩИХ кирпичей продукта (честно) + суть-звук несёт ТЕКСТ; выдуманная waveform = обман (отвергнута). Метод 3.1 разрешает «отдельные элементы / упростить».
**Манифест синхронизирован** (было кодовое «Ember» → `name`=`Minimalist Timer`, `description`=краткое, иконки-искра).
**ПАЛИТРА ПРОДУКТА → глубокий синий почти чёрный** (реш. автора, `hearth.html`+`hearth.js`: body `#0e1638→#090d22→#05060f`; аура `#room` база .06 — холоднее, автор вернул после пробы .18). Звёзды+звезда тёплые = контраст. Панель, оба баннера, иконка — ЕДИНАЯ гамма (баннер честен к продукту).

## Фокус инструмента (якорь)
> **Фокус-таймер, чьё время сделано из тёплого звука. Фрейм (реш. автора 07-25): не «отсчёт, который терпишь», а
> РЕЙС, на который садишься — закоммитил маршрут → тебя НЕСЁТ → прибытие.** Ценность = обязательство + быть-несомым +
> прибытие (не счёт минут). Это и есть «тело звуку / протез времени» ощутимо: не смотришь время — ЕДЕШЬ в нём.

«таймер» = как тебя НАХОДЯТ (SEO-двери `focus/aesthetic/cute timer` 4.4k/2.9k/2.4k; рэк студии 42/50) · «рейс» = как ЧУВСТВУЮТ.
Ядро — ADHD-взрослые (боль = слепота-ко-времени, `time blindness` 9.9k). НО автор может НЕ быть целевым (гиперфокусер, таймерами
не пользуется) → правило **feel-before-ship**: автор летит реальную сессию (ценность кожей) + 1 живой ADHD-тестер ПОСЛЕ релиза.
Клин «звук=тело времени + мягкие границы, без нейро-обещаний» свободен. Досье: `app-studio/research/focus_sound_revision_2026-07-22`.

## Онтология — РЕЙС К ЗВЕЗДЕ (развернула «очаг» 07-25; история → DECISIONS)
- **ЗВЕЗДА** — одно тёплое присутствие по курсу; ПРИБЛИЖАЕТСЯ по прогрессу сессии (рост = приближение, не раздувание →
  «закипать» невозможно by design); дышит; НЕ персонаж (три нельзя: не являть существо / не давать взгляд / не заставлять опекать).
  Прибытие = мягкий финал (не будильник). Тёплая янтарная — не холодный сай-фай.
- **ПОЛЁТ** — искры сносит от центра наружу = летим вперёд; движение ТОЛЬКО когда летим (покой = искр нет, не взлетел · пауза = гаснут).
  Двойное чтение (приближение к звезде ↔ шар, из которого летят светлячки) — ХРАНИМ, это подарок.
- **ВОЗДУХ-КОСМОС** — тёплое янтарное поле, ВНЕ времени суток (космос ≠ сутки; рассвет-как-цикл ОТКЛОНЁН автором — привязка к суткам).
- **ЗВУК** = воздух, сквозь который летишь. А-концептуален: работает функцией (маскировка), не образом; тяготеет к ВОДЕ/ВОЗДУХУ,
  НЕ к огню; с «теплом» связан кросс-модально. Материал — тёплый brown (неизменен). Закон «без потрескиваний» цел.
- Прежняя онтология ОЧАГ·ЖАР·УГЛИ (эра «уголёк в янтарной атмосфере»; отвергнута автором «картошка с ободком») — в DECISIONS 07-23/24/25.

## Механика — MECHANICS v1.1 (метафора-агностична, пережила пивот)
Автомат 4 состояния · вход-нарастание → плато → рассвет («таймер решил», ~40с) / выдох («я решил», 3–5с) · принцип двух концов.
**Кожей изменилось (07-25):** завод — не кручение, а **ПОЛЗУНОК** под сценой (`5·15·25·45·90·∞`, snap, подсветка) · **тап по звезде = взлёт** ·
**энергия = скорость полёта** (искры быстрее/медленнее — слабо, но заметно) · кокон (маскировка) — только звук, без визуала.
Накопление сессий: данные копятся, визуал ЗАПАРКОВАН («журнал рейсов» позже). Звук-фазы/маяки — SOUND.md (узел ниже).

## Что построено (экст MV3; **палитра = глубокий синий** 07-26; звук в расширении играет)
- **Панель = РЕЙС К ЗВЕЗДЕ во ВЕСЬ сайдбар** (`hearth.html/js`, переписана 07-25): звёздное небо на `<canvas id=sky>` — покой=звёзды
  левитируют+дышат (небо стоит с 1-го кадра), полёт=медленно плывём СКВОЗЬ них от звезды-цели наружу, ПАУЗА=поток замирает,
  энергия=скорость потока (плавно, без прыжка — множитель кадра, НЕ CSS-duration; старый баг-«прокрутка» мёртв). Звезда-цель
  (`#star`, размер ← `--p`) + аура (`#room` ← heat), управление доком снизу. **`ResizeObserver`** обязателен: canvas в side panel
  получал 0-высоту на старте → небо схлопывалось в горизонтальную ПОЛОСУ (звёзды сеются только с реальным размером). Прежний квадрат
  `.field` / CSS-`.sparks` / кольцо / уголёк — УБРАНЫ. Скрипты `?b=22`. Тюнинг: 60 звёзд, движение тихое, мерцание мягкое (не рябит).
- **Звук в offscreen** (`offscreen.js`+`remote.js`, контракт AudioEngine; B2-фикс: `_ensure` всегда спрашивает фон + очередь `_chain` + ретрай).
  Переживает закрытие панели. ✅ **В РАСШИРЕНИИ ИГРАЕТ** (подтв. автором 07-25, offscreen-путь ожил после чистой переустановки); стенд — 0 ошибок.
- **Язычок-дверь** (`glow.js`/`background.js`): постоянная дверь (тлеет в покое, ярче в сессии; крестик/тумблер `storage.glowEnabled`;
  усыновление сирот-поколений; вкладки оживают на переключении). ⚠ **когерентность НОВОЙ сцене не переосмыслена** (узел 1).
- **Welcome** panarini.github.io/focus-pages/welcome/ (onInstalled) · **Uninstall** panarini.github.io/focus-pages/uninstall/
  (форма автора встроена ✅; `setUninstallURL` стоит; Reinstall-ссылка появится с публикацией).
- **Оценка** (ExportGPT): со 2-й завершённой сессии, в покое; ≥4★ → CWS (`RATE_URL` пусто до публикации, молчит) · 1–3★ → форма автора ✅.
- Права: `sidePanel storage offscreen scripting` + `optional_host` (язычок, по кнопке). Стенд localhost 8781 (движок напрямую). `orb.*/sidepanel.*/popup.*` — отжили.
- **Наука шумов перекалибрована** (AUDIENCE 07-25): эффект мал (g≈0.25) + в основном на ДЕТЯХ · SR убрать · brown = 0 исследований ·
  «тёплый шум без нейро-клеймов» = самая точная позиция; закон «гладко, без потрескиваний» согласуется с доказанной базой.

## «Премиум» = ПОЛКА ЭКСПЕРИМЕНТОВ (терминология, автор 07-22)
Слоёв «free vs платные функции» НЕТ: на старте всё бесплатно, при росте платным станет ДОСТУП целиком (веха 2 = 2000 юз).
`tier` в движке = склад идей (гармонизация · характеры · «прибой»), не монетизация.

## ✅ УЗЕЛ РЕЛИЗА №0 — СНЯТ (звук в расширении играет, 07-25)
Автор подтвердил: в расширении звук ЕСТЬ (offscreen-путь ожил после чистой переустановки). Блокер релиза №0 закрыт.

## Открытые узлы (дизайн / полка)
1. **Язычок — когерентность рейс-сцене** (glow.js, extension-only): оставить, но «нить тёплого света ПОЛЁТА у кромки» (тоньше/лучевее,
   крепнет к прибытию), а не «жар-плашка». Делать тем же extension-проходом, что и возврат звука.
2. 🎚 **Звук-верстак** (`lab/`, ухом автора): дрейф слышимее (2-й слой 8–20с) · плато одна пологая дуга · вход щедрее · маяки 10/2 мин
   в материале · «прибой» на полку. Тетрадь SOUND.md. Публикацию НЕ блокирует.
3. **Накопление = «журнал рейсов»** (данные копятся, визуал запаркован при пивоте): форму придумать позже (было «тепло очага», DECISIONS 07-23).
4. **Тест звука в наушниках** (brown-низы полны в наушниках, режутся на колонках).
5. **Полка (не MVP):** mindful-блокер v2 · платный доступ (после роста) · ИИ-слой · new-tab · «+5 минуток» · «прибой» ·
   взвесь-искры (доза C) · финал-хранение (пылинка→след-вкрапление).

## App-Store-маяк (07-17)
HyperFocus — ADHD Focus Timer (iOS) — философский двойник («guilt-free», «stopping midway is success»), но геймификация, НЕ
звук-присутствие → валидирует угол, клин цел. iOS = горизонт после вехи 2.

## Следующий шаг — К ПУБЛИКАЦИИ (листинг + графика + ПЕРЕВОДЫ ГОТОВЫ 07-26)
Готово: звук ✅ · кожа рейс-к-звезде ✅ · язычок ✅ · имя+бронь ✅ · мета 2.1 ✅ · графика 3.1 ✅ · **права-обоснование 3.6 ✅** (`lab/permissions.md`) · **feel-before-ship ПРОЙДЕН ✅** · **переводы: 53 локали NAME+SHORT + 8 топ FULL ✅** (`extension/_locales/`, манифест `__MSG__`, `lab/full/`).

**За Клодом (след. сессия — по приоритету):**
1. ⚡ **finish-правка (НЕ СДЕЛАНА, из feel-before-ship):** ручной `finish` (`engine.js:274 extinguish`) уводить в тишину БЫСТРЕЕ — «в плавности нет смысла, когда юзер сам нажал» (ухудшает поведенческие). Авто-рассвет `DAWN=120` НЕ трогаем (принцип двух концов 07-17). Fade почти мгновенный, но не 0 (иначе щелчок). **Сделать первой.**
2. **FULL хвост** (~11 европ: nl sv da no fi pl cs tr uk ro hu + сложные): перевод EN-полного по образцу `lab/full/de.md`; грузится в CWS отдельно per-язык (менее критичен, метод `3.4:38`).
3. ⚖ **Права-развилка язычка** (открыта после канона 5.5): метод рекомендует МАКСИМУМ host + артефакт-на-всех-страницах (=наш язычок) РАНО, чтобы не терять базу на апдейте (окно Remove → −половина). Наш `optional_host` (07-12) — вариация (чистая установка ценой «host спит до кнопки»). Развилка **required-рано vs optional** — решить С АВТОРОМ (влияет на монетизацию-аффилиат после роста). См. DECISIONS 07-26.
4. Опц.: разрядить 2 плотных куска `timer` в полном описании EN.

**За автором:** сверить иконку-искру в тулбаре вживую (`chrome://extensions` → load unpacked) · выбрать: 8 сложных локалей — имена-транслит на вычитку носителем или так · после релиза 1 живой ADHD-тестер.

**Финал:** pre-publish 3.5 → загрузка билда в CWS Dashboard + заполнить листинг (все языки через `_locales` + FULL per-язык + `RATE_URL`/`STORE_URL`) → публикация → метаморфоза в продукт-ДНК.

⚠ **Открытые узлы к сборке:** когерентность язычка НОВОЙ рейс-сцене (узел 1) не переосмыслена; `<all_urls>` для язычка слетает при Remove→reinstall (включать тумблером).

Триггер новой сессии: «**работаем над sound-focus-timer — К ПУБЛИКАЦИИ. ГОТОВО: имя+бронь · мета 2.1 · графика 3.1 · права 3.6
(`lab/permissions.md`) · feel-before-ship пройден · переводы 53 локали NAME+SHORT (`extension/_locales/`, манифест `__MSG__`) + 8 топ
FULL (`lab/full/`); дверь локалей = POMODORO (не focus, выверено DFS/Wordstat, досье `lab/translations.md`). ЗА КЛОДОМ по приоритету:
1) finish-правка (`engine.js:274 extinguish` — быстрее в тишину, НЕ сделана) · 2) FULL хвост ~11 языков (образец `lab/full/de.md`) ·
3) права-развилка язычка required-vs-optional (после канона 5.5, см. DECISIONS 07-26). Дальше: pre-publish 3.5 → CWS Dashboard →
публикация. Метод: `app-studio/knowledge-base/module-3` (база знаний ПОЛНАЯ I–V). STATE + DECISIONS 07-26**».

# State — где мы сейчас

Последнее обновление: **2026-08-10** (ШАББАТ) · метаболизм **2026-08-17** (замер позиций в поиске CWS +
снимок карточки: 1.1.1 в сторе, 83 юзера, 0 отзывов). Этап: **ПРОДУКТ. 1.1.0 прошла модерацию и живёт в
сторе · реклама Яндекс идёт непрерывно с 28.07 · публичный стенд пересобран под новый звук и выложен ·
у продукта появилась страница в хабе.** В деве лежит 1.1.1 (просьба об оценке), билд не собран.

Store: `chromewebstore.google.com/detail/minimalist-timer/miknhphoakphfhgjajhkalmpdnadkeic`

🆕 **Снимок карточки 2026-08-17 (прочитан со страницы стора):** **Version 1.1.1** · Updated August 10, 2026 ·
**83 юзера** (было 49 на 08-06 → +34 за 11 дней ≈ 3/день) · **рейтинг 0.0, отзывов НЕТ**.
Значит 1.1.1 **уже в сторе** — строки ниже про «⬜ НЕ ЗАЛИТА» устарели; дата Updated осталась 08-10
(в тот же день аппрувилась 1.1.0), это единственное расхождение — уточнить у автора при касании.

## Первые цифры продукта (замер 08-06, 10 дней)

```
3 434 ₽  →  370 кликов  →  436 визитов листинга  →  49 установок
```

- **Конверсия листинга 11.3 %** при норме канона 15–30 % → листинг продаёт хуже нормы. Это измеренный факт
  и прямое основание работать над текстами и графикой.
- **Цена ПЛАТНОЙ установки 95 ₽** (реклама дала 36 из 49; делить расход на все 49 — ошибка).
- **82 % трафика платные**, органики за 10 дней — 9 человек. Кампания одна: `713122208`.
- **Россия 40 из 49.** Tier-1 — 4 (8 %). Вне России 9 установок, **6 из них органические** (Австралия,
  Эквадор, Грузия, Индонезия, Израиль, Корея).
- 🔶 Прайор ExportGPT: 8 дней рекламы подняли поток вчетверо и он держался месяц органикой — реклама
  покупает не установки, а место в выдаче. Tier-1 у ExportGPT 45 % и **все органические**: Яндекс Tier-1
  не приносит структурно → нужен второй канал (ФБ/Google).

**Слепое пятно:** внутри расширения нет ни одного события (по ДНК «слежка = v2»). Видно «сколько
поставили», не видно «летают ли». GA4-ресурс листинга (`547365719`) живёт в служебном аккаунте Google,
админа нет ни у кого → читается OAuth'ом от имени автора (`analytics/ga4_oauth.py`).

## Фокус инструмента (якорь) — не менялся
> **Фокус-таймер, чьё время сделано из тёплого звука. Фрейм: не «отсчёт, который терпишь», а РЕЙС, на который садишься** —
> закоммитил маршрут → тебя НЕСЁТ → прибытие. Ценность = обязательство + быть-несомым + прибытие (не счёт минут).

«таймер» = как НАХОДЯТ (SEO-дверь EN `minimalist timer` KD1; дверь ЛОКАЛЕЙ = **pomodoro**, выверено DFS/Wordstat) ·
«рейс» = как ЧУВСТВУЮТ. Ядро — ADHD-взрослые (`time blindness` 9.9k). Клин «звук=тело времени + мягкие
границы, без нейро-обещаний» свободен.

## Онтология и механика — не менялись
Звезда (одно тёплое присутствие, близится по прогрессу, НЕ персонаж) · полёт (звёзды несутся от центра) ·
воздух-космос вне времени суток · звук = воздух, тёплый brown. Панель = рейс на `<canvas>` во весь сайдбар.
Автомат 4 состояний · вход → плато → рассвет («таймер решил») / выдох («я решил») · принцип двух концов.
Завод = ПОЛЗУНОК (`5·15·25·45·90·∞`) · тап по небу = взлёт · энергия = скорость полёта.

## Что построено (MV3, версия 1.1.1)

- **Панель** `hearth.html/js` — рейс к звезде на `<canvas>`; **звук в offscreen** переживает закрытие панели.
- **`engine.js`** — brown-ядро + дирижёр фаз + arousal; **`tone.js`** — тоновый слой; **`icon.js`** — дуга
  остатка на иконке; **`glow.js`** — язычок-дверь; **`remote.js`** — контракт панели с домом звука.
- Права: `sidePanel storage offscreen scripting` + `optional_host` (язычок по кнопке — избегает in-depth review).
- Отжили и в zip НЕ входят: `orb.* sidepanel.* popup.* dev.html ui.html`.
- **Просьба об оценке** — плашка «Rate us» в углу дока + карточка поверх звезды (см. §1.1.1).
- **Сборка только скриптом — `bash lab/build-zip.sh dev|prod`.** Белый список внутри; падает, если файла
  нет на диске или в готовом архиве. Причина: `icon.js` и `tone.js` подключены МЯГКО — забытый файл не
  роняет расширение, а молча оставляет продукт без иконки и без тона. Закон режимов — ниже, §Закон сборки.

## ВЕРСИЯ 1.1.0 — В СТОРЕ с 08-10 (выложена 08-08, аппрув 08-10)

`dist/minimalist-timer-1.1.0.zip` · 125 файлов · 53 локали · 232 КБ. Не 1.0.2: новый слой звука и
переделанное управление — это функциональный релиз.

**1. Иконка-таймер** (`icon.js` + `paintAction` в `background.js`). Дуга оставшегося пути прямо на иконке
расширения: видно всегда, на любой вкладке, без единого впрыска в чужие страницы. Закон тот же, что у
звука — показываем оставшееся расстояние, а не утекающее время: без цифр, без красного, без ускорения к
финалу. Стенд `lab/icon-stand.html` грузит ТОТ ЖЕ файл.

**2. Тоновый слой** (`tone.js`, перенос из `lab/tone-stand.html`). Пласт из восьми голосов, присутствие
ведут пары волн с несоизмеримыми периодами; аккорд переселяется голос за голосом, один задерживается и
разрешается через 9–20 с; сверху мотивы, снизу ход, сбоку второй путник. Заморожено всё, кроме одной
ручки: характер «Стекло+» (срез 1500, пульс в середине пласта — правки автора по слуху 08-04), пентатоника.
- **Выход слоя идёт в `engine.master`**, а не в динамики: тон едет по тем же рельсам, что и шум — общая
  громкость, пауза, рассвет, keep-alive. Иначе на паузе шум замолкал бы, а тон играл.
- **Слой знает фазы рейса** (закрыт узел «бесконечность без прибытия»): ВХОДИТ в собирании · СТОИТ в ткани ·
  на паузе не копит события · **РАЗРЕШАЕТСЯ на рассвете** (мотивы, ход и путник прекращаются, аккорд
  возвращается на первое созвучие, верхние голоса уходят).
- **Ручка утончает шум, а не глушит:** `noiseTrim` (пол 0.34 — дорога остаётся) + `wallThin`. Громкость и
  кокон остаются за пользователем, ручка ими не распоряжается.
- **Дефолт — НОЛЬ для всех.** Продукт с первой секунды звучит тем, за чем пришли (весь листинг продаёт
  тёплый шум, главный боль-ключ `brown noise no ads`). Тон — то, что находят. Хранится в
  `chrome.storage.local` (в `sync` нельзя: квота 120 записей/мин, крутилка её убьёт), запись отложена 300 мс.

**3. Крутилки и симметричный док.** `makeKnob` в `hearth.js` — вертикальное перетаскивание + колесо +
стрелки. Порядок: полоса времени → **Volume** и **Tone** в линию (подписаны словом) → «Settings» →
**энергия** и **кокон** в линию (только значки). **Полоса осталась ТОЛЬКО у времени** — там форма несёт
смысл: длина рейса читается как расстояние. Полоса громкости под полосой времени была эклектикой.

**Приёмка (localhost, реальная панель, замер по спектру):** пласт поднимается и снимается по ручке ·
тональные пики до +32 дБ над соседними частотами против +2–7 дБ у чистого шума · увёл ручку в ноль и
вернул посреди рейса — жизнь слоя возвращается · на рассвете все таймеры сняты, слой разрешается · после
рейса пласт снят, контекст закрыт · на чистом профиле ручка 0, шум не подрезан, пласт не строится.

**ВЫЛОЖЕНА 08-08 → ✅ ПРОШЛА МОДЕРАЦИЮ (слово автора 08-10): 1.1.0 ЖИВЁТ В СТОРЕ.** Значит замер
конверсии листинга при неизменном кратком описании пошёл — база 11.3 %.
**Краткое описание уехало ПРЕЖНЕЕ — вариант C** (115 симв, несёт `pomodoro timer` + `aesthetic`);
D и E не взяты, развилка закрыта. **Тургенев прогнан, ок.** Значит после аппрува конверсия листинга
меряется при НЕИЗМЕННОМ кратком: всё, что сдвинется относительно 11.3 %, — заслуга полного описания
(боль-текст, 4817 симв) и графики, а не заголовочной строки. Чистый замер, не смешанный.

## ВЕРСИЯ 1.1.1 — СОБРАНА и УТВЕРЖДЕНА 08-10, ✅ В СТОРЕ (карточка отдаёт 1.1.1 на 08-17)

`dist/minimalist-timer-1.1.1.zip` · 125 файлов · 53 локали · 236 КБ · `?b=38`. Рядом лежат дев-пакет и
`unpacked-1.1.1-dev/`. **Заливка за автором** (VirusTotal → CWS Dashboard → Package). Всё содержание —
просьба об оценке, переделанная по паттерну ExportGPT: **две громкости одной просьбы**.

- **ПЛАШКА — дверь.** Ряд звёзд «Rate us» **под закрытым Settings, в правом нижнем углу, неброско**
  (`align-self: flex-end`, звёзды 15px, подпись 10.5px, прозрачность .62 → 1 под курсором). Видна
  **СРАЗУ, с первого открытия панели** (`BAR_AFTER = 0`): дверь не открывается по расписанию.
  Метафору рейса несёт ховер-подсказка, не подпись.
- **КАРТОЧКА — просьба.** «How was the flight?» поверх звезды по прибытии, с **3-го** зачтённого рейса
  (`ASK_AFTER = 3`), звёзды 25px. Пока висит — плашка скрыта: одна просьба на экране.
- **Обойти нельзя, но не заперто.** Вуаль `#askveil` над небом (z 3) и под доком (z 5): клик по небу
  карточку СБРАСЫВАЕТ и рейс НЕ поднимает; громкость, время и настройки всё это время в руках.
  Сброс живёт до следующего прибытия (флаг в памяти панели, не на диске — свежая панель не встречает
  человека просьбой). Видимое «Not now» делает то же, что клик мимо.
- **Навсегда.** Первый ответ звездой пишет `hearth.rated` → не спрашиваем больше никогда (≥4★ → отзывы
  CWS, 1–3★ → форма автора). Ключ прежний — кто уже оценил, спрошен не будет.
- **Зачтённый рейс** = ≥1 «минуты» несомого времени (`ASK_MIN_MIN`); тычок «послушать, что это» счётчик
  `hearth.sessions` не двигает. Тип конца не различаем (у ∞-рейса рассвета нет вообще).
- **Стенд цел:** карточка строится из JS, в разметке её нет → шим `hearth.rated='1'` на
  `focus-pages/flight/` по-прежнему гасит просьбу. Схема потока — `schemas/rate-ask-loop-2026-08-09.svg`.
- Проверено на живой панели 360×760 (чистый профиль · настоящие клики · реальный рейс до рассвета ·
  обе ветки звёзд): полный протокол приёмки — в DECISIONS 08-09/08-10. **Полевой прогон в самом
  расширении за автором** — на localhost работает локальный движок, в расширении `RemoteEngine`; путь
  тот же (`render(phase='off')`, на нём же живёт ратбар, который уже в сторе), но глазами не смотрели.

## Закон сборки (с 08-10): дев на тест → прод после слова автора

Заведён в ExportGPT, действует и здесь. У `lab/build-zip.sh` два режима:
- `bash lab/build-zip.sh dev` → `dist/minimalist-timer-<версия>-dev.zip` + **`dist/unpacked-<версия>-dev/`**.
  `load unpacked` берётся ОТТУДА, не из `extension/`: тестируется то, что уехало в архив, с белым
  списком и без `.DS_Store`. Дев несёт `version_name: <версия>-dev` (видно в `chrome://extensions`) и
  пересобирается свободно.
- `bash lab/build-zip.sh prod` → `dist/minimalist-timer-<версия>.zip`, `version_name` снят, молча не
  перезаписывается (`--force` осознанно).
- Пакуется из КОПИИ — рабочий манифест не правится. Перед упаковкой `rm -f` архива: `zip -r`
  дописывает в существующий, и файл, выпавший из пакета между сборками, тихо выживал бы в нём
  (пойман в ExportGPT 08-10). Сборка падает, если `version_name` не там, где должен быть.
- ⚠️ **Прод-пакет живёт ровно до следующей правки кода.** После неё он не «старый», а ЛОЖНЫЙ: 08-10
  промежуточный прод 1.1.1, собранный до плашки, пришлось удалить (в стор не уезжал, номер не сгорел).

## 🆕 Что сделано 08-08…08-10 — стенд, схемы, страница в хабе

**Публичный стенд пересобран и живёт:** `panarini.github.io/focus-pages/flight/`. Был слепком от
26 июля — без тонового слоя, с русскими строками при английском посте. Теперь `engine.js` · `tone.js` ·
`remote.js` · `hearth.js` копируются из `extension/` **байт в байт**, руками живёт только `index.html`.
Шим ровно один: на стенде гасится просьба об оценке (в панели звать в отзывы честно, на стенде перед
нами прохожий из поста). Приёмка по спектру: ручка 0.7 → 17 устойчивых тональных пиков, `noiseTrim`
0.64; ручка 0 → 1 пик, всё вернулось в 1. AudioContext на свежей загрузке не создаётся.

**Три инструмента в `lab/`, все воспроизводимы:**
- `gen_tone_schema.py` — схема генерации тона; кривые считаются из формул `tone.js`, не рисуются;
- `render_schema.py` — SVG → PNG нужного размера и SVG с зафиксированной темой (`--svg`);
- `gen_timeline.py` — хронология внешности из гита: разворачивает дерево каждого коммита, тронувшего
  видимую поверхность, и снимает панель headless-Chrome. Одиннадцать кадров с 10 июля;
- `gen_icon_strip.py` — полоска настоящих состояний иконки тем же `icon.js`, что в service worker.

**Страница в хабе:** `panarini.github.io/projects/sound-focus.html` — хронология (Tide → очаг →
пламя → небо тёплое → небо холодное → две ручки), живой стенд в iframe, схема звука, схема сегментов,
полоска иконки, лог решений на 8 записей.

**Археология вскрыла то, чего не помнил никто:** 10 июля продукт назывался **Tide** и был синим
циферблатом с вкладкой «премиум»; 26 июля палитра менялась **дважды за день** — в 03:08 небо было
ещё янтарным, в 19:34 стало холодным.

⚠️ **Замер, которого нет в коде:** комментарий в `tone.js` утверждает, что два держащих голоса
«не пропадают». Арифметика даёт им **18 % тишины** (внешние — 53 %, все восемь одновременно —
0.024 %). Слышимо утверждение верно, но держит его не порог, а шестисекундный хвост эха.
Место факта — `SOUND.md`; на схеме его нет сознательно (схема показывает процесс).

## Листинг и SEO

### 🆕 ЗАМЕР ПОЗИЦИЙ В ПОИСКЕ CWS (08-17) — дверь работает, головные запросы закрыты
Данные и метод: `lab/serp-cws-2026-08-17.csv` + `.md`. Повод: автор искал по `focus timer` /
`beautiful timer` / `timer for focus` и не нашёл себя.
- **Стоим:** `brown noise no ads` **#1** · `brown noise timer` **#2** · `minimalist timer` **#3**
  (и в EN, и в RU) · `aesthetic timer` **#4** · `minimalist pomodoro timer` **#4** ·
  `brown noise` **#7** · `коричневый шум` **#3** · `минималистичный таймер` **#8**.
- **Вне десятки:** `focus timer` · `timer for focus` · `beautiful timer` · `timer for study` ·
  `study timer` · `pomodoro timer` · `focus sounds` · `таймер` · `помодоро таймер` · `таймер для фокуса`.
- ⭐ **Механика: выдача CWS = ровно 10 карточек, «глубины» НЕ существует** (проверено скроллом +
  «More» до застоя). Узкий запрос отдаёт меньше десяти, больше — никогда. Значит «не нашёл даже
  глубоко» = «не в десятке», и низких позиций как утешения не бывает.
- ⭐ **Имя решает:** все 10 в топе `focus timer` несут `focus timer`/`pomodoro` в ИМЕНИ. Совпадение
  по краткому/полному в десятку не выводит — RU-контроль: `таймер для фокуса` вне десятки, хотя
  RU-краткое несёт «для фокуса и учёбы»; `помодоро таймер` вне десятки, хотя RU-имя с него начинается.
- Вывод: SEO попал ровно туда, куда целились (`minimalist timer` KD1 + боль-ключи brown noise).
  Головные запросы не «провалены» — они не были дверью. Ограничители роста сейчас **не текст**, а
  канон `4.1:28` (органика приходит за 1–2 мес, полная сила SEO — ~6 мес; мы в сторе ~3 недели) и
  канон `4.6`/`4.7` (**рейтинг и отзывы — огромная роль в ранжировании, у нас 0 отзывов**).
- ⭐ 🆕 **ГУГЛ-ПРОВЕРКА КЛЮЧЕЙ (канон `2.2:11`, DFS, 08-17) — боль-ключи оказались активом ВЕБАППА,
  не листинга.** Возражение автора («`brown noise no ads` в CWS никто не вводит») проверено и верно
  вдвойне. Топ-10 Google по `brown noise no ads` = 7 роликов YouTube + Amazon Music + YouTube Music +
  `brownnoise.com`; по `minimalist timer` = `flocus.com`, `flipclock.app`, Google Play, App Store,
  физические таймеры, `pomodorotimer.online`. **Страниц CWS в обеих выдачах — НОЛЬ.** То есть гугл-трафик
  до карточки не доходит ни по боль-ключам, ни по нашей двери; расширение находят ТОЛЬКО внутренним
  поиском стора (10 слотов, вес на имени + отзывах + возрасте). Собранные объёмы (2900 · 2400 · 1600)
  окупаются на **вебаппе** — в обеих выдачах стоят веб-страницы, и место там свободно для страницы,
  а не для расширения. Это поднимает вебапп из «закрывает четыре линии» в главный SEO-ход.
- ⬜ **Открытая развилка (за автором): расширять ли ИМЯ** в сторону `focus timer` (поле 75 симв,
  занято 16). Цена: пересборка zip + Тургенев + **загрязнение чистого замера конверсии листинга**
  (база 11.3 % идёт при неизменном кратком). Плюс канон `2.2:11` — функция должна отвечать ключу
  из имени. Не решать походя.

**Полное описание (EN) — доведено 08-07.** Боль-текст 08-04 лежал только в черновике и никуда не доезжал;
теперь буллет + абзац «Most people get their brown noise from a streaming tab…» перенесены в
`lab/full/en.md`, локали пересобраны. EN-полное 4364 → **4817 симв**. Боль-текст несут **9 локалей из 53**
(en + 8 транслит на EN-fallback); 44 нативных остались на тексте 07-27 — названный пробел, не поломка.
- **Правка по канону:** FAQ-пункт «Why not just play brown noise in a streaming tab?» СНЯТ — почти дословно
  повторял абзац, одна мысль звучала трижды. По плотности канон это пропускал (`ads` 0.67 % при потолке 3 %):
  резали за повтор смысла, а не за спам. Своих ключей пункт не нёс.
- Сейчас: `ads` 4× · 0.48 % · `brown noise` 8× · 1.90 %. ⚠️ Ближе всех к красной зоне **`timer` — 25× ·
  2.97 %** при потолке 3 % (текст стал короче, доля выросла). Флагнет Тургенев — менять одно вхождение на
  синоним, не вычёркивать.
- ⚠️ **Ловушка черновика:** в `listing-draft.md` первой строкой FULL стоит `⭐ Minimalist Timer — …`, а
  решением 07-27 эта строка из FULL УБРАНА (дублировала Title). При синхронизации не возвращать.

**Открыто — краткое описание** (едет ИЗ АРХИВА, смена = пересборка zip + Тургенев):
- **C** (в архиве, 115 симв) — несёт `pomodoro timer` + `aesthetic`.
- **D** (черновик 08-04, 122) — берёт `no ads`, теряет ОБА.
- **E** (предложен Клодом, 116) — `A clean, aesthetic minimalist timer for focus and study — warm brown
  noise and focus sounds, no ads, on your device.` — теряет только `pomodoro` (в EN дверь `minimalist
  timer` KD1, а pomodoro — дверь локалей, не английской выдачи).

**Ключи-боли** (`lab/meta-keywords.md`, разведка `app-studio/research/focus_sound_demand_en_2026-08-04`):
`brown noise no ads` 2900 · `brown noise black screen` 2400 · `deep brown noise` 1600. Сезон учебной двери —
пик сентябрь–март, дно июнь–июль (×3.3).

**Графика** — пересобрана 08-02 ригом `lab/graphics/shots/` (панель в кадре = реальный `hearth.html` в
iframe, уведённый в настоящий рейс). 4 кадра 1280×800. **За автором: залить кадры в листинг.**

**Локализация — 53 языка, воспроизводимо.** `lab/locales-data.json` + `lab/full/*.md` →
`lab/build-locales.py` → `_locales/<code>/messages.json` (NAME + SHORT + `storeDesc`). `storeDesc` Chrome
не читает — он для скрипта массовой загрузки; автор кладёт FULL руками.

## Реклама и аналитика

**Яндекс ЕПК** (запущена 07-28, идёт непрерывно). Поиск-only · все страны · Максимум кликов + низкая ручная
ставка · мобильные −100 %. Факт на 08-04: CPC **9.28 ₽** (прайор ExportGPT 5.66 — реальность ×1.64), CTR
**10.33 %** (у ExportGPT 4.39 %). Два узла: **конверсии = 0 у обеих кампаний** (цели не настроены, Директ
крутится вслепую) и **бюджет не ограничитель** (~430 ₽/день при лимите 60 000 ₽/нед; потолок — число
запросов, рычаг в ширине ключей, не в деньгах). Язык боли для текстов → `app-studio/research/ad_pain_language_ru_2026-08-01`.

**Аналитика.** Umami на welcome ЖИВ (real-time установки) · GA4 листинга (`G-HNVT5MM6Y4`) · Clarity снят
решением автора 08-04. Пульс: `python3 app-studio/analytics/pulse.py`. У CWS программного доступа к
установкам НЕТ — дашборд читается глазами.
- ⚠️ **`focus-pages`: тег GA4 `G-X9WZSNCLXF` + событие `install_welcome` запушены, но НЕ живые.** Сайт
  работает, Umami считает; не доехало только дописанное после 29 июля — сборка Pages не обновлялась
  (`last-modified: 29 Jul`). Не срочно, третий замер просто отсутствует.

⚠️ **Политика разошлась с реальностью (найдено 08-09 на разборе ExportGPT).** `focus-pages/privacy/`
  обещает про наши веб-страницы «privacy-friendly, cookieless analytics that counts visits only» — а на
  welcome стоят **GA4 (с куками)** и **Clarity (запись поведения)**. Umami под это описание подходит,
  остальные два — нет. Плюс Clarity решением автора 04-08 должен был быть снят, а тег на месте.
  Лечение на выбор: снять Clarity и тег GA4 с welcome (тогда фраза станет правдой) ИЛИ переписать
  абзац политики под факт. Расширение это не трогает — оно по-прежнему не шлёт ничего, и «will not
  collect» в карточке стора остаётся правдой.

**Reddit → ВЕБ-РЕЙС, не листинг** (канон 5.4: на Reddit веб > расширение, install = трение).
Прогрев: аккаунт 5 мес, ≥20 комментов за 2–3 нед без ссылок, потом пост.

### 🆕 Публичный стенд пересобран под 1.1.0 (08-08)
`focus-pages/flight/` был разовым слепком от 26 июля — без тонового слоя, с русскими строками при
английском посте. Пересобран: `engine.js` · `tone.js` · `remote.js` · `hearth.js` скопированы из
`extension/` **байт в байт**, руками живёт только `index.html` (= `hearth.html` + шапка + один шим).
**Закон стенда вперёд: логику не форкать.** Стенд протух ровно потому, что был слепком, а не копией.
- **Шим ровно один:** на стенде гасится просьба об оценке (`hearth.rated='1'` до загрузки `hearth.js`).
  В панели звать в отзывы CWS честно — человек поставил; на стенде перед нами прохожий из поста,
  он ничего не ставил. Раньше вопрос не стоял: в слепке 26.07 `RATE_URL` был пуст, его вписали 08-01.
- **Приёмка живьём** (localhost, реальная страница, спектр через `AnalyserNode` на `engine.master`):
  ручка 0.7 → **17 устойчивых тональных пиков** (296·517·635·775·947·1262·1305 Гц), `noiseTrim` 0.64 —
  шум утончён, не заглушен · ручка 0 → **1 пик**, `noiseTrim` и `wallThin` вернулись в 1 · на свежей
  загрузке AudioContext вообще не создан (только по касанию), `mix` 0, консоль чиста · мобильный
  375×812 без переполнения (панель рисовалась под узкий сайдбар — телефон ей родной).
- **Не в мире, пока не запушено:** Pages отдаёт `PanarinI/focus-pages`, изменения лежат локально.
- ⚠️ **Стенд снова отстал (08-10):** `hearth.js`/`hearth.html` изменились под 1.1.1 (плашка + карточка),
  а на стенде лежат копии от 08-08. Ничего не сломано — шим `hearth.rated='1'` гасит просьбу, и CSS
  карточки стенду не нужен, — но закон копии нарушен. Пересобрать при следующем касании стенда
  (`engine.js` · `tone.js` · `remote.js` · `hearth.js` — байт в байт из `extension/`).

## Открытые узлы

- **Три носителя присутствия** — сайдбар, иконка в тулбаре, впрыск в страницу. Автор назвал это
  избыточностью. Ставка Клода: иконка закрыла присутствие, у язычка осталась одна работа — дверь обратно;
  держать выключенным по умолчанию, право `optional_host` не снимать (почва под аффилиатную монетизацию,
  канон 5.3). **Решение за автором.**
- **Счётчики поведения внутри экста** — автор спросил 08-07, решили НЕ тащить в 1.1.0. Цена: галочка
  «User activity» в Privacy tab + абзац в политику + возможен долгий ревью. Делать отдельной версией.
- **Маршрут в сцене** — построен и отклонён автором; `lab/route.js` + `lab/route-stand.html` запаркованы.
- Reinstall-на-uninstall (`STORE_URL`) не проверялся · счётчик сессий занижен при закрытой панели
  (`dropEmber` живёт в панели, перенос в `chrome.storage` — отдельный шаг) · звук в наушниках не тестирован ·
  процессор на 90-минутной сессии не мерян · журнал рейсов (накопление) не построен.
- Чип: проверить privacy-соответствие `save-chatgpt-as-pdf` (счётчики в расширении vs декларация).

## Следующий шаг

1. **ОТЗЫВЫ: канонный путь `4.7` ЗАКРЫТ волей автора (08-17)** — «знакомых просить не хочу».
   На 08-17 у карточки **0 отзывов, рейтинг 0.0** при 83 юзерах; просьба об оценке (1.1.1) в сторе
   ~неделю и отзывов не принесла. Покупка отзывов НЕ рассматривается: политика CWS + канон `4.7`
   («Chrome эффективно борется с ботоводными расширениями») + один аккаунт разработчика держит и
   ExportGPT (реш. 07-26, DECISIONS:637) → страйк бьёт по зарабатывающему продукту, не по этому.
   Остаётся законный платный путь — **незнакомцы за настоящее использование, не за оценку**:
   Userbrain ~$30/тест (см. п.3) + `focus-pages/uninstall/` (канон `5.1`: форму заполняют 5–15 %
   удаливших — голоса без просьбы). Следствие: **рейтинговый буст приходит с масштабом, а не раньше**
   (🔶 при 83 юзерах даже рабочая просьба даёт единицы отзывов) → ближний рычаг = установки
   (второй рекламный канал + вебапп), а не текст листинга.
2. ~~Залить 1.1.1~~ — **сделано, в сторе** (карточка отдаёт 1.1.1). Осталось: **полевой прогон просьбы
   об оценке в самом расширении** (на localhost движок локальный, в расширении `RemoteEngine`; глазами
   не смотрели). Краткое описание НЕ меняем — вариант C, замер конверсии (база 11.3 %) идёт при неизменном.
3. **ЖИВЫЕ ТЕСТ-ЮЗЕРЫ — главный приоритет и по-прежнему не сделано.** Userbrain ~$30/тест (think-aloud
   видео, ноль соц-просьбы — фит под психологию автора: платить = убрать просьбу). Голоса → завести `ГОЛОСА.md`.
4. **Вебапп** (звезда в центре) — закрывает четыре линии разом: Reddit по канону 5.4, SEO на боль-ключи с
   KD 2–4 (стор в гугле не индексируется, страница — да), мобильных, «поделиться стендом».
5. **Второй рекламный канал** (ФБ/Google) — Яндекс Tier-1 не приносит структурно.

Триггер новой сессии: «**sound-focus-timer: в сторе 1.1.1 — просьба об оценке в две громкости (тихая
плашка «Rate us» в углу дока, видна сразу + карточка «How was the flight?» поверх звезды с 3-го
зачтённого рейса; клик мимо сбрасывает до следующего прибытия, ответ звездой закрывает навсегда:
≥4★ → отзывы CWS, 1–3★ → форма). Карточка на 08-17: **83 юзера, рейтинг 0.0, отзывов НЕТ**.
Замер позиций 08-17 (`lab/serp-cws-2026-08-17.csv`): дверь взята — `brown noise no ads` #1,
`brown noise timer` #2, `minimalist timer` #3, `aesthetic timer` #4, `коричневый шум` #3; головные
`focus timer`/`study timer`/`pomodoro timer` вне десятки. **Выдача CWS = ровно 10 карточек, глубины
нет; имя решает.** Значит текст свою работу сделал, упор теперь в возраст (в сторе ~3 недели, канон
4.1:28) и в НОЛЬ ОТЗЫВОВ (канон 4.6/4.7). Закон сборки: дев-пакет → живой тест автора → его слово →
прод (`bash lab/build-zip.sh dev|prod`); прод-пакет после любой правки кода становится ЛОЖНЫМ, а не
старым. Конверсия листинга: база 11.3 % при норме 15–30 %, платная установка 95 ₽. ДАЛЬШЕ:
(1) первичные отзывы 4–5 знакомых (канон 4.7); (2) полевой прогон просьбы в самом расширении;
(3) живые тест-юзеры (Userbrain); (4) вебапп. Открыто: расширять ли ИМЯ в сторону `focus timer`
(загрязнит замер конверсии) — за автором. Читать: CLAUDE.md → STATE → DECISIONS 08-09/08-10 → LAUNCH.md**».

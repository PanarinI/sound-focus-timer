# State — где мы сейчас

Последнее обновление: **2026-07-29** (ШАББАТ — сессия продвижения). Этап: **ОПУБЛИКОВАН в CWS (веха 1 ✅) · РЕКЛАМА ЯНДЕКС (ЕПК) ЗАПУЩЕНА 07-28, учится** (0 показов на старте — норма ЕПК). Аналитика поднята.
Холодный старт: платно набираем первые 100–300 установок из ранжир-ямы. Store: `chromewebstore.google.com/detail/minimalist-timer/miknhphoakphfhgjajhkalmpdnadkeic`. (07-27 засабмичен → аппрув → метаморфоза «стройка → продукт».)

**Что ушло на модерацию:** билд `dist/minimalist-timer-1.0.0.zip` (v1.0.0, VirusTotal чист) · листинг на **53 языках** (имя+краткое+полное) · графика (иконка-искра 128 · скриншоты 1280×800 · промо 440×280) · Privacy tab заполнен · категория **Workflow & Planning** · privacy policy `panarini.github.io/focus-pages/privacy/`.

## Фокус инструмента (якорь) — не менялся
> **Фокус-таймер, чьё время сделано из тёплого звука. Фрейм: не «отсчёт, который терпишь», а РЕЙС, на который садишься** —
> закоммитил маршрут → тебя НЕСЁТ → прибытие. Ценность = обязательство + быть-несомым + прибытие (не счёт минут).

«таймер» = как НАХОДЯТ (SEO-дверь EN `minimalist timer` KD1; дверь ЛОКАЛЕЙ = **pomodoro**, выверено DFS/Wordstat) · «рейс» = как ЧУВСТВУЮТ.
Ядро — ADHD-взрослые (`time blindness` 9.9k). Клин «звук=тело времени + мягкие границы, без нейро-обещаний» свободен.

## Онтология — РЕЙС К ЗВЕЗДЕ (история → DECISIONS 07-23…25) — не менялась
Звезда (одно тёплое присутствие, приближается по прогрессу, НЕ персонаж) · Полёт (искры от центра = летим; покой=нет искр) ·
Воздух-космос (вне времени суток) · Звук = воздух, тёплый brown, без потрескиваний. Панель = рейс на `<canvas>` во весь сайдбар.

## Механика — MECHANICS v1.1 — не менялась
Автомат 4 состояния · вход → плато → рассвет («таймер решил») / выдох («я решил») · принцип двух концов. Завод = ПОЛЗУНОК
(`5·15·25·45·90·∞`) · тап по звезде = взлёт · энергия = скорость полёта.
**finish-правка СДЕЛАНА 07-27:** ручной стоп = почти мгновенный fade (`hearth.js` `QUENCH=0.4`, было 4с) — «сам нажал → в плавности нет смысла»; авто-рассвет не тронут.

## Что построено (экст MV3, v1.0.0)
- **Панель = рейс к звезде** (`hearth.html/js`, `<canvas id=sky>` + `ResizeObserver`) · **звук в offscreen** переживает закрытие панели, ✅ в расширении играет · **язычок-дверь** (`glow.js`, `scripting`-инъекция) · оценка (со 2-й сессии) · welcome/uninstall.
- Права: `sidePanel storage offscreen scripting` + `optional_host` (язычок по кнопке — избегает in-depth review, host justification не спрашивают).
- Отжили: `orb.* sidepanel.* popup.* dev.html ui.html` (в zip НЕ входят — билд собирается whitelist'ом).

## Локализация — 53 языка, воспроизводимо
- Пайплайн: `lab/locales-data.json` → `lab/build-locales.py` → `extension/_locales/<code>/messages.json` (NAME+SHORT+**storeDesc**).
- **FULL**: 44 нативных языка (`lab/full/*.md`) + `en.md` + 8 транслит на **EN-fallback** (am bn gu kn ml mr ta te — «инструмент=явь», принцип 07-26).
- **storeDesc** в `_locales` — для СКРИПТА массовой загрузки (`lab/cws-upload-full.js`, канон 3.6); Chrome его не читает. `build-locales.py` кладёт родной FULL или en-fallback.
- Строка-заголовок `⭐ Имя — tagline` из FULL **убрана** (дублировала Title; см. DECISIONS 07-27).

## Служебные страницы — REBRAND сделан 07-27
`focus-pages` (публичный репо, github-pages): welcome/uninstall/**privacy** переписаны из старого «Ember/очаг» в **Minimalist Timer + синий/звезда**, задеплоены. Контакт-email продукта = `callmestewie90@gmail.com`.

## Метаморфоза: СТРОЙКА → ПРОДУКТ (начата 07-27)
Форма «стройка» закрывается сабмитом. При аппруве организм линяет в продукт-ДНК; `LAUNCH.md` (чеклист публикации) → архив.

## Следующий горизонт — PRODUCT-этап

**1. Яндекс (ЕПК) — запущена 07-28, учится.** Тип — **Единая перформанс-кампания** (классические «текстово-графические» Яндекс отменил ~30.06.2026 → ЕПК единственный тип; 0 показов на старте = норма, авто-разгон). Внутри: Поиск-only · все страны · Максимум кликов + низкая ручная ставка (~5 ₽) · большой дневной бюджет · мобильные −100 % · «Директ помогает» off. Ключи — помодоро + **эстетичная дверь** (`красивый`1001+`эстетичный`830 ≈ pomodoro в рунете) + автотаргетинг (2 базовые). Прайор ExportGPT: CPC 5.66 ₽, ~$115. Отключить при первой органике (1–2 мес, 4.1:24), не >$150. Настройки/кит → `app-studio/knowledge-base/module-4/4.3` (детальные настройки Директа+ЕПК досыпаны 07-28).
   - **Следом ФБ** (2-й канал, кит НЕ собран) → сравнить Яндекс↔ФБ (поиск-намерение vs лента-импульс).

**2. Аналитика поднята (метки → счётчики).** UTM на кампании (`Параметры URL`) → GA4-**листинга** (opt-in `G-HNVT5MM6Y4`, CWS сам тегирует, ждёт трафик) · **Umami** на welcome ЖИВ (`7999549b…`, real-time установки) · **Clarity** (`xto0omit59`) код починен, **но ждёт деплоя `focus-pages`** (был баг: закоммитили закомментированным). 3 источника установок: CWS-дашборд · GA4 · Umami. Читать `install/first_visit` (норма 15–30 %), вычет орг-бейзлайна (4.5).

**3. Reddit → ВЕБ-РЕЙС, не листинг (стратегия сессии).** Метод (видео Капитана, конспект → `knowledge-base/module-5/5.4`): на Reddit **веб > расширение** (человек не искал → install = трение). Веб-рейс уже есть — `focus-pages/flight/` («Рейс к звезде» как страница). ⚠ **flight/ слегка устарел** (hearth.js 07-26 vs экст 07-27, не доехал `QUENCH=0.4`) + **вопрос standalone** (звук в самой вкладке, не через offscreen). Архитектура (модуль 2.6): вебсервис-рейс = ядро (пиарим в Reddit), CWS-экст = лаунчер, открывающий его → под стор-траст+Яндекс. Прогрев Reddit: аккаунт **5 мес** (актив), **≥20 комментов за 2–3 нед** без ссылок → потом пост. Это оживший **продуктовый контур Герольда** (Reddit/PH/Store).

**Открытый узел (аппрув-остаток):** вписан ли `STORE_URL`/`RATE_URL` в код (rate-бар ≥4★ · reinstall на uninstall) → пересобрать zip → апдейт. За автором.

**ГЛАВНЫЙ приоритет — ЖИВЫЕ ТЕСТ-ЮЗЕРЫ + наблюдение** (автор готов платить). Разведано в сессии: **платные usability-платформы** (Userbrain ~$30/тест — think-aloud видео, ноль соц-просьбы; фит под психологию автора — платить = убрать просьбу) · Reddit-сообщества · «на шаг от друзей» (реферал, не повтор). Голоса → завести `ГОЛОСА.md`. **Разворачивать свежей сессией.**

**Дальше по методу (product):** реклама + **UTM-метки** (атрибуция источников — на рекламном этапе, не раньше) · аналитика поведения = **v2** (по DNA «слежка — не MVP»; таймер на старте private = отстройка, шину событий закладываем) · веха 2 = 2000 юзеров (порог разговора о монетизации: платным станет ДОСТУП целиком).

**Отложенные узлы (публикацию не блокировали):** язычок-когерентность рейс-сцене (нить тёплого света вместо жар-плашки) · звук-верстак (`SOUND.md`) · журнал рейсов (накопление) · тест звука в наушниках.

**Открытый хвост:** чип «проверить privacy-соответствие `save-chatgpt-as-pdf`» (счётчики в расширении vs декларация — отдельная сессия).

Триггер новой сессии: «**sound-focus-timer ОПУБЛИКОВАН в CWS, реклама Яндекс (ЕПК) учится, аналитика поднята (GA4-листинг + Umami живы; Clarity код готов — ЖДЁТ деплоя `focus-pages`).
ДАЛЬШЕ по продвижению: (1) задеплоить Clarity + следить за установками → отключить рекламу при органике; (2) Reddit → вести на ВЕБ-РЕЙС `focus-pages/flight/` (сперва обновить hearth.js до 07-27 + проверить standalone-звук), прогрев аккаунта ≥20 комментов/2–3 нед; (3) ФБ-кит (2-й канал) → сравнить; (4) ГЛАВНОЕ — живые тест-юзеры (платные usability-платформы, автор готов платить) свежей сессией.
Метод: `app-studio/knowledge-base/module-4` (реклама/ЕПК) + `module-5/5.4` (Reddit — видео-конспект). STATE + DECISIONS 07-29**».

# State — где мы сейчас

Последнее обновление: **2026-08-04** (метаболизм по ходу сессии). Этап: **ОПУБЛИКОВАН в CWS (веха 1 ✅) · РЕКЛАМА ЯНДЕКС (ЕПК) идёт несколько дней · 1.0.1 ЗАЛИТ** (слово автора 08-04 — 6 прод-фиксов уехали в стор). Аналитика поднята.
Холодный старт: платно набираем первые 100–300 установок из ранжир-ямы. Store: `chromewebstore.google.com/detail/minimalist-timer/miknhphoakphfhgjajhkalmpdnadkeic`. (07-27 засабмичен → аппрув → метаморфоза «стройка → продукт».)

**Что ушло на модерацию:** билд `dist/minimalist-timer-1.0.0.zip` (v1.0.0, VirusTotal чист) · листинг на **53 языках** (имя+краткое+полное) · графика (иконка-искра 128 · скриншоты 1280×800 · промо 440×280) · Privacy tab заполнен · категория **Workflow & Planning** · privacy policy `panarini.github.io/focus-pages/privacy/`.

## Фокус инструмента (якорь) — не менялся
> **Фокус-таймер, чьё время сделано из тёплого звука. Фрейм: не «отсчёт, который терпишь», а РЕЙС, на который садишься** —
> закоммитил маршрут → тебя НЕСЁТ → прибытие. Ценность = обязательство + быть-несомым + прибытие (не счёт минут).

«таймер» = как НАХОДЯТ (SEO-дверь EN `minimalist timer` KD1; дверь ЛОКАЛЕЙ = **pomodoro**, выверено DFS/Wordstat) · «рейс» = как ЧУВСТВУЮТ.
Ядро — ADHD-взрослые (`time blindness` 9.9k). Клин «звук=тело времени + мягкие границы, без нейро-обещаний» свободен.

## Онтология — РЕЙС К ЗВЕЗДЕ (история → DECISIONS 07-23…25) — не менялась
Звезда (одно тёплое присутствие, приближается по прогрессу, НЕ персонаж) · Полёт (искры от центра = летим; покой=нет искр) ·
Воздух-космос (вне времени суток) · Звук = воздух, тёплый brown, без потрескиваний. Панель = рейс на `<canvas>` во весь сайдбар.

## Механика — MECHANICS v1.1 — не менялась
Автомат 4 состояния · вход → плато → рассвет («таймер решил») / выдох («я решил») · принцип двух концов. Завод = ПОЛЗУНОК
(`5·15·25·45·90·∞`) · тап по звезде = взлёт · энергия = скорость полёта.
**finish-правка СДЕЛАНА 07-27:** ручной стоп = почти мгновенный fade (`hearth.js` `QUENCH=0.4`, было 4с) — «сам нажал → в плавности нет смысла»; авто-рассвет не тронут.

## Что построено (экст MV3, v1.0.0)
- **Панель = рейс к звезде** (`hearth.html/js`, `<canvas id=sky>` + `ResizeObserver`) · **звук в offscreen** переживает закрытие панели, ✅ в расширении играет · **язычок-дверь** (`glow.js`, `scripting`-инъекция) · оценка (со 2-й сессии) · welcome/uninstall.
- Права: `sidePanel storage offscreen scripting` + `optional_host` (язычок по кнопке — избегает in-depth review, host justification не спрашивают).
- Отжили: `orb.* sidepanel.* popup.* dev.html ui.html` (в zip НЕ входят — билд собирается whitelist'ом).

## Волна прод-фиксов → 1.0.1 (08-01, на живом тесте) — ЗАЛИТА в стор (08-04)
6 багов (часть давние в 1.0.0), исправлены и уехали в стор. «Почему» → DECISIONS 08-01.
- **КРИТ — звук пропадал при vol=0:** Chrome гасит offscreen-документ за ТИШИНОЙ (наблюдалось 07-22 🔶, доказано симптомом автора) → умирал движок+сессия. Лечение **floor keep-alive** — пока рейс идёт (phase≠off), master держит неслышимый пол ~0.0015 (−80 дБ) → дом «играет», Chrome не гасит (`engine.js` `_apply`).
- **Конец таймера при свёрнутой панели** оставлял звук играть (фаза «ручей»): `turnOff` звала только панель → теперь движок ГАСИТ САМ (`engine.js` natural-end).
- **Рассинхрон панели при развороте** (Chrome то хранит DOM side-panel, то перезагружает): призрак кнопки Finish + прыжок ползунка громкости → `visibilitychange`→`sync` + **персист громкости/energy/cocoon** (`hearth.js`).
- **Язычок:** toggle с 1-й попытки (`syncGlow` не верит кэшу `glowLit` на выкл) · **потухший в покое** (`active`-флаг) · закрытие панели через реконнект порта (рвался при перезапуске SW).
- **Rate:** вписан боевой `RATE_URL` (≥4★ → отзывы CWS; был пустой → уходило в форму) + текст-метка. 🔶 узел: счётчик сессий занижен при закрытой панели (dropEmber в панели) — перенос в `chrome.storage` отдельным шагом.
- **Звезда (вкус):** финиш 166px (был раздут до 300), размер линейный, дыхание спокойное 7с (ускорение читалось как «сердце» — убрано), сдувание плавное за рассвет/выдох, входы ease-out.
Версии: в СТОРЕ **1.0.1** (не 1.0.2 — перескок исправлен). Cache-bust `?b=24`.

## Графика листинга — ПЕРЕСОБРАНА 08-02 (кадры вместо настроения)
Жалоба автора: старый `graphics/banner-final.png` даёт ощущение, но не даёт ПОНИМАНИЯ (мелкий текст, механики не видно).
Рождён риг `lab/graphics/shots/` (`build-shots.py` + README): **панель в кадре — реальный `hearth.html`**, поднятый в iframe
и уведённый в НАСТОЯЩИЙ рейс (`fast`: 1 мин = 1 с), снимок headless-Chrome при виртуальном времени → размер звезды честный
(«инструмент = явь»). Волна на кадре 5 — та же формула `engine.js fillBrown`.
**Правка автора в тот же день (сильнее первой сборки): это НЕ инструкция.** Первый заход вышел пошаговой методичкой
(«шаг 1 — выбери, шаг 2 — тапни») и водянистым. Кадр несёт ОДНУ мысль о сути, панель рядом просто живёт и ненавязчиво
показывает интерфейс. Императивов нет вовсе (на десктопе «tap» ещё и неверен — «click»).
**4 кадра 1280×800:** ① что это — таймер из **звука** (ударение на `sound`) · ② время, которое не считают
(врезка minute 1 → minute 24) · ③ ничего не блокируется и не наказывается · ④ конец как рассвет (реальная фаза `DAWN=120с`).
Убрано: кадр с волной шума (лишний), плашки STEP, лупа, выноски «tap anywhere». Пересборка: `python3 build-shots.py`.
**За автором:** залить кадры в листинг + (опция) 20-сек экранная запись рейса на YouTube — CWS принимает только ссылку (сценарий в README).

## Локализация — 53 языка, воспроизводимо
- Пайплайн: `lab/locales-data.json` → `lab/build-locales.py` → `extension/_locales/<code>/messages.json` (NAME+SHORT+**storeDesc**).
- **FULL**: 44 нативных языка (`lab/full/*.md`) + `en.md` + 8 транслит на **EN-fallback** (am bn gu kn ml mr ta te — «инструмент=явь», принцип 07-26).
- **storeDesc** в `_locales` — для СКРИПТА массовой загрузки (`lab/cws-upload-full.js`, канон 3.6); Chrome его не читает. `build-locales.py` кладёт родной FULL или en-fallback.
- Строка-заголовок `⭐ Имя — tagline` из FULL **убрана** (дублировала Title; см. DECISIONS 07-27).

## Служебные страницы — REBRAND сделан 07-27
`focus-pages` (публичный репо, github-pages): welcome/uninstall/**privacy** переписаны из старого «Ember/очаг» в **Minimalist Timer + синий/звезда**, задеплоены. Контакт-email продукта = `callmestewie90@gmail.com`.

## Метаморфоза: СТРОЙКА → ПРОДУКТ (начата 07-27)
Форма «стройка» закрывается сабмитом. При аппруве организм линяет в продукт-ДНК; `LAUNCH.md` (чеклист публикации) → архив.

## Следующий горизонт — PRODUCT-этап

**1. Яндекс (ЕПК) — запущена 07-28, учится.** Тип — **Единая перформанс-кампания** (классические «текстово-графические» Яндекс отменил ~30.06.2026 → ЕПК единственный тип; 0 показов на старте = норма, авто-разгон). Внутри: Поиск-only · все страны · Максимум кликов + низкая ручная ставка (~5 ₽) · большой дневной бюджет · мобильные −100 % · «Директ помогает» off. Ключи — помодоро + **эстетичная дверь** (`красивый`1001+`эстетичный`830 ≈ pomodoro в рунете) + автотаргетинг (2 базовые). Прайор ExportGPT: CPC 5.66 ₽, ~$115. Отключить при первой органике (1–2 мес, 4.1:24), не >$150. Настройки/кит → `app-studio/knowledge-base/module-4/4.3` (детальные настройки Директа+ЕПК досыпаны 07-28).
   - **Язык боли для ТЕКСТОВ объявлений собран 08-01** → `app-studio/research/ad_pain_language_ru_2026-08-01.{md,csv}`.
     Главное: человек ищет **ИМЯ состояния**, а не просьбу о помощи (`прокрастинация` 4400 : `как перестать прокрастинировать` 20 = 220:1;
     всё семейство «как сосредоточиться» = 10, пол шкалы). Боль рунета — **НАЧАТЬ**, не удержать (`как заставить себя` 40+30 > «сосредоточиться» 10) —
     прямое попадание в трение старта. Звук в рунете зовут **«белый шум» 18 100**, наш `коричневый шум` — 90. ⚠️ РФ в DFS НЕТ вообще
     (санкции; данные = KZ-прокси ≈1/30 US) → точные числа только Wordstat, список для вставки в §6 отчёта. 3 развилки за автором (§7).
   - **ФАКТ по ЕПК №3 на 2026-08-04 (8 дней открутки):** расход 3 433,60 ₽ · показы 3 582 · клики 370 ·
     **CPC 9.28 ₽** (прайор ExportGPT был 5.66 ₽ — реальность ×1.64) · **CTR 10.33 %** (у ExportGPT было 4.39 % — вдвое хуже).
     Разбор вопроса автора «почему у №1 показов на рубль в 4 раза больше»: загадки нет, `показы/рубль = 1/(CTR×CPC)`;
     2.35 × 1.64 = 3.86 — вся разница. Мало показов на рубль = **хорошо**: рубль уходит в клик, а не в пролистывание.
     **Два реальных узла:** (1) **конверсии = 0 у обеих кампаний** — цели не настроены, Директ крутится вслепую,
     цену установки в кабинете не посчитать; (2) **бюджет не ограничитель** — ~430 ₽/день при лимите 60 000 ₽/нед
     (освоено ~5 %), потолок = число запросов (~450 показов/день), рычаг в ширине ключей, не в деньгах.
     Прикидка 🔶: 370 кликов × 15–30 % ≈ 55–110 установок ≈ 31–62 ₽ за установку — проверяется через Umami.
   - **Следом ФБ** (2-й канал, кит НЕ собран) → сравнить Яндекс↔ФБ (поиск-намерение vs лента-импульс).
   - **Спрос на ЗВУК разведан 08-04** (повод — импульс автора добавить переключатель) → `app-studio/research/focus_sound_demand_en_2026-08-04.{md,csv}`.
     Главное: поле шума огромно (white 301k), но его хвост — **сон и железки**, не фокус; растёт только фокус-ветка коричневого
     (`brown noise for studying` +60 %/год, `for adhd` +125 %). Две **боли прямо под нас**: `brown noise no ads` **2900** и
     `brown noise black screen` **2400** — люди слушают шум на YouTube и упираются в рекламу и картинку; наш on-device генератор
     отвечает на это самим устройством. ⚠️ В листинге/объявлениях этот аргумент **не звучит** — узел за текстами.
     Сезон учебной двери (`study/focus music`) — пик сентябрь–март, дно июнь–июль (×3.3): окно открывается через 4–6 недель.

**2. Аналитика поднята (метки → счётчики).** UTM на кампании (`Параметры URL`) → GA4-**листинга** (opt-in `G-HNVT5MM6Y4`, CWS сам тегирует, ждёт трафик) · **Umami** на welcome ЖИВ (`7999549b…`, real-time установки) · **Clarity** (`xto0omit59`) — **задеплоен и ЖИВ, проверено curl'ом на проде 08-04** (обе метки в HTML `welcome/`; прежняя запись «ждёт деплоя» устарела). 3 источника установок: CWS-дашборд · GA4 · Umami. Читать `install/first_visit` (норма 15–30 %), вычет орг-бейзлайна (4.5).
   - **Что можно читать программно (разведано 08-04):** Umami Cloud REST (`https://api.umami.is/v1`, ключ в профиле, Bearer) · Clarity Data Export (`clarity.ms/export-data/api/v1/project-live-insights`, Bearer, **лимит 10 запросов/сут и только 1–3 последних дня**) · GA4 Data API (нужен сервис-аккаунт Google Cloud) · Яндекс.Директ API v5 (нужен OAuth-токен). **У CWS программного доступа к установкам НЕТ** (Publish API умеет только публиковать) — дашборд читается только глазами/браузером.
   - **Слепое пятно:** внутри экста НЕТ ни одного события (по ДНК «слежка = v2»). Видно «сколько поставили», не видно «летают ли».

**3. Reddit → ВЕБ-РЕЙС, не листинг (стратегия сессии).** Метод (видео Капитана, конспект → `knowledge-base/module-5/5.4`): на Reddit **веб > расширение** (человек не искал → install = трение). Веб-рейс уже есть — `focus-pages/flight/` («Рейс к звезде» как страница). ⚠ **flight/ слегка устарел** (hearth.js 07-26 vs экст 07-27, не доехал `QUENCH=0.4`) + **вопрос standalone** (звук в самой вкладке, не через offscreen). Архитектура (модуль 2.6): вебсервис-рейс = ядро (пиарим в Reddit), CWS-экст = лаунчер, открывающий его → под стор-траст+Яндекс. Прогрев Reddit: аккаунт **5 мес** (актив), **≥20 комментов за 2–3 нед** без ссылок → потом пост. Это оживший **продуктовый контур Герольда** (Reddit/PH/Store).

**Узел (аппрув-остаток) — закрыт 08-04:** `RATE_URL` вписан (rate-бар ≥4★ → отзывы CWS), **1.0.1 залит**. Reinstall-на-uninstall (`STORE_URL`) в этой волне НЕ проверялся — остаётся узлом.

**Аналитика — решения автора 08-04:** **Clarity снят** (обычный welcome-лендинг кликать не по чему; тепловая карта была логикой ExportGPT, здесь вторична и неканонична) · порядок подключения: **Umami → GA4 → Директ**. Пульс-скрипт + CSV-реестр строим на Umami; ключ за автором (Umami Cloud → профиль → API keys).

**Листинг — добор боли 08-04 (за автором вставить в стор):** в `lab/meta-keywords.md` заведён блок «🩸 БОЛЬ-ключи» (`brown noise no ads` 2900 · `brown noise black screen` 2400 · `deep brown noise` 1600), в `lab/listing-draft.md` вписаны абзац «Most people get their brown noise from a streaming tab…», буллет, FAQ-пункт и **вариант D** краткого описания. ⚠️ Правка пока **только EN** — 53 локали (`lab/full/*.md`) разошлись с EN-текстом, пересборка отложена.

**ГЛАВНЫЙ приоритет — ЖИВЫЕ ТЕСТ-ЮЗЕРЫ + наблюдение** (автор готов платить). Разведано в сессии: **платные usability-платформы** (Userbrain ~$30/тест — think-aloud видео, ноль соц-просьбы; фит под психологию автора — платить = убрать просьбу) · Reddit-сообщества · «на шаг от друзей» (реферал, не повтор). Голоса → завести `ГОЛОСА.md`. **Разворачивать свежей сессией.**

**Дальше по методу (product):** реклама + **UTM-метки** (атрибуция источников — на рекламном этапе, не раньше) · аналитика поведения = **v2** (по DNA «слежка — не MVP»; таймер на старте private = отстройка, шину событий закладываем) · веха 2 = 2000 юзеров (порог разговора о монетизации: платным станет ДОСТУП целиком).

**Отложенные узлы (публикацию не блокировали):** язычок-когерентность рейс-сцене (нить тёплого света вместо жар-плашки) · звук-верстак (`SOUND.md`) · журнал рейсов (накопление) · тест звука в наушниках.

**Открытый хвост:** чип «проверить privacy-соответствие `save-chatgpt-as-pdf`» (счётчики в расширении vs декларация — отдельная сессия).

Триггер новой сессии: «**sound-focus-timer ОПУБЛИКОВАН в CWS, реклама Яндекс (ЕПК) учится, аналитика поднята (GA4-листинг + Umami живы; Clarity код готов — ЖДЁТ деплоя `focus-pages`).
ДАЛЬШЕ по продвижению: (1) задеплоить Clarity + следить за установками → отключить рекламу при органике; (2) Reddit → вести на ВЕБ-РЕЙС `focus-pages/flight/` (сперва обновить hearth.js до 07-27 + проверить standalone-звук), прогрев аккаунта ≥20 комментов/2–3 нед; (3) ФБ-кит (2-й канал) → сравнить; (4) ГЛАВНОЕ — живые тест-юзеры (платные usability-платформы, автор готов платить) свежей сессией.
Плюс техническое: **залить `dist/minimalist-timer-1.0.1.zip`** (6 прод-фиксов, критбаг — Chrome гасил offscreen за тишиной). Метод: `app-studio/knowledge-base/module-4` (реклама/ЕПК) + `module-5/5.4` (Reddit — видео-конспект). STATE + DECISIONS: продвижение 07-29, прод-фиксы 08-01**».

# State — где мы сейчас

Последнее обновление: **2026-07-10** (шаббат: звук пересобран · пульт-компас (side-panel) · свежие данные · переоснова гипотезы)

## Фаза
Веха 1 (до публикации в CWS). Звук пересобран и работает; собран **живой пульт-компас** (side-panel),
грузится распакованным. Гипотеза перепроверена по методу — держится (🚀 в рэке студии).
Дальше: обкатать таймер-поток в поле + вернуть offscreen-персистентность звука.

## Что построено и работает (в файлах)
- **`extension/engine.js`** — движок пересобран: **Brown-ядро** (тёплый brown-шум + реверб + 3 несоизмеримых
  дрейфа; дрон и пульс вырезаны). Дирижёр фаз (ручей→собирание→ткань→рассвет→ниточка) цел. Есть:
  динамический arousal · **два слоя `tier` basic/premium** · **слой гармонизации** (премиум-эксперимент:
  редкие мягкие консонансные тоны, C-пентатоника). Крутилки: `energy` (яркость, тёплый потолок) ·
  `masking` (ТОЛЩИНА стены, не яркость) · `volume` · `harmony`.
- **`extension/sidepanel.html` + `sidepanel.js`** — **пульт-компас** (главная поверхность): орб-центр
  (звук-душа: дышит, теплеет; клик = вкл/пауза) + **убывающее кольцо** (время: крути=завёл, безцифренно,
  цифра лишь пока крутишь — Time Timer). ⏻ = стоп. Настройки по «настроить»; премиум раскрывает гармонизацию.
  Движок крутится ПРЯМО в панели (v0.2). ⚠ логика вынесена в `.js` — MV3 не исполняет inline-скрипт.
- **`extension/manifest.json` + `background.js`** — форма = **side-panel**: клик по иконке открывает панель.
  Права `sidePanel`+`storage`.
- `extension/ui.html` (прототип пульта, лёг в sidepanel) · `dev.html` (голый стенд движка) · `offscreen.*`+
  `popup.*` (архитектура «спутника дня» — пока в стороне, вернём при персистентности).
- `AUDIENCE.md` — + веб-разведка голосов r/ADHD (07-09, пересказ поиска 🔶). `lab/taste2.html` — исходная
  дегустация (материал Brown-ядра). `schemas/` — схемы сущности.

## Данные ниши (DFS свежий 07-09/10) ✅
- Двери живы и крупнее: `visual timer` 22.2k · `time blindness` 12.1k · `body doubling` 33.1k ·
  `brown noise for studying` 1.6k · `adhd focus music` 2.9k.
- **KD на наших углах зелёный**: aesthetic pomodoro 🟢7 · brown noise studying 🟢10 · adhd white noise 🟢10 ·
  time blindness 🟡23 · body doubling 🟡33 · visual timer 🟡38 · focus timer 🔴45 (не в лоб).
- **Деньги — в ADHD-боли, не в дженерике**: CPC `adhd focus app` $5.80 · `app for adhd adults` $5.36 ·
  `body doubling` $3.87; дженерик дёшев (visual timer $0.47, brown noise $0.39).
- Mobile SERP «adhd focus app» = AI-обзор + листиклы + Reddit (r/adhdwomen 50+ = золото вербатима) + апп-пак.
  Запрос информационный; App-Store-спрос внутри стора (слепая зона — нужен AppFigures).
- Перепроверка гипотезы по методу держится ~37-40/50, 🚀 обоснован. Крупный игрок (Endel/Brain.fm) ≠ стоп.

## Открытые узлы
1. **Таймер-поток — обкатать в поле.** Черновая логика: орб=вкл/пауза · кольцо=сессия · ⏻=стоп. Юз-кейсы
   выведены (просто-звук / сессия / чувство-времени). Проверить по-человечески, переставить по ощущению.
2. **Offscreen-персистентность** — вернуть «спутника дня» (звук переживает закрытие панели; сейчас звук в панели).
3. **Настоящие материалы-характеры** (пурга/дождь как непрерывные примеси, премиум) — сейчас характеры =
   пресеты energy/masking. Endel монетизирует ровно Nature Elements/Rainy = премиум-рычаг.
4. **Одушевление орба** — ось «характер» (микро-реакции на старт/паузу/поток), НЕ маскот; растёт со временем
   (дар автора — iOS-характер).
5. **Маскировка → целить в речевую полосу** (300–3000 Гц) — будущее уточнение «стены».
6. **Запись-«лес» сессий** (квадратики автора: обязательство + накопление, без вины/слежки) — пост-запуск, не MVP.
7. Хвосты: имя (раб. «Tide», без «Pomodoro») · иконки-PNG · листинг (двери aesthetic pomodoro + «no ads»/приватность) ·
   детент-звук крутилки (опц., глушить в потоке) · **git-репо у проекта НЕТ** (завести, приватный) ·
   глубокий вербатим r/ADHD (r/adhdwomen тред).

## Следующий шаг
**Обкатать пульт-компас в поле + отладить таймер-поток.** Триггер для новой сессии:
«**работаем над sound-focus-timer — щупаем пульт-компас (side-panel) в браузере: логика орб/кольцо/выкл,
потом offscreen-персистентность звука и настоящие характеры-примеси (пурга)**».
